# ⚠️ 本文件是范例模板，会随仓库一起提交、评委可见 —— 请勿在此填入真实密钥，只保留空值。
# 实际使用时：先复制本文件为 .env（cp .env.example .env），再把值填进 .env。
# .env 已被 .gitignore 排除，不会被提交。

# ===== 模型推理（仅服务端使用）=====
# 调用方式：CodeBuddy Agent SDK（@tencent-ai/agent-sdk），已实测通过文字与图片调用。
# 认证不走 HTTP 地址与自定义 header，而是由 SDK 读取下列环境变量，
# 因此这里没有 BASE_URL。
# 模型由上游动态选择，同一份代码两次实测结果不同（文字曾返回 hy3 与 glm-5.3，
# 含图曾返回 hy4-preview-f 与 glm-5.3-flash），因此绝不硬编码模型名，
# 只能在响应里回读本次实际使用的模型。
CODEBUDDY_API_KEY=
CODEBUDDY_INTERNET_ENVIRONMENT=internal

# ===== 服务端 =====
PORT=3000
NODE_ENV=development

# 模型请求超时（毫秒），对应说明书 5.3 的 60 秒约束
MODEL_TIMEOUT_MS=60000
