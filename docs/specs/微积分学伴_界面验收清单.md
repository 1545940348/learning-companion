# 微积分学伴 · 界面验收清单

> **版本 V1.0 ｜ 2026-09-17 ｜ 产出：成员 A（`apps/web`），对应待办 `P-A9`**
>
> 用途：演示前走查、改动后的回归依据、以及向评委说明「哪些能力是真的、哪些还没接入」。
> 上游口径见 [`微积分学伴_初赛补充说明.md`](./微积分学伴_初赛补充说明.md) **§8.2 核心闭环**与 **§8.4 真实性底线**；
> 能力真实状态见同一文件 **§3.4**。本清单与代码冲突时**以代码与回归脚本为准**。
>
> **本文件只追加新版本，不改写旧版本。**

---

## 0 这份清单的边界（先看）

| 项 | 说明 |
|---|---|
| **它能做什么** | 逐项给出「从哪进、怎么点、该看到什么」，让验收可执行、可判定、可复现 |
| **它不能做什么** | **不能替代浏览器视觉验证。** 开发环境无法安装 `agent-browser`（已知限制 `I8`），布局、配色、动画、真实点击链路**只能人工实点** |
| 自动覆盖到哪 | `npm run verify:render -w @lc/web`（**58 项**）以服务端渲染断言"渲染不抛异常"与"关键标注确实出现在 HTML 里"；交互态由 `verify:flow`（**32 项**，真实 HTTP）覆盖 |
| 判定符号 | ✅ 期望出现 ｜ ⛔ 期望**不**出现 ｜ 👁 只能人工判定 |

### 走查前置

```bash
# 服务端（mock 通道，无需密钥）
cd apps/server && MODEL_PROVIDER=mock PORT=3000 npx tsx src/index.ts
# 前端
npm run dev -w @lc/web        # http://localhost:5173
```

⛔ **必须在 mock 通道下走一遍、在真实通道下再走一遍**：两条路径的标注规则不同（见 §4）。

---

## 1 三入口齐全（说明书的硬要求）

| 编号 | 入口 | 操作 | 期望结果 |
|---|---|---|---|
| A-1 | 文字 | 「材料」面板查看输入区 | ✅ 有单次字数上限提示（3000 字）✅ 有提交按钮 |
| A-2 | 图片 | 面板内点击「上传图片」 | ✅ 入口存在；✅ **如实说明图片识别未接入**，不假装能识别 |
| A-3 | 语音 | 面板内点击「上传语音」 | ✅ 入口存在；✅ **如实说明语音识别未接入** |
| A-4 | 上限 | 连续提交到超过 5 份或 15000 字 | ✅ 拒绝并说明上限（材料上限 5 份 / 15000 字），⛔ 不静默截断 |

> `A-2`/`A-3` 的要点是**入口在、且说实话**。入口有而能力没有时，界面必须说"未接入"（§8.4）。

---

## 2 六大面板与核心闭环（对应 §8.2 九步）

按 §8.2 的顺序走一遍，每步都能看到预期结果即算通过。

| 编号 | 闭环步骤 | 入口 | 操作 | 期望结果 |
|---|---|---|---|---|
| B-1 | 零材料提问 | 「答疑」面板 | 不传材料直接提问 | ✅ 回答标注**未基于你的材料**；✅ 提示可上传讲义获得更贴合的回答 |
| B-2 | 上传讲义 | 「材料」面板 | 粘贴一段含「单调性」的讲义并提交 | ✅ 材料卡片出现；✅ 页脚材料版本 `v1`；✅ 提示解析出 N 个知识点 |
| B-3 | 图谱构建 | 「图谱」面板 | 观察节点 | ✅ 节点数 ≥ 1；✅ 节点带状态文案；⚠️ **无边时明确说明"没有关系边"且界面不布线** |
| B-4 | 缺口发现 | 「知识点与前置依赖」 | 查看前置依赖区 | ✅ 单调性材料下，**导数**显示为 **`MISSING`／材料未覆盖**；✅ 给出理由 |
| B-5 | 一键补充 | 缺口行 | 点「补上这一段」 | ✅ 出现 **`AI 补充：非上传讲义内容`** 横幅；✅ 状态转为 `SUPPLEMENTED`／AI 已补充；✅ 验证状态显示**未验证** |
| B-6 | 符号验证 | 同上 + 页头徽标 | 观察验证状态 | ⛔ **当前不可演示**：`/api/health` 的 `verification.available` 为 `false`，页头显「符号验证：未接入」，`/api/gap` 停在 `SUPPLEMENTED`。**这是如实标注，不是缺陷**（`B3`） |
| B-7 | 继续答疑 | 「答疑」面板 | 基于新材料再问一次 | ✅ 回答标注**基于你的材料**；✅ 每块带来源类别与验证状态 |
| B-8 | 练习 | 「练习」面板 | 选主题与来源后取题 | ✅ 固定题与自编题可分；✅ 提交前**不显示答案**；✅ 自编题解析**不算作你材料的证据** |
| B-9 | 画像更新 | 「画像」面板 | 补充缺口后再看画像 | ✅ 掌握状态出现变化；✅ 说明「我已掌握」**不会把材料未覆盖改判为已覆盖**；✅ 说明 AI 已补充**不等于已验证** |

> `B-4`→`B-5` 是演示的主线。**缺口必须是"材料确实没讲"**，不是模型随手判的（用例 E1/E2）。

---

## 3 联动与交互（`P-A8` 新增）

| 编号 | 场景 | 操作 | 期望结果 |
|---|---|---|---|
| C-1 | 卡片 → 图谱 | 点知识卡片上的「在图谱中查看」 | ✅ 卡片高亮（`kp-card-focus`）；✅ 图谱中对应节点高亮；✅ 图谱**自动滚动**到该节点 |
| C-2 | 图谱 → 卡片 | 点图谱中的某个节点 | ✅ 该节点高亮；✅ 上方对应卡片同时高亮 |
| C-3 | 取消 | 再点一次，或点「取消图谱高亮」/「取消选中」 | ✅ 两侧高亮同时消失 |
| C-4 | 选中项不在图谱里 | 选中一个图谱中没有的概念 | ✅ **如实说明**"不在当前图谱的节点里"，⛔ 不假装定位成功 |
| C-5 | 无副作用 | 反复点选 | ✅ 不产生网络请求、不产生提示条、不改变任何六态与画像 |

---

## 4 真实性与标注（§8.4 底线，逐条核对）

| 编号 | 场景 | 操作 | 期望结果 |
|---|---|---|---|
| D-1 | mock 通道 | `MODEL_PROVIDER=mock` 打开页面 | ✅ 页头**醒目横幅**，含「不代表真实模型输出质量」 |
| D-2 | mock 逐条标记 | 在 mock 下提问 | ✅ **每一条**回答都带 `mock 演示数据` 标记（不是只在页头提一次） |
| D-3 | 真实通道 | `MODEL_PROVIDER=deepseek` 且配好密钥 | ⛔ 不出现任何 mock 横幅与 `mock 演示数据` 标记（`verify:render` 有断言锁住） |
| D-4 | 符号验证未接入 | 任意通道 | ✅ 页内明确说明"补充内容与推导一律标为未验证" |
| D-5 | 未接入能力总览 | 页面页脚 | ✅ 列出尚未接入项：图片与语音识别、符号验证引擎、教师视图、多智能体编排、错题归因 |
| D-6 | 教师视图 | 浏览全页 | ⛔ **不渲染任何教师视图面板**（空面板比无入口更易被误读为已实现） |
| D-7 | 来源三类 | 展开任意「依据详情」 | ✅ 来源类别取自 ① 讲义原文 ② 基于材料的解释推导 ③ AI 补充；✅ AI 补充内容**不因展开而消失** |

---

## 5 错误态与重试（`§5.4`）

| 编号 | 场景 | 操作 | 期望结果 |
|---|---|---|---|
| E-1 | 服务端未启动 | 关掉服务端后打开页面 | ✅ 页头显「服务端未连接」，⛔ 不白屏 |
| E-2 | 可重试失败 | 制造一次超时（`MODEL_TIMEOUT` 或 `SESSION_STALE`） | ✅ 出现**真正的重试按钮**，点它重放同一动作 |
| E-3 | 不可重试失败 | 制造一次 400 入参错误 | ⛔ **不给**重试按钮（重试没有意义，给了只会误导） |
| E-4 | 失败保留输入 | 提交材料失败 | ✅ 输入框内容**仍在**；✅ 成功后才清空 |
| E-5 | 单飞 | 一次解析未完成时点答疑 | ✅ 拒绝并说明"上一个操作还没完成"；✅ 两个动作不会互相覆盖忙碌态 |
| E-6 | 版本过期 | 材料更新后再看旧回答 | ✅ 旧回答被标为**依据已更新**，或响应被丢弃并告知（用例 `E8`） |

---

## 6 窄屏与可用性（`P-A12`）

| 编号 | 场景 | 操作 | 期望结果 |
|---|---|---|---|
| F-1 | 双栏折叠 | 窗口宽度拖到 < 900px | 👁 布局变单列，两列内容不重叠 |
| F-2 | 手机竖屏 | 视口 ≤ 560px | 👁 单列 + 留白收紧；知识点卡片单列排布 |
| F-3 | 长公式 | 提交含超长 LaTeX 的材料 | 👁 **不撑破卡片、不出现横向滚动条** |
| F-4 | 长引用编号 | 展开依据详情中的长 `refId` | 👁 能断行，不溢出 |
| F-5 | 图谱 | 节点很多时 | 👁 图谱区**自身**横向滚动，整页不被撑宽 |
| F-6 | 缩放 | 浏览器缩放 150% | 👁 无内容被裁切 |

> `F-*` 全部只能人工判定（`I8`）。代码层已对 `overflow-wrap: anywhere`、`max-width: 100%`
> 与 `@media (max-width: 560px)` 做了防御，但**"写了 CSS"不等于"看起来对"**。

---

## 7 走查记录（每次演示前复制一份填写）

| 项 | 内容 |
|---|---|
| 走查日期 / 执行人 | |
| 通道 | `mock` ／ `deepseek` |
| 浏览器与版本 | |
| 不通过项编号 | |
| 处置 | 记入 `docs/todo.md` 或当轮 changelog；**未修完的不得在演示中绕过** |
| 自动脚本结果 | `verify:all` ___ ／ `verify:flow` ___ ／ `verify:dist` ___ ／ `verify:render` ___ |

---

## 8 版本修订记录（只追加，不改写上文）

### V1.1 ｜ 2026-09-21 ｜ 产出：成员 C（远端审查后的状态复核）

**背景**：`B3` 符号验证引擎已于 2026-09-20 落地，并随 PR #12 并入 `main`（现 `72ae5ba`）。
本清单 V1.0 写在 `B3` **之前**，其中若干条状态随之改变。**上文一字未动，冲突以本节为准。**

| 上文条目（V1.0） | V1.0 的写法 | V1.1 现状（2026-09-21 按代码与回归核对） |
|---|---|---|
| `§2 B-6` | ⛔ **当前不可演示**：`/api/health` 的 `verification.available` 为 `false`，页头显「符号验证：未接入」，`/api/gap` 停在 `SUPPLEMENTED` | ✅ **已可演示**：`verification.available` 为 `true`，页头显「**符号验证：可用（mathjs）**」；`/api/gap` 端到端可到 `VERIFIED` + `symbolic`，由 `verify:flow` ⑥/⑦/⑧ 锁住。⚠️ 两条边界如实标注：**真实模型通道是否按新 schema 返回 `claims` 尚未实测**（回归全走 mock，届时可能如实落 `unverified`）；它**不是通用 prover**，只覆盖四类断言 |
| `§4 D-4` | ✅ 页内明确说明"补充内容与推导一律标为未验证" | ✅ **仍然成立，无需改动**：该提示是**条件渲染**（仅当 `verification.available === false` 时出现）；引擎可用时不出现，措辞本身准确 |
| `§4 D-5` | ✅ 列出尚未接入项：图片与语音识别、**符号验证引擎**、教师视图、多智能体编排、错题归因 | ⛔ **V1.0 该条已失真**（把已接入的项列为未接入）→ **已修正**：页脚"本页尚未接入"清单**不再含「符号验证引擎」**，现为「图片与语音识别、教师视图、多智能体编排、错题归因」；并**新增**页脚块「符号验证的边界」，写明覆盖四类断言、超出范围一律标「未验证」 |
| `§0` 自动覆盖范围 | `verify:render` **58 项** ／ `verify:flow` **32 项** | 数值已随多轮扩写上升：`verify:render` **107 项** ／ `verify:flow` **46 项**（09-20 实测口径）。**以脚本实时输出为准**，本节不复述后续变化 |

**V1.1 的可复跑判据**：

1. mock 通道打开页面，页头徽标显示「符号验证：可用（mathjs）」；
2. 页脚「本页尚未接入」清单内**不出现**"符号验证引擎"字样，且能读到「符号验证的边界」块；
3. `npm run verify:render -w @lc/web` 与 `npm run verify:flow`（需先起服务端）全绿。

> **口径提醒**：本文件与代码冲突时**以代码与回归脚本为准**（见文件头 §0）。

---

## 9 V1.2 修订（2026-09-21 · 视觉接入与在线重部署）

**背景**：图片识别已于 2026-09-21 随 PR #14 并入 `main`（`eb06ab6`）并**部署到线上**（第 005 次发布）；
`content-disposition` 也已定案为平台行为。V1.0 原表与 V1.1 修订节中若干条随之改变。
**上文一字未动，冲突以本节为准。**

| 上文条目 | 上文写法 | V1.2 现状（2026-09-21 按线上实测与回归核对） |
|---|---|---|
| `§1 A-2` 图片 | ✅ 入口存在；✅ **如实说明图片识别未接入** | ✅ **已接入**：点「上传图片」选图后由**服务端**识别为文本，识别结果可**就地纠错**；线上真实通道实测 `POST /api/parse` → `200` / **2.07 s** |
| `§1 A-3` 语音 | ✅ 入口存在；✅ 如实说明语音识别未接入 | ✅ **仍然成立**（`D3` 定走浏览器内置识别，服务端不转写） |
| `§2 B-6` 符号验证 | ⛔ 当前不可演示 | ✅ **已可演示**（V1.1 已修正，此处仅同步口径） |
| `§4 D-5` 页脚清单 | V1.1 时为「图片与语音识别、教师视图、多智能体编排、错题归因」 | ⛔ **V1.1 该条现已失真**（图片已接入）→ **已修正**：页脚清单现为「**语音识别**、教师视图、多智能体编排、错题归因」 |
| 在线访问（V1.0 未列） | — | 新增两条：① 线上快照**已与 `main` 一致**（`I37` 关闭，安全响应头实测生效）；② `content-disposition: attachment` **已定案为平台网关行为**（直连实测 + 应用源码无此头 + `server: cbrgw`），但**"点开是打开还是下载"仍需人工点一次** |

**V1.2 的可复跑判据**：

1. 页脚「本页尚未接入」清单内**不出现**"图片"字样，也不出现"符号验证引擎"；
2. 页头徽标显示「符号验证：可用（mathjs）」；
3. `npm run verify:render -w @lc/web`（107 项）与 `npm run verify:all -w @lc/server`（301 项）全绿。

> ⚠️ **待人工判定项（不许用自动化代替）**：浏览器点开在线链接是"打开页面"还是"下载文件"。
> 依据：`curl`/直连 python 都只看到响应头，**看不到浏览器的处理行为**。
