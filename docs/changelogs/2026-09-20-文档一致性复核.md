# 2026-09-20 · 文档一致性复核（`B3` 与第一批 `W0` 落地之后）

- **性质**：审计与修正记录。做法照 `doc-drift-audit`：**先取实况，再读文档**，逐处核实后修正并留痕。
- **触发**：同日两轮落地（第一批 `W0` ＋ `B3`）改动面很大（23 改 + 9 新增），文档极可能已经漂移。
- **原则**：**不涂改历史**。历史留档（旧 changelog / 旧 plans / 旧 reviews）里的旧数字**一律保留**；
  需要澄清的用**追加批注**，不改写原文。

---

## 一、本轮实况（先取，后读）

| 项 | 实测 |
|---|---|
| 三分支 | `HEAD` = `main` = `origin/main` = **`3da6f4d`**（一致） |
| 工作区 | 未提交：**23 改 + 9 新增**（两轮合并，见文末清单） |
| 回归 | `verify:predeploy` **退出码 0**；**506 项**（`all` 263 / `flow` 46 / `dist` 11 / `render` 107 / `serve-web` 43 / `repo` 36） |
| 警告类 | `lint` 0/0；`lint:boundaries` 0 error / 6 warn（`no-circular` 已转阻断）；`verify:file-size` **7 个超限** |

## 二、查出的漂移（逐条）

| # | 位置 | 漂移 | 类别 | 处置 |
|---|---|---|---|---|
| 1 | `docs/todo.md:9` | 「回归现值 **433**」 | 自述过时 | 改为 **506** |
| 2 | `docs/知识库.md` §9 | 「`verify:repo` 的 **34** 是恒定值 …任何工作区状态下都是 34」 | 自述过时 | **追加批注**（不改原文）：现值 **36**（`licenses` 10→11），并说明"恒定性"论点指的是**子脚本内部计数**，不是总数 |
| 3 | `tech/2026-09-17-B-提示词与工作流配置.md` §6 | 「符号验证**未实现**」「图谱关系边**未产出**（尚未要求 `edges`）」 | **自述过时（两行全假）** | 按实况更新 + **删除线保留原表述**；补修订说明 |
| 4 | `tech/2026-09-17-符号验证引擎选型-纯TS.md` §4.4-5 | 「仍设超时保护（包裹 **`Promise.race`**）」——**这条建议是错的** | **错误建议（最危险）** | **追加 §8 订正记录**（不改原文）：`Promise.race` 对同步计算无效；切线判据改 `rationalize`；`derivative` 静默返回 0 的陷阱；`MathClaim` 形状 |
| 5 | `tech/2026-09-17-云托管部署说明.md` L132-133 | 命令注释写死「18 项 / 190 项」 | 自述过时（会误导操作者） | 去掉写死的项数 + 补注"现值查 `知识库` §9 / `todo.md` 的 `P-C5`" |
| 6 | `plans/2026-09-20-后续全量推进（按阻断关系）.md` §九 | 「**所有工作项均未执行**」 | 自述过时 | **追加 §十 执行状态**（不改原文）：已完成/未完成/两条判断被实况确认 |
| 7 | `plans/2026-09-20-…可行性订正与实施卡片.md` §五 | 「工作项均未执行」「未改 `todo.md`/`知识库.md`」 | 自述过时 | **追加 §六 执行状态**：8 条订正的落地情况 + 卡片里两处不能照做的地方 |
| 8 | `docs/README.md` | 头部「最近整理：2026-09-17」 | 入口过时 | 改为 2026-09-20，并加一句"查实时状态看 `todo.md`" |

**未发现的漂移**：`README.md`（仓库根）与 `docs/specs/微积分学伴_参赛说明.md` 已在同日两轮里同步，
本轮反向搜索无残留（见 §四）。

## 三、⚠️ 一处**对自己改动**的登记（`docs/README.md` §六 新增行）

`docs/README.md` 写着「`specs/` ★ 规格与约束正本（**不可改写，只可追加新版本**）」。
而本轮之前的 `B3` 工作里，对 **`specs/微积分学伴_参赛说明.md`** 的 §2.4 / §4.1 / §4.2 / §6 做了**就地更新**
（把"符号验证未落地""在线链接未完成"改成实况）。

**为什么仍然这么做**：它是**提交材料的口径依据**，而其中**低报了一个 P0 能力**。
按"不事后补造"的反面同样成立 —— **既不许补造没发生的，也不该把已发生的说成没发生**。

**如实登记，不隐藏**：已在 `docs/README.md` §六 加一行，写明改动范围、原因、与
"specs 只追加不改写"的张力，以及**建议的后续处置**（把"能力状态"从 specs 移出到 `todo.md`，
specs 只留规格 —— 需负责人拍板）。改动均在原行内以"✅ 已落地 / 已交付"标注，**未删除原表述**。

## 四、反向搜索（收尾自查）

```bash
grep -rn "合计 433\|433 项\|194 项\|graph 53\|尚未部署\|28 项" README.md docs/todo.md docs/知识库.md
```
- 命中项**全部落在历史留档文本内**（changelog 的"演进"叙述、`I9` 行的历史记录），**按原则保留**；
- 需要更新的 8 处（§二）已全部改完，`docs/` 各处表格列数校验 **0 处不一致**。

## 五、本轮改动清单

`docs/todo.md`、`docs/知识库.md`、`docs/README.md`、
`docs/tech/2026-09-17-B-提示词与工作流配置.md`、`docs/tech/2026-09-17-符号验证引擎选型-纯TS.md`、
`docs/tech/2026-09-17-云托管部署说明.md`、
`docs/plans/2026-09-20-后续全量推进（按阻断关系）.md`、
`docs/plans/2026-09-20-后续全量推进-可行性订正与实施卡片.md`、本文件。

> **未做**：本轮只做"文档与实况对齐"，**没有改任何代码或配置**，因此**不触发计数变更**
> （506 这个数在两轮落地时已同步四处）。
