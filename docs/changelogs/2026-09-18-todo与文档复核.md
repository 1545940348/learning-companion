# 变更记录 · todo 与文档复核（2026-09-18）

- **日期**：2026-09-18
- **触发**：负责人一句「检查 todo」。**没有当轮 `plans/`**（不是排期轮，是复核轮），按"不得事后补造"如实记录，不补计划文件
- **方法**：按 `docs/README.md` §五 的留痕要求 —— **先取实况，再读文档；数字一律本轮实测后再落笔；历史留档不改写**
- **依据与证据**：下每一条都能对上本轮的一次命令或一次 API 调用

---

## 1 复核口径与实况

| 项 | 实况（2026-09-18 10:0x–10:4x） |
|---|---|
| 远端 `main` | **`77c2e9ed`**（已含 #8、#9） |
| 开放 PR | **0 个** |
| 仓库可见性 | **public**（`B2` 仍有效） |
| 本地 `main` | `1957fc2` —— **落后于远端**（合并是在服务端造的），另有一处已改、4 个未跟踪文件 |
| 本轮实测 | `serve-web` **18** ／ `all` **190** ／ `flow` **36** ／ `render` **88** ／ `repo` **33**（15/10/8 ＋ 2 条"注意"）＝ **365 项，0 失败**；`dist` **11** 项本轮未重跑（其输入未被本轮改动波及，今日早前已实测） |

> 复核前先做了两部分实况取证：**① 用 API 取 main 上的四份文档**（本地落后，直接读工作区会读到旧版）；
> **② 在真实路径下跑验证**：`apps/server/scripts/verify-serve-web.mjs` 直接执行得 **18/18**，
> 且其第 3 段自动探测**真找到了 `apps/web/dist`**。

### 端到端复验（本轮最强的一条证据）

补齐 #9 的 `index.ts`/`env.ts`/`serve-web.ts` 后，用**真实前端产物**起真服务（`MODEL_PROVIDER=mock PORT=3014 npx tsx src/index.ts`）：

| 请求 | 结果 |
|---|---|
| 启动日志 | `web.dist.mounted {"dir":"D:\\...\\apps\\web\\dist"}` —— 路径自动探测成功 |
| `GET /` | **200 `text/html`**，且确认是学生界面（含 `<div id="root">`） |
| `GET /assets/index-DSX10U23.js` / `…css` | 200 `application/javascript` / 200 `text/css` |
| `GET /dashboard` | 200 首页（SPA 回退生效） |
| `GET /api/health` | 200 `application/json` |
| `GET /api/不存在的路由` | **404 `application/json`** —— 顺带证实了 PR #9 正文"404 JSON"的说法（先前探针返 `text/html` 是探针没挂应用的 JSON 404 catch-all，不是缺陷） |
| `GET /assets/not-built.js` / `/favicon.ico` / `/.env` | 全部 **404**（合并轮收紧回退范围的效果） |
| `GET /apifoo` | 200 首页（`isApiPath` 精确化后不再误伤） |
| `POST /api/tutor`（`sessionId:null` 轻路径） | 200 JSON，未被回退 |

收尾：`netstat` 确认 3014 无 `LISTENING`。

---

## 2 修正清单（逐处，均先核对再改）

### 2.1 `docs/todo.md`

| # | 问题 | 处置 |
|---|---|---|
| 1 | **L 泳道表（「✅ 可立即开始」）里有 9 条早已完成的条目仍以"待做"形态挂着** —— `L3`（`P-B11` ✅ 第 8 轮）、`L7`（`P-C17` ✅）、`L8`（`P-C15` ✅）、`L10`（`P-A2` ✅）、`L11`（`P-A9` ✅）、`L12`（`P-A13` ✅）、`L13`（`P-A11` ✅）、`L15`（`P-A12` ✅）、`L14`（脚本已完成） | 逐条标 ✅ 并写明轮次/PR。**这是本轮影响最大的一处**：该表是"挑活"用的，过期的 ⬜ 会让下一个人去认领已完成的活 |
| 2 | `L6` 写"在线链接骨架（**静态托管** + mock 入口）" | 改为 🟡 骨架已完成（PR #9，**同源托管** + `Dockerfile` + `pack:deploy`），并把"载体已定（`D4`=CloudBase 云托管）"写进产出列 |
| 3 | `J7`「部署流程固定加入 `verify:dist`」仍 ⬜，**与 `P-C20` 是同一件事且 `P-C20` 已 ✅** | 标 ✅，写明已由 `verify:predeploy` 落地，并注明"本条长期与 `P-C20` 重复" |
| 4 | `I10`（`知识库.md` 自身过时引用）未标闭合 | 标 ✅ 已修复，并写明三处的闭合情况（§10.2 与 §9 第 7 轮已改好；§2.2 本轮补上） |
| 5 | `§已登记断点` 里「**全量复查轮的产出未提交**」仍以风险形态挂着 —— 那 6 个路径**早已随 `279c546`/`1957fc2` 提交并随 #8 合入 main** | 标 ✅ **已解决**，写明提交与合并路径；保留条目说明它曾存在 |
| 6 | `§已登记断点` 缺"本地再次落后"这一条 | 新增一条（含实测过的对齐方式与"零损失"说明） |
| 7 | 缺"A 侧 `verify:render` 有一条断言偏弱"的登记 | 新增一条（页面级宽正则，未改，登记给 A） |
| 8 | `最近更新` 仍写 2026-09-17 | 改 2026-09-18，并指向本文件 |
| 9 | 第 7 轮泳道表本身声明"不改写"，但其**完成标志列会被误读为现状** | 加一句"状态一律以总表为准"，不改写表内内容 |
| 10 | **`D4` 行比我一次上轮多写了一列**（表头 3 列、该行 4 列），Markdown 不报错、只静默错位 | 合并回 3 列。**这是上一轮我自己引入的**，本轮用脚本逐表核对列数时抓到（共查 16 张表，只有这一处不符） |

### 2.2 `docs/知识库.md`

| # | 问题 | 处置 |
|---|---|---|
| 1 | §2.2「当前交付状态（2026-09-17）」的**在线链接一行仍是"⬜ 未完成…只剩托管落地"**（对外口径，属最高优先的失真） | 改 🟡 **未部署但已无未知项**：载体已定（`D4`）、同源托管本地已实现并验证（`P-C13`）、只剩控制台执行；并补上**默认域名「访问提示中间页」**这一限制 |
| 2 | 同行"源码仓库"写 `main` 含 `#1`–`#7` 全部 | 改 **`#1`–`#9`** |
| 3 | §10.2「已合并记录」**只到 #7**，`#8`/`#9` 未登记（低报） | 补 #8（`3299d647`）与 #9（`77c2e9ed`）两行，并注明 #9 曾冲突与解法、以及"当前没有开放 PR" |

### 2.3 `README.md`

| # | 问题 | 处置 |
|---|---|---|
| 1 | §未实现表「在线链接 / 部署」只写"尚未部署…剩下的只有托管落地" —— **低报**：载体已定、同源托管已实现并验证、只剩控制台 | 补齐现状与中间页限制，编号仍为 `P-C14` |

### 2.4 新增

- 本文件（`changelogs/2026-09-18-todo与文档复核.md`）
- `docs/todo.md` §最近完成 新增一行

---

## 3 明确**没有**改的（守住"不涂改历史"）

- `todo.md` 第 7 轮焦点表、`J1`/`J2`/`J13` 的旧数字（`verify:flow 32`、`dist 8`、`render 42`）—— 历史结论，`J2` 已自带"不要把这两处当现值"的告诫；
- `changelogs/` 与 `reviews/` 里的旧数字（`288`/`334`/`358`）—— 当时确实是那个数；
- `todo.md` 里 `I13` 行的"原 334 项一条都没经过它"—— 引用历史，正确保留；
- **`docs/specs/` 里任何"当前状态"表述** —— `docs/README.md` §六 已登记"specs 只追加不改写，状态列不随实况更新属于设计如此"。本轮**只核对**（确认它仍按设计停留在快照态），**未动一个字**。

---

## 4 边界

- ❌ 没有改任何代码（本轮只动 4 处文档）；
- ❌ 没有动 `apps/web/**`（那条弱断言登记给 A，未代改）；
- ❌ 没有推进任何功能项（`B3` / `P-C14` / `D3` 的处置不在本轮）；
- ⚠️ 本地 `main` 仍落后于远端 —— 需要负责人跑一次 `git fetch origin && git reset --hard origin/main`（见 `todo.md` §已登记断点）。
