# 2026-09-21 · 远端审查 + 状态刷新 + `W0-7` 断言补齐

> **起点**：远端 `main` = `72ae5ba`（PR #12 已合并）；本地 `HEAD`/`main` 落后 **2 个提交**。
> **终点**：本地与远端 `main` 对齐；回归 **506 → 513**；`verify:predeploy` 退出码 0。
> **指令来源**：用户「拉取远端仓库并且自动处理冲突，然后不分类型全量推进 todo 中的剩余任务」。
> ⚠️ **本轮未执行任何远端写操作**（未提交、未推送、未建/合 PR）。

---

## 一、远端审查（只读）与本地对齐

| 项 | 实况 |
|---|---|
| 远端 `main` | **`72ae5ba`** = `Merge pull request #12 from …/feat/b3-and-w0-batch`（2026-09-20T15:38:55Z = 23:38:55 +08） |
| 合并是否无损 | ✅ **无损** —— `git diff-tree 03ac760 72ae5ba` **输出为空**（PR head 树 ≡ 合并结果树） |
| PR 全景 | **13 个全部 closed、0 个 open**（#1–#12 merged；#13 closed 未合并） |
| 远端治理 | **无 GitHub Actions**；**`main` 未设分支保护**；无 releases / tags；`open_issues_count` = 0 |
| 本地 vs 远端（对齐前） | 工作区树 `bc86475` vs 远端树 `901327e2` → **只差 1 个文件**（`docs/reviews/2026-09-20-现状核对与两项新发现.md`，本地独有） |

**关键判据**：`git status` 当时报的 **37 条（26 `M` ＋ 11 `??`）是索引停在 `3da6f4d` 造成的假象**，
远端 `main` 的 172 个文件在本地**全部存在且内容相同** —— 用**树哈希**证明，不是靠 `git diff` 的字母。

**对齐动作**：`git fetch origin` → **`git reset --hard origin/main` 一次通过（exit 0）**。
⇒ `HEAD` = `main` = `origin/main` = **`72ae5ba`**，`git status` 仅余那份本地 review 文档。
**冲突：零**（不需要解冲突 —— PR #12 已在服务端合并，本地只是落后）。

> 📌 **环境结论更新**：`blocked-file-writes-env` 里"本机写不了 `.git/index` 与 ref"的判断**再次失效**
> —— 这一次连 `git reset --hard`（要写 index ＋ 移动 ref ＋ 追加 reflog）都直接跑通。
> 印证该技能自己的告诫：**这是环境态、逐次试**，不要预先把绕法当必由之路。
> 本轮另用 `cp .git/index .learnbuddy/audit-0921/index-backup-3da6f4d` 留了一份索引备份（未用上）。

---

## 二、修对外失真（已随 #12 上线到 `main`）

`B3` 落地后，**页脚仍把「符号验证引擎」列在"尚未接入"里**，而同一页页头已显示「可用（mathjs）」——
这条自相矛盾**已经合进 `main`**，是对外可见的成对失真。

| 文件 | 改动 |
|---|---|
| `apps/web/src/App.tsx` | 页脚"本页尚未接入"清单**删去「符号验证引擎」**（余：图片与语音识别、教师视图、多智能体编排、错题归因）；**新增**页脚块「**符号验证的边界**」——覆盖求导值/切线/单调区间/极值点四类断言，超出课程范围或输入规模一律标「未验证」 |
| `docs/specs/微积分学伴_界面验收清单.md` | 该文件头部写明"**只追加新版本，不改写旧版本**"→ **追加 §8 V1.1 修订节**，逐条对照 `B-6`/`D-4`/`D-5`/§0 的新状态；V1.0 原文一字未动 |

> **`D-4` 复核后判定无需改**：那句"补充内容与推导一律标为未验证"是**条件渲染**
> （仅当 `verification.available === false` 时出现），引擎可用时不出现，措辞本身准确。

---

## 三、文档类零依赖项

| 项 | 处置 |
|---|---|
| `I41` 决策编号重号 | `知识库.md` §14 表**之前**加「编号重号提示」段：写明 `D1`/`D3` **各有两个含义**、分属两套来源（第 6 轮裁定 vs 09-19 拍板）、以及**"凡引用决策必须连内容一起写"**的规则；并在 4 条重复项后**注明来源轮次**用于消歧。编号按"不涂改历史"保留 |
| `D2`/`L17` 母本来源 | `docs/designs/2026-09-17-多模态教学智能体设计方案.md` 加「来源与定位说明」：注明它是**设计参考、不是本项目实现方案**，并列出文中提到而**本项目未采用**的技术（LangGraph / GraphRAG / MinerU / Nougat / VLM / ASR 等）。⚠️ **具体出处无留痕，按"不得事后补造"未代为填写**，待负责人补充 |

---

## 四、`W0-7` 补两条「已登记断点」的断言（本轮唯一代码实质改动）

两条断点都挂在 `todo.md` §已登记断点 里，标注"**如实登记，未造假断言**"。
共同障碍：要断言的逻辑**跑不起来**，不是不会写断言。

### 4.1 前置：把两段逻辑提到无副作用的叶子模块

**新建 `apps/server/src/http/student-reason.ts`**，从 `routes.ts` 移出并导出：

| 移出的逻辑 | 原位置 | 为什么必须移 |
|---|---|---|
| `toStudentReason()`（`I35`） | `routes.ts` 私有函数 | `routes.ts` 在**模块顶层**就 `Router()` 建路由表 —— 验证脚本 import 它会**连 express 一起拉起** |
| `buildDroppedBlocks()`（`I14`，本轮新提取）+ `containsUuid()` | `routes.ts` 内联在响应构造里 | 让"部分通过"的**正面路径**能在不启服务的前提下断言 |

> ⚠️ **这是有意识的接口改动，不是顺手导出**（实施卡片 `卡 0-7` 明确要求在此写明）：
> 它换来的是两条"已登记断点"被**真正关掉**，而不是继续挂着。
> `routes.ts` 侧只留一行注释指向新模块 + 一个 `const droppedBlocks = buildDroppedBlocks(rejected);`。

### 4.2 新增断言（进 `verify:guards`）

`apps/server/scripts/verify-guards-and-errors.mjs` 新增**第 10 组**（7 项）：

1. 前置：用 `validateAnswerBlocks` 造出「引用的来源不存在」的拒绝；
2. ★ **原始 reason 里确实带 uuid**（否则本组等于空跑 —— 防"恒真断言"，见 `I24` 的教训）；
3. ★ **改写后不得出现 uuid**（`I35` 的核心判据，正则 `/[0-9a-f]{8}-[0-9a-f]{4}-/`）；
4. ★ 一块通过 + 一块被拒 → `valid=1 / rejected=1`；
5. ★ `droppedBlocks` 出现且 `count===1`（`I14` 的正面路径，此前只有否定断言）；
6. ★ `reasons` 非空且同样不含 uuid；
7. ★ 无丢弃时 `buildDroppedBlocks()` 返回 `null`（不凭空造出契约字段）。

### 4.3 计数

`verify:guards` **81 → 88**；`verify:all` **263 → 270**；回归合计 **506 → 513**。
**四处计数已同步**：`todo.md` 的 `P-C5` 与头部 / `知识库.md` §9（两处）/ `README.md` 回归段 / 本文件。

---

## 五、部署包重打（`I37` 前置已满足）

本批改动已随 PR #12 并入 `main`，"先提交再打包"这个前置**不再存在**。

```
PACK_DEPLOY_OUT="D:/smallbox/Projects/LearnBuddy-deploy-0921" npm run pack:deploy
```

- 产出 **`D:\smallbox\Projects\LearnBuddy-deploy-0921`** —— **173 → 174 文件 / 2.23 MB**
  （初打 173：远端 172 ＋ 本地那份 review 文档；随后因新增 `student-reason.ts` 再打一次为 174）
- 体积构成：86 `.md` / 39 `.ts` / 15 `.mjs` / 11 `.json` / 10 `.tsx` / 5 无扩展名 / 2 `.docx` / 1 `.cjs`
- 安全检查：✅ 无排除名单目录、无密钥/日志文件，体积在 10 MB 闸门内
- ⚠️ **旧包 `…-deploy-0920`（163 文件）作废** —— 它打在 `B3` **之前**，不含符号验证与图谱边

**下一步（人工）**：云托管控制台 →「本地代码部署 → 上传代码包 → 类型选**文件夹**」→ 选新目录 → 端口 **3000**。
验收判据见 `plans/2026-09-20-I37重部署前置检查清单.md`（`/nope.webmanifest` 与 `/assets/` 必须 404、`/nope` 仍 200、7 条安全响应头齐备）。

---

## 六、本轮验证留痕

| 检查 | 结果 |
|---|---|
| 改前基线（`typecheck` ＋ `verify:all` ＋ `verify:render` ＋ `verify:repo`） | **EXIT=0**，`all` 263 / `render` 107 / `repo`（11＋8）全绿 |
| 改后 `verify:all` | **270 项通过 / 0 失败**，`EXIT=0`（store 22 / errors 34 / **guards 88** / graph 57 / symbolic 69） |
| `npm run verify:predeploy`（含 `build:clean` → `build` → `dist` → `serve-web` → `repo`） | **EXIT=0**（`dist` 11 / `serve-web` 43 / `senstive` 11 / `licenses` 17 含 1 项注意 / `prompts` 8） |
| `npm run lint` | **0 error / 0 warning**（`eslint .` 无输出） |
| `npm run typecheck -w @lc/server` | 通过 |
| 表格校验（`check-tables.py`：`todo.md` / `知识库.md` / `界面验收清单.md`） | **mismatched rows: 0** |

**证据目录**：`.learnbuddy/audit-0921/`（审查脚本、实况快照 JSON、索引备份、各轮日志）。

---

## 七、未做（如实列出）

| 项 | 原因 |
|---|---|
| `I37` 控制台上传与重建 | **只能人工**（本机无 CLI、无凭据） |
| 浏览器视觉验收、`content-disposition` 判定、3 分钟录屏 | **只能人工**（`I8`：环境无 `agent-browser`、不许 `npm install -g`） |
| `P-C18` LearnBuddy 对话记录导出 | 需人工从 LearnBuddy 侧导出 |
| 真实模型通道实测（`I1` 真实边、`B3` 真实 `claims`、`J5` 闭环） | 本机无密钥，回归全走 mock —— **不得声称真实通道已验证** |
| `I39` 锁文件根治 | 本机 npm 写不了已有文件；且**临近打包/录屏不宜动依赖** |
| `P-C4`/`P-C19` 模型通道扩展、`P-B5` 多模态解析 | 需真实通道/密钥，属外部依赖 |
| `P-B8`/`P-B9` 多智能体与错题归因、`P-B15` 素材、`P-B7` 测试记录、`P-B12`/`P-B17` 等 | **本轮未开工**，仍在 `todo.md` 待做 |
| `P-C11`/`L20` `/api/teacher` 聚合 | **本轮未开工** |
| 契约串行项 `I2`/`I3` | **本轮未开工**（契约闸一次只放一个在飞） |
| 阶段 1–8 结构性拆分 | 冻结线 09-25 临近，属零行为变更之外的改动，需专门窗口 |
