# 2026-09-20 · 计划落地轮（第一批 `W0` 项）

- **性质**：执行记录。按 `plans/2026-09-20-后续全量推进（按阻断关系）.md` **第 0 层（零上游、今天就能开）**
  与配套的 `plans/2026-09-20-后续全量推进-可行性订正与实施卡片.md` 动工，本轮完成 **4 项 + 1 处界面修正**。
- **本轮动过的文件**：见文末「文件清单」。
- **未做**（不声称完成）：`H1` 上传重部署（人工）、`B3` 符号验证（B 的工件）、阶段 1 结构搬迁、
  以及 `W0-6`/`W0-7`/`W0-9`…`W0-15` 等其余第 0 层项（见文末「本轮之外」）。

---

## 一、逐项记录

### `W0-1` / `I9`：README 的「尚未部署」对外失真

| 项 | 内容 |
|---|---|
| 落点 | `README.md` |
| 改动 | ① **删掉**「未实现」表里的「在线链接 / 部署」行（原第 207 行，全 README 唯一一处「尚未部署」）；② 在「已实现（可演示）」补在线链接条目（`P-C14`，含线上地址、同源托管、真实 `deepseek` 端到端 `200`、数学结论如实标 `unverified`）；③ 在「已知限制」补**三条真实限制**：默认域名「访问提示中间页」（口径写明是"打开 → 点确定访问 → 可用"，**不写"点开即用"**）、**线上快照落后于 `main`**（`I37` 重部署尚未执行，故线上**不具备**阶段 0 的 7 条安全响应头）、`content-disposition` **待人工判定**；④ 顺带补上"冷启动会先 503 一次"这条不是故障的现象 |
| 验收 | `npm run verify:repo` **35 项 0 失败**；`grep -n "尚未部署\|28 项" README.md` **无残留** |
| 遵守的约束 | **没动**「第三方依赖声明」表（那是 `scripts/check-licenses.mjs` 的三重比对对象，动它会牵动 licenses 检查） |

> ⚠️ **订正记录**：卡 0-1 只说"补两条已知限制"。实际写了**三条** —— 第三条（`content-disposition`）写在
> `plans/2026-09-20-I37重部署前置检查清单.md` §五，那里明说"**交付说明必须带上**"，缺了就是低报。
> 保留卡 0-1 的第 ④ 条要求：`P-C14` 编号仍在文中出现。

### `W0-2` / `I40`：`lint` 残留 3 条 warning

| 项 | 内容 |
|---|---|
| 落点 | `scripts/check-file-size.mjs:120`（`rows`）、`scripts/check-licenses.mjs:66` 与 `:229`（`file`） |
| 改动 | 三处未使用绑定删除：`const rows = await report(limit);` → `await report(limit);`（阶段 4 转阻断的写法改成一行可复制的注释）；两处 `for (const { file, pkg, label } of workspacePkgs)` → `for (const { pkg, label } of …)` |
| 验收 | `npm run lint` → **0 error / 0 warning**（原 0 / 3） |
| 注意 | **未触发计数同步** —— `知识库.md` §9 明写这三条只告警检查"不纳入回归项数" |

> 与卡 0-2 一致：**没有**采用 `_` 前缀那种糊过去的写法。`package.json` 里的 lint 脚本不需要改（本来就是 `eslint .`，自动扫到 `scripts/`）。

### `W0-5` / `I38`：拆 `deepseek.ts ↔ model/index.ts` 的循环依赖（**先验红，再修绿**）

| 项 | 内容 |
|---|---|
| 落点 | 新增 `apps/server/src/model/adapter.ts`；`apps/server/src/model/index.ts`、`apps/server/src/model/deepseek.ts`、`.dependency-cruiser.cjs` |
| 改动 | 把 `ModelAdapter` 类型契约从 `index.ts` **下沉到叶子模块** `adapter.ts`（该文件**不 import 本项目任何模块**）。`deepseek.ts` 改为 `import type … from './adapter.js'`；`index.ts` 改为从 `adapter.ts` 取并**再导出**（保持既有导入路径可用）。`index.ts` 里因此不再需要 `ModelCaller`，已从 import 列表移除（否则 lint 会新出一条未使用告警 —— 刚清零就又脏了） |
| **先验红的做法** | 先把 `.dependency-cruiser.cjs` 的 `no-circular` 从 `warn` 改成 **`error`**，跑一次确认命令**确实报红**（`1 errors, 6 warnings`、退出码 **1**）—— 证明这条规则**有牙齿**；再拆环。 |
| 验收 | `npm run lint:boundaries` → **`0 errors, 6 warnings`、退出码 0**；剩余 6 条**全是** `panels-should-not-import-state-internals`（`cross-check`：与卡 0-5 的预测一致）。`npm run typecheck` 全绿，`verify:all` 全绿 |
| 规则口径变更 | `.dependency-cruiser.cjs` 文件头原写"阶段 0 一律 `warn`，阶段 1 起逐条转 `error`" → 改为**"每条规则在它自己清零之后转 `error`"**（采纳卡片订正 3 的建议：一次性总开关会把门禁推到阶段 4 之后）。文件头同时写下该规则的**转阻断时点**与"阶段 1 必须同批改写 `from`/`to` 路径"的警告 |

### `W0-4` / `I1`：图谱关系边（**三个落点 + 两处订正**）

| 落点 | 改动 |
|---|---|
| `packages/teaching/src/prompt.ts` | `SYSTEM_KNOWLEDGE` 增加关系边要求（方向 `from` = 依赖方 → `to` = 被依赖方；`kind` 六个取值；**"没有把握的边不要写；一条都写不出时 `edges` 返回空数组，不要为了凑边而编造依赖"**），JSON 契约补 `"graph":{"edges":[{"from":"","to":"","kind":"prerequisite","reason":""}]}` 常量 706 → **1012** 字符 |
| `docs/tech/2026-09-17-B-提示词与工作流配置.md` | **同批逐字同步**（`scripts/check-prompts.mjs` 做逐字比对 + 反向检查）。`verify:repo` 的 `prompts` 8 项通过 |
| `apps/server/src/model/index.ts` | `mockKnowledge` 补 `graph.edges` —— **mock 通道原本 `edges` 零命中**，于是无密钥的演示/录屏里"有边"这条路径**既不能证实也不能证伪**。同时把 `createMockAdapter` **导出**（`createModelAdapter()` 读环境变量，回归脚本里没法保证 `MODEL_PROVIDER=mock`，而"mock 到底产不产边"必须能被断言） |
| `apps/server/scripts/verify-graph-and-profile.mjs` | 新增 4 条：mock **产出边** / **方向**正确 / `kind` 缺省为 `prerequisite` / 边端点与 `prerequisites[].conceptId` 一致 |
| `apps/server/scripts/verify-ui-flow.mjs` | 新增 1 条：`/api/graph` **读得回关系边**且 `> 0`。**为什么必须补**：紧随其后的 ⑦ 断言原文是 `graphAfter.edges.length === 0 \|\| …` —— mock 无边时它**恒真**（本项目吃过 `I24`"恒真断言"的亏），补上正面路径后它才恢复分辨力 |
| `apps/web/src/components/GraphPanel.tsx` | 见下「三、界面修正」 |

> **⚠️ 订正记录 1（必须留痕）**：卡 0-4 的落点 1 写"`SYSTEM_KNOWLEDGE` 的 JSON schema 加 **`edges`**"，
> 读起来是**顶层**字段。但代码里是 `buildGraph(parsed.graph, points)`（`packages/teaching/src/tasks.ts:192`）
> —— **读的是 `parsed.graph.edges`**。照卡片写顶层 `edges` 的话，**模型返回了也不会被采纳**，
> 而且不会有任何报错（`source.edges` 为 `undefined` → `rawEdges = []` → 静默无边）。
> 已按 `parsed.graph.edges` 实现，并写了断言兜住。
>
> **⚠️ 订正记录 2**：卡 0-4 的落点 2 写"一条 `kp-derivative → kp-monotonicity`（prerequisite）"。
> 契约里 `GraphEdge.from` = **依赖方**、`to` = **被依赖方**（`packages/contracts/src/knowledge.ts:124-127`），
> 与 `verify:graph` 里既有的 `{from:'monotonicity', to:'derivative'}` 写法一致。
> 按卡片字面写会把方向**写反** —— 不报错，但图谱分层与缺口判定都会错。
> 已按 **`kp-monotonicity → kp-derivative`**（单调性依赖导数）实现，并加了方向断言。

### `W0-7`（部分）：把"已登记断点"的正面路径补上

顺带修好了 `verify:flow` ③ 那处**因 mock 无边而恒真**的断言（见上表）。`I14`（`droppedBlocks` 正面路径）
与 `I35`（`toStudentReason` 不含 uuid）两项**未动** —— 后者需要先把 `routes.ts` 的私有函数提到独立模块，
属有意识的接口改动，留待下一批。

---

## 二、本轮产出的订正与新增登记

| 编号 | 内容 |
|---|---|
| （订正，已落在代码与本文） | **卡 0-4 的两处**：JSON 契约应为 `parsed.graph.edges`（不是顶层 `edges`）；边方向应为 `单调性 → 导数`（不是 `导数 → 单调性`） |
| （订正，已落在配置注释） | **卡/父文档订正 3 的落地**：`no-circular` **逐条转 `error`**，不搞一次性总开关 |
| **`I42` 新登记** | 图谱关系边的**端点可以不是节点**（前置缺口概念尚未抽为节点），`GraphPanel` 此前**静默丢边**。界面侧已修（见下）；**"前置概念应否补为图谱节点"属契约判断，未擅自改** |

> 另有两处**未改、仅记录**：`知识库.md` §13.2 尚未收录"`deploy-dist` 删不掉"这条环境结论；
> `知识库.md` §14 的决策编号重号（`I41`）。两者都不阻塞本轮。

---

## 三、界面修正：`GraphPanel` 不再静默丢边

**问题**：契约里边的 `to` 是"被依赖的**前置概念**"，而图谱节点只来自 `points[]`（材料抽出的知识点）。
演示案例正是这种形态 —— 边 `kp-monotonicity → kp-derivative`，而 `kp-derivative` 因材料未覆盖（`MISSING`）
**不是节点**。`GraphPanel` 用 `position.get(edge.to)` 取坐标，取不到就 `return null`：
**头部显示"N 关系"却一条线都没有**，看的人只会以为界面坏了。

**修正**：如实报出条数 —— "另有 N 条关系指向尚未抽取为节点的概念（通常是材料未覆盖的前置缺口），
因此这里画不出连线；这些概念在上方「前置依赖」区可见。"
并配套 **2 条断言**：① 该说明确实出现；② **反例** —— 端点都是节点时**不**出现（防止这条断言恒真）。
同时更新了该文件顶部已过时的注释（原写"在提示词补齐之前 `edges` 为空"）。

---

## 四、验证结果（本轮实测）

| 套件 | 结果 |
|---|---|
| `npm run verify:all -w @lc/server` | **57 项通过 / 0 失败**（graph 段；原 53） |
| `npm run verify:flow -w @lc/server` | **43 项通过 / 0 失败**（原 42） |
| `npm run verify:dist -w @lc/server` | **11 项通过 / 0 失败** |
| `npm run verify:render -w @lc/web` | **107 项通过 / 0 失败**（原 104） |
| `npm run verify:serve-web -w @lc/server` | **43 项通过 / 0 失败** |
| `npm run verify:repo` | **35 项通过 / 0 失败**（`sensitive` 17 / `licenses` 10 / `prompts` 8） |
| `npm run verify:predeploy` | **退出码 0**（`build:clean` → `build` → `dist` → `serve-web` → `repo`） |
| `npm run typecheck` | 4 个 workspace 全绿 |
| `npm run lint` | **0 error / 0 warning**（原 0 / 3） |
| `npm run lint:boundaries` | **0 error / 6 warning**（原 0 / 7，含 1 个环）—— 环已消，`no-circular` 已转 `error` |
| `npm run verify:file-size` | **5 个文件超 300 行**（阶段 4 的目标，本轮不涉及；`GraphPanel.tsx` 因本轮修改 +15 行左右，仍在这 5 个之内） |

**回归合计：`all` 194 ＋ `flow` 43 ＋ `dist` 11 ＋ `render` 107 ＋ `serve-web` 43 ＋ `repo` 35 ＝ 433**（原 425）。
已同步四处：`docs/todo.md` 的 `P-C5` 行、`docs/知识库.md` §9、`README.md` 回归段、本文件。

`verify:flow` 是**打真实 HTTP** 的，跑之前按要求确认 3000 端口 `LISTENING` 计数为 0，
用 `MODEL_PROVIDER=mock PORT=3000` 起服务，跑完 `taskkill /PID … /F /T` 并复确认端口已释放。

---

## 五、文件清单

**新增**：`apps/server/src/model/adapter.ts`、本文件。

**修改**：
`README.md`、`docs/todo.md`、`docs/知识库.md`、`docs/tech/2026-09-17-B-提示词与工作流配置.md`、
`.dependency-cruiser.cjs`、`scripts/check-file-size.mjs`、`scripts/check-licenses.mjs`、
`packages/teaching/src/prompt.ts`、`apps/server/src/model/index.ts`、`apps/server/src/model/deepseek.ts`、
`apps/server/scripts/verify-graph-and-profile.mjs`、`apps/server/scripts/verify-ui-flow.mjs`、
`apps/web/src/components/GraphPanel.tsx`、`apps/web/scripts/verify-render.tsx`。

> **未提交**：本轮**没有**执行 `git commit`／`git push`（远端写操作需用户明确要求）。
> 因此"工作区 = 远端 `main`"这句在本轮之后**不再成立** —— 差的正是上面这批文件。

---

## 六、本轮之外（下一批的直接候选）

1. **`H1` 上传重部署**（人工）——但**注意**：打包前应先让本地树与 `main` 对齐，
   否则 `pack:deploy`（按文件系统遍历、不看索引）会把**未提交的工作区版本**打进上传包
   （这是卡片订正 4 指出的问题，仍未解决）。
2. **`W0-3`（`P-B13` LaTeX→表达式白名单）与 `B3` 符号验证** —— **B 的工件，本轮未代做**。
   ⚠️ 若要做，注意卡片订正 6/7：装 `mathjs` 与重生成锁文件是**同一次 `npm install` 的后果**，必须合并成一次；
   且 `mathjs` 是**同步计算**，`Promise.race` 形式的"超时保护"**保护不了任何东西**（应改用输入规模上限）。
3. `W0-6`（`I39` 锁文件根治，**本机高危**）、`W0-9`（`/api/teacher`）、`W0-10`（模型通道扩展）、
   `W0-11`（边界用例留痕）、`W0-15`（阶段 1 —— ⚠️ 见卡片订正 2：**"7 → 0"不能单独当阶段 1 的验收**）。
