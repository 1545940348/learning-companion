# 微积分学伴 · 待办总表

> **本文件是项目待做事项的唯一汇总处。** 每轮开始前从这里认领，结束后勾掉并写进 `changelogs/`。
> 相关的长期约定见 [`知识库.md`](./知识库.md)；能力真实状态见
> [`specs/微积分学伴_初赛补充说明.md`](./specs/微积分学伴_初赛补充说明.md) §3.4。
> 最近更新：**2026-09-21（视觉接入落地 + 线上重部署 + 真实通道识图实测）**：`main` 已到 **`eb06ab6`**
> （= **PR #14 合并提交**，含图片识别）；**线上已重部署到它**（第 005 次发布，20:34）→ **`I37` 关闭**；
> 并补上**真实模型通道的识图实测**（`POST /api/parse` → `200` / 2.07 s，转写与公式定位均正确）——
> 此前只在 mock 下验过链路，故此前"线上能识图"**不成立，现已成立**。
> 同批修正 `README` / 《初赛补充说明》§3.4 / 《参赛说明》/ `知识库` / 《界面验收清单》（追加 §9）里
> "符号验证未接入、多模态未接入、线上是旧快照"等**对外失真**，并给视觉接入补上变更记录。
> **当前剩余工作见下方「📌 当前剩余总览」**；回归现值 **301（6 套件）＋ 107（渲染）**。
> —— 上一轮：2026-09-21（远端审查 + 状态刷新轮）：远端 `main` 前进到 **`72ae5ba`**（= PR #12 合并提交）；
> 本地 `reset --hard` 对齐；修正 `App.tsx` 页脚与《界面验收清单》里把已接入的「符号验证引擎」仍列为"尚未接入"的
> 对外失真；`W0-7①②` 落地（`verify:guards` 81 → 88）；回归 506 → **513**。
> —— 再上一轮：2026-09-20（状态核对与剩余项重排轮）：本地对齐 `3da6f4d`；`npm run verify:predeploy` 全绿；
> 逐条改正 `I9`（部分未闭）/`I11`/`I17`/`P-C13`/`J3`/`J5` 的过时状态，并新登记 `I40`。
> —— 再再上一轮：2026-09-19（`I17`/`I20②③④⑤`/`I21`/`I29`–`I36`；`P-C14` 在线链接交付；新登记 `I37`；回归 387 → **403**。
> 记录见 `changelogs/2026-09-19-I类缺陷推进.md`、`changelogs/2026-09-19-C-在线链接验收.md`、`changelogs/2026-09-21-视觉接入与在线重部署.md`）

## 使用规则

| 项 | 约定 |
|---|---|
| 状态标记 | ⬜ 未开始 ｜ 🟡 进行中 ｜ ✅ 已完成 ｜ ⛔ 阻断（不解决则下游全部阻塞） |
| 编号 | `B`=阻断项 `I`=缺陷 `P-A/P-B/P-C`=分角色计划 `J`=共同事项 `D`=待确认 |
| 认领 | 动手前把「负责人」填上真实姓名并把状态改为 🟡 |
| 完成 | 勾掉状态，**并在当轮 `changelogs/` 里写明改了什么、验收结论**；不要只在这里打勾 |
| 新增 | 发现新问题当场加进来，不要只写在对话里 |
| 并行 | **本文件由 C 汇总落笔**，其他人用对话或 PR 评论提出，避免并行编辑冲突（见下节） |
| 排期 | 要并行开工，直接看下面的「🔀 并行执行视图」：先确认文件归属，再挑「可立即开始」的泳道 |

---

## 📌 当前剩余总览（2026-09-20 重排）

> **用途**：未完成项原本散在「阻断项 / 缺陷 / 计划项 / 拆分路线图 / 待确认」五处，没有一处能一眼看全。
> 本节把它们按**"谁现在能动手"**重排一次；**明细与论证仍在各自章节**，这里只做入口与排序。
> 排序依据是 2026-09-20 的**实况核对**（不是文档自述），核对方式与结论见每条的「实况」标注。
>
> 📄 **要照着干活、看"按什么顺序做、被谁卡着、做完解锁谁" → 读 `plans/2026-09-20-后续全量推进（按阻断关系）.md`**
> （该文件**不按角色分任务**，按阻断关系分波次：第 0 层零上游可立即开 → 逐层解锁；含关键路径判定与人工闸门清单）。
> 本节回答"还剩什么"，那份回答"按什么顺序做"。

### 🔴 一、只有人工能做（机器做不到，且卡着交付）

| # | 事项 | 卡在哪 | 依据 |
|---|---|---|---|
| ~~1~~ | ~~**`I37` 重部署**：把线上快照刷到 `main`~~ ✅ **已完成（2026-09-21 20:34）** | 用 `PACK_DEPLOY_OUT` 在**仓库外**重打（`D:\LearnBuddy-deploy-0921c`，**180 文件 / 2.30 MB / 自检 ✅**），经 CloudBase 通道发布为**第 005 次**（`FlowRatio = 100`）；部署后逐项核对**环境变量与实例数未被改动**；随后实测线上真实通道识图（`200` / 2.07 s）。⚠️ 历史包全部作废（`0920` 打在 `B3` 之前、`0921`/`0921b` 缺视觉那批） | `I37`、`plans/2026-09-20-I37重部署前置检查清单.md`、`changelogs/2026-09-21-视觉接入与在线重部署.md` |
| 2 | **浏览器视觉验收**（`content-disposition` 的**平台行为已定案**，剩"点开是打开还是下载"仍需人工点一次） | 环境无 `agent-browser`、不许 `npm install -g` | `I8`、`specs/微积分学伴_界面验收清单.md` §9 |
| 3 | **3 分钟录屏**（`P-A10`） | 第 1 条**已完成**（线上已是新版），只剩第 2 条；分镜已按现状追加 **V2 修订节** | `P-A10`、`P-A14` |
| 4 | `P-C18` LearnBuddy 对话记录归档 | 需人工从 LearnBuddy 侧导出 | `P-C18` / `L9` |
| 5 | `J10` 提交回执核对、`J12` 四件套逐项签字 | 人工 | `J10` / `J12` |

### 🔓 二、原被 `B3` 卡住的一整条链 —— **09-20 已解锁**（`B3` 落地）

**实况（09-20 `B3` 落地轮）**：`packages/teaching/src/symbolic.ts` **已实现**（852 行）、`mathjs@15.2.0`
**已安装并在 `packages/teaching/package.json` 声明**；`/api/gap` 端到端实测可到
`VERIFIED` + `symbolic`（`verify:flow` ⑥/⑦/⑧），`/api/health` 的 `verification` 由引擎自身声明推导。

**随之收口的**（逐条见「缺陷与不一致」节与 `changelogs/2026-09-20-B3符号验证落地.md`）：

| 原被卡项 | 现状 |
|---|---|
| `I5` `health.verification.available` | ✅ 已改：**由 `SYMBOLIC_ENGINE.available` 推导**，不再手写常量 |
| `I15` `/api/gap` 的 `conceptName` | 🟡 **部分收口**：已改为"从图谱反查概念名、查不到退回 `conceptId`"；⚠️ **现管线里通常查不到**（前置概念不是图谱节点，见 `I42`），完整修复取决于 `I42` 的契约判断或一次契约变更 |
| `I16` 幂等重放的 `status` | ✅ 已改：从图谱入边读实际状态，不再硬编码 `'SUPPLEMENTED'`；读不到才退回"按已有补充块的验证状态推导" |
| `I18` 两个"声明了没接线"的常量 | ✅ 已接线：`VERIFYING_STATUSES` 用于 `SUPPLEMENTED → VERIFIED` / `failed → DISPUTED` 的推导；`SUPPLEMENT_LENGTH` 既写进提示词（有断言锁一致性）也在校验层拦超长；`commitSupplement` 加**一致性断言**（未验证却传 `VERIFIED` → 抛错） |
| `P-B13` LaTeX 白名单 | ✅ 已实现（`packages/teaching/src/latex.ts`，292 行），**该通的通、该拦的拦**（含正反例断言） |
| `P-B1`/`P-B2`/`P-B16` 验证回环与固定案例基线 | 🟡 固定案例基线 ✅（`FIXED_CASES` 10 例，实现与回归共用一份）；**失败回环（`P-B2` 的修正回环）未做**，当前 `failed` 只落 `DISPUTED` 并如实展示 |
| `J5` 核心闭环第 6 步 | 🟡 **mock 通道已走通**（`MISSING → SUPPLEMENTED → VERIFIED`）；真实通道未实测 |

**仍然未做的**：`P-B2` 的**失败修正回环**、`P-B12` 别名合并补测、`P-B7`/`P-B14` 教学与边界测试记录、
`P-B8`/`P-B9` 多智能体与错题归因、`P-B17` 变式题。
**⇒ 当前唯一的技术阻断项已消失**（`B3` 闭合后，`todo.md` 不再有"技术阻断"这一档）。

### 🟡 三、可以马上做（零外部依赖）

| 事项 | 负责人 | 实况 / 备注 |
|---|---|---|
| `I39` 锁文件缺全平台可选依赖 | C | **09-20 复核：当前不阻断**（`@rollup/rollup-win32-x64-msvc`、`@esbuild/win32-x64` 都在，`verify:predeploy` 全绿）；根治 = 重生成含全平台可选依赖的锁文件 |
| `I3`/`P-C10` 材料替换语义（用例 E12 完整化） | C | 契约变更 → **改完 A 必须同步** |
| `I2`/`P-C9` `ProfileEventInput` 去掉请求侧冗余 `sessionId` | C | 同上；A 侧已把冗余收敛在 `withSessionId()` 一处 |
| `P-C11`/`L20` `/api/teacher` 班级聚合实现 | C | 界面侧（`P-A6`）等它；样本不足要显式提示 |
| `P-B7`/`L34` 教学与边界测试记录、`P-B12`/`L36` 别名合并补测、`P-B14`/`L29` 边界用例留痕、`P-B15`/`L4` 教学素材 9 份、`P-B17`/`L28` 变式题、`P-B8`/`L26` 多智能体降级、`P-B9`/`L27` 错题归因 | B | 全在 `packages/teaching`，B 线独立，互不等待 |
| `J8`/`L30` 边界用例 E8/E10/E11/E13/E16/E18 留痕 | C | 多为会话与错误相关，多数可立即做 |
| `J11`、`D2`/`L17` | 共同 | `J11` 建议仍向组委会问一次（`D1` 已按"仓库已 public"关闭）；`D2` 是 `docs/designs/` 母本来源说明 |
| `P-C4`/`P-C19`/`L21` 模型通道扩展（视觉 / 嵌入 / 语音） | C | `D3` 已按建议拍板（视觉复用已接入通道、嵌入不引、语音优先浏览器内置）→ **可以开工**；下游 `P-B5`/`I6`（`L22`）与 `P-A7`（`L23`）等它 |

### 🔵 四、尚未开工的结构性工作（拆分路线图）

**阶段 1 起全部未开始**（阶段 0 已于 2026-09-19 完成）。认领前对照**当前基线（09-20 实测）**：

- `npm run verify:file-size`：**7 个文件超 300 行** —— `useWorkbench.ts` **994**、`routes.ts` **868**、
  `symbolic.ts` **852**（`B3` 新增）、`tasks.ts` 448、`store/index.ts` 401、`model/index.ts` 361、
  `GraphPanel.tsx` 333（**阶段 4 的目标是 0 个**，且届时转阻断）。⚠️ **09-20 由 5 → 7**，已登记 `I43`；
- `npm run lint:boundaries`：**6 条 warn、0 error**（09-20 起 `no-circular` 已清零并**转 `error`**；
  余下 6 条全是 `panels-should-not-import-state-internals`，即阶段 1 门面要消的耦合）；
- `npm run lint`：**0 error / 0 warning**（`I40` 已于 09-20 清零）；
- ⚠️ **冻结线**：A 线 **09-25→09-26** 只修缺陷、不动结构；B 线 **10-10** 起停止一切结构改动 → 阶段 1–6 的实际窗口是**现在到 09-25**（今天 09-20，**只剩 5 天**）。
- 依赖顺序：`L`（契约阶段，**必须先于登录**）→ `V`（登录）→ `W`（教师视图）；阶段 7（路由化）排在 `V` 之前。

### ⚪ 五、已经闭合，不要再当待办（09-20 核对）

`I11`（`docs/README.md` §七 章节号已归位为 **§7 / §10 / §13**，实况核对无误）｜
`I17`（**09-19 已拍板并落地**：`describeVersion()` 运行时读 `package.json`、契约 `version: string \| null`）｜
`J3`（提交物四件套 09-19 已齐）｜`B1`/`B2`/`I4`/`J6`/`J7`/`P-C20` 等早前已闭合项｜
**本地 ref 与 index 落后**（09-20 已对齐 `3da6f4d`；**09-21 起随 PR #12 合并再次对齐到 `72ae5ba`** —— `HEAD` = `main` = `origin/main`、工作区干净、`git status` 仅余一份本地 review 文档）。

**09-20 `B3` 符号验证落地轮新闭合**（详见 `changelogs/2026-09-20-B3符号验证落地.md`）：

- **`B3`**（唯一技术阻断项）：引擎 `packages/teaching/src/symbolic.ts` + LaTeX 白名单 `latex.ts` 落地，
  `mathjs@15.2.0` 装入并声明；四类断言（导数/切线/单调/极值）逐条核验；`FIXED_CASES` 10 例（含负例与越界例）
  由 `verify:symbolic` 自动核对；接线到 `/api/gap` 与 `/api/health`。
  端点实测：mock 通道 `MISSING → SUPPLEMENTED → VERIFIED` 走通（`verify:flow` ⑥/⑦/⑧）。
  ⚠️ **真实模型是否按新 schema 返回 `claims` 未实测**（回归全走 mock）；**不是通用 prover**；
  **失败修正回环（`P-B2`）未做**。
- **`I5`**：`health.verification.available` 改为由引擎自身声明推导（不再手写 `false`）。
- **`I16`**：`/api/gap` 幂等重放的 `status` 从图谱入边读，不再硬编码 `'SUPPLEMENTED'`。
- **`I18`**：`VERIFYING_STATUSES` / `SUPPLEMENT_LENGTH` 接线；`commitSupplement` 加一致性断言。
- **`P-B13`**：LaTeX → 表达式白名单（`I1` 轮已开工，本轮完成）。
- **`I15`**：🟡 **部分收口** —— 已改为"反查图谱概念名、查不到退回 `conceptId`"，
  但**现管线里通常查不到**（前置概念不是图谱节点，详见 `I42`）。完整修复需 `I42` 的决定或一次契约变更。

**09-20 同期（前一轮）闭合**：`I9`｜`I40`｜`I38`｜`I1`（代码侧，真实通道未实测）。

- **`I9`**（对外失真，优先级最高）：`README.md` 的「尚未部署」行**已删**，改为「已实现」里的在线链接条目，
  `verify:serve-web` 口径校正为 **43 项**，并在「已知限制」补上三条真实限制
  （访问提示中间页 / 线上快照落后于 `main` / `content-disposition` 待人工判定）。反向搜索 `尚未部署\|28 项` 无残留。
- **`I38`**（循环依赖）：`ModelAdapter` 契约下沉到**叶子模块** `apps/server/src/model/adapter.ts`，
  `deepseek.ts ↔ model/index.ts` 的环消失；`no-circular` 规则**在清零后转 `error`**（先验红、再修绿）。
  实测 `lint:boundaries` 由 **7 warn（含 1 环）→ 6 warn（0 error）**，剩余 6 条全是 `panels-*`。
- **`I40`**（lint 残留）：3 处未使用绑定已删，`npm run lint` **0 error / 0 warning**。
- **`I1`**（图谱关系边）**代码侧闭合、真实通道未实测**：`SYSTEM_KNOWLEDGE` 的 JSON 契约补上
  `graph.edges`（方向 `from` = 依赖方 → `to` = 被依赖方）＋"没有把握的边不要写、写不出就返回空数组"；
  快照文档 `tech/2026-09-17-B-提示词与工作流配置.md` 同批逐字同步（`verify:prompts` 8 项通过，常量 706 → 1012 字符）；
  **mock 通道已产出边并落盘、可从 `/api/graph` 读回**（新增断言，此前该处因无边而恒真）。
  ⚠️ **仍未做**：真实模型是否按新 schema 返回 `edges` —— 本轮回归全走 mock，**真实通道未实测**；
  另登记 **`I42`**（边的端点可以不是节点，界面已如实说明，但"应否把前置概念补为节点"属 B/C 的契约判断）。

---

## 🎯 第 7 轮焦点与分配（已完成）

> **第 7 轮已结束**：A 侧四项（`P-A8`/`A9`/`A12`/`A14`）与文档全量整理（`I9`/`I10`/`I11`）完成，
> `B2` 也已解除。本轮之后又跑了**第 8 轮**（P 类待办全量清理，见「✅ 最近完成」与
> `changelogs/2026-09-17-第8轮-P类待办清理.md`）。下表保留当轮的分配，不改写。

> **只列本轮要做的，条目一律引用下方总表编号，不在这里重复描述** ——
> 同一件事写两处必然漂移。
> ⚠️ **下表的「完成标志」列是第 7 轮末的记录，之后各条的状态已变**（例如 `P-A16` 已于 09-18 定为
> "同源直连、前端无需改动"）。**本表只作当轮分配的历史留痕，状态一律以总表为准。**
> 排期依据：核心闭环的第 6 步是「符号验证」，而四件套的第一件是「在线链接」。
> 当前卡住核心闭环的是 `B3`；在线链接一侧的构建阻断 `B1` **已解除**，剩下的是
> 仓库公开（`B2`）与托管落地（`P-C14`）。因此本轮优先打掉 `B3` 与 `B2`/`P-C14`。

| 成员 | 本轮焦点（编号） | 一句话目标 | 完成标志 |
|---|---|---|---|
| **A** | ~~`P-A2`~~ ✅ ~~`P-A11`~~ ✅ ~~`P-A13`~~ ✅ ~~`P-A15`~~ ✅ → 本轮 ~~`P-A8`~~ ✅ ~~`P-A9`~~ ✅ ~~`P-A12`~~ ✅ ~~`P-A14`~~ ✅ → 剩 `P-A1` `P-A4` `P-A10` `P-A16` | 正确性与真实性已收口；本轮**界面联动、验收清单、窄屏防御与演示分镜**全部完成 | ✅ 已达：卡片点击可定位图谱节点（16 项断言）；验收清单 V1.0；窄屏防御 + 长文本不溢出；3 分钟分镜 9 镜 180 秒。**下一步转 `P-A10` 录屏（等 `P-C14` 在线链接）** |
| **B** | **`B3` → `P-B1`** `I1` `P-B2` `I5` | **符号验证落地**（本轮最高优先），并让图谱产出关系边 | §7.1 三个固定案例（x² 在 1 处导数 = 2；切线 y = 2x − 1；x³ − 3x 单调区间）符号验证结果与人工核验一致；`/api/gap` 可从 `SUPPLEMENTED` 转 `VERIFIED`；图谱有边 |
| **C** | ~~`B1`~~ ✅ ~~`B2`~~ ✅（**本轮已转 public**） **`P-C14`** `P-C16`(剩姓名→已按决定关闭) `P-C7` `I2` | **交出在线链接**（本轮剩余最高优先） | 在线链接在无痕窗口可访问且评委无需配置密钥；部署流程固定执行 `verify:dist` |
| **共同** | `J1` `J2` `J3` `J4` `J5` | 依次合并 PR，并逐项核对四件套 | #3 已合并 → #5/#4 可合；四件套齐备；核心闭环可在一次演示中走通 |

### 本轮明确不排入

教师视图界面（`I7`/`P-A6`）、语音输入（`P-A7`/`P-B5` 的语音部分）、错题归因（`P-B9`）、
多智能体编排（`P-B8`）—— 均为 P1。**不是砍掉，是排期顺序**：
它们要等本轮的两个阻断项解除、核心闭环先跑通之后再评估。

### 本轮结束时的判定

- ✅ `B1` 已在本轮开始前解除（PR #6）：构建产物可运行，`verify:dist` 与产物态界面流程均通过；
- 若 `B3` 仍未解除 → 核心闭环第 6 步不成立，**必须如实上报而非绕开**；
- ✅ `B2` 已于 2026-09-17 18:00 解除（仓库转 public，匿名态可读、可 `ls-remote`）。
  **下一个瓶颈变成 `P-C14` 在线链接** —— 四件套里唯一还没落地的一件。

---

## 🔀 并行执行视图

> 目的：三个人（或三个 Agent 会话）能**同时开工而不互相等待、不互相覆盖**。
> 用法：先看下面的「文件所有权」确认自己要改的文件归属，再按「可立即开始」挑活。

### 并行原则

1. **依赖只看两件事**：要不要改 `packages/contracts`（契约）、要不要碰别人的目录。
2. **契约变更一律串行**：`packages/contracts` 同一时间只允许一个变更加在飞 ——
   它被 A/B/C 三方消费，两个契约变更并行必然产生"谁先谁后"的语义问题。
3. **文档不要并行写同一个文件**：`todo.md` / `知识库.md` 由 C 汇总落笔，
   其他人用 PR 评论或对话提出，避免并行编辑冲突。
4. **未实现的能力继续如实标注**：并行推进时最容易出现"为了演示好看先写上"，这是红线。

### 文件所有权（防冲突）

| 路径 | 唯一所有者 | 并行注意 |
|---|---|---|
| `packages/contracts/**` | **C** | **必须串行**。改完要 A、B 确认后同步 |
| `apps/server/**` | **C** | 含路由、store、构建配置、脚本 |
| `packages/teaching/**` | **B** | 提示词、图谱、校验、题目都在此 |
| `apps/web/**` | **A** | — |
| `docs/changelogs/**`、`docs/reviews/**` | 各人写各自文件 | 文件名带 `-A-`/`-B-`/`-C-` 前缀，不会撞 |
| `docs/todo.md`、`docs/知识库.md`、`docs/README.md` | **C 汇总改** | **多人同时改必然冲突** → 他人提，C 落笔 |
| `docs/specs/**` | 任何人 | **不改**：正本只追加新版本，不改写旧版本 |
| `.learnbuddy/**` | 各自 | 已被 git 忽略，可随意放临时产物 |

### 关键路径（这几步只能串行）

```
① 合并 #3  →  ② 重跑 verify:dist / verify:flow  →  ③ 合并 #6 / #5 / #4
契约变更（改 packages/contracts）→ A/B 同步  →  才允许下一个契约变更
符号验证（B）→ 验证回环（B）→ 多智能体验证 Agent（B）
模型通道扩展：视觉/语音（C）→ 多模态解析（B）→ 语音输入界面（A）
在线链接（C）→ 公网可访问的前端（A）→ 演示录制（A）
```

### ✅ 可立即开始（零依赖，互不等待）

| 泳道 | 任务 | 负责人 | 产出 |
|---|---|---|---|
| L1 | **符号验证落地**：装 `mathjs` + `symbolic.ts` + 三类断言 + 固定案例断言 | B | `P-B1`/`B3` —— **当前最高优先，且完全独立** |
| L2 | **图谱关系边**：改提示词让模型输出 `graph.edges` | B | `I1` —— 与 L1 可同时做（不同文件区域） |
| L3 | ✅ **已完成（第 8 轮）** —— 提示词与工作流配置整理入库 | B | `P-B11` —— 满足源码仓库要求 |
| L4 | **教学素材 9 份**（3 主题 × 图文语音，单调性保留导数缺口） | B | `P-B15` |
| L5 | ✅ **已完成（第 7 轮）** —— 仓库转 public（`B2`）；姓名按决定不填（`I4`） | C | `B2`/`I4` |
| L6 | 🟡 **只剩"人工上传"这一步（2026-09-21 更新）** —— 同源托管（`serve-web.ts`：同一地址既给页面也给 `/api`）＋ `Dockerfile` ＋ `pack:deploy` ＋ 部署说明均已完成（PR #9），且**已于 09-19 真机部署并验收**（`P-C14` ✅，四件套齐备）。**现剩把线上快照刷到 `main`**：✅ **上传包已重打（2026-09-21）** —— `D:\smallbox\Projects\LearnBuddy-deploy-0921b`（**175 文件 / 2.25 MB / 自检 ✅**），**只剩控制台上传与重建**（`I37`）。⚠️ 旧包（`…-deploy-0920` 163 文件、`…-deploy-0921` 173 文件）**都不要用**。评委无需密钥这点已满足（密钥只填平台环境变量） | C | `P-C14` ✅ / `I37` 🟡 —— `B1` 已解除；载体已定（`D4` = 腾讯云 CloudBase 云托管） |
| L7 | ✅ **已完成（第 8 轮）** —— 提交前敏感信息检查（`.env` / `node_modules` / `dist/`） | C | `P-C17` |
| L8 | ✅ **已完成（第 8 轮）** —— 第三方依赖与许可证声明核对 | C | `P-C15` |
| L9 | **LearnBuddy 对话记录归档** | C | `P-C18` —— 持续积累，不阻塞但别拖到最后 |
| L10 | ✅ **已完成（PR #4）** —— mock / 真实双入口在界面上一眼分清 | A | `P-A2` |
| L11 | ✅ **已完成（第 7 轮）** —— 界面验收清单（入口 / 操作 / 期望结果） | A | `P-A9` |
| L12 | ✅ **已完成（PR #4）** —— 错误态与重试走查（读 `retryable`、`SESSION_STALE` 丢弃过期响应） | A | `P-A13` |
| L13 | ✅ **已完成（PR #4）** —— 未实现能力的标注复查（配合 J4 真实性核对） | A | `P-A11` |
| L14 | ✅ **脚本已完成（第 7 轮）**，录屏仍待做 —— 3 分钟视频分镜与旁白 | A | `P-A14`；**录屏属 `P-A10`** —— 在线链接 09-19 已交付，**阻塞已解除**；⚠️ 建议**先做完 `I37` 重部署再录**，否则录进去的是缺 `I22` 修复的旧快照 |
| L15 | ✅ **已完成（第 7 轮）** —— 窄屏 / 可用性自检 | A | `P-A12` |
| L16 | **向组委会确认「赛道二作品需开源」表述** | 共同 | `D1` —— **`D1` 已按"仓库已 public"关闭，但表述本身的不一致并未被官方澄清** → `J11` 仍建议问一次（早问早踏实） |
| L17 | ✅ **已完成（2026-09-21）** —— `docs/designs/` 方案母本来源与定位说明 | 共同 | `D2` —— 已注明"参考输入、非实现方案"与未采用技术清单；**具体出处待负责人补充** |

> **L33–L37 系 2026-09-17 复查时补挂**：原表（L1–L17）遗漏了这几条，其中多数零依赖，
> 按泳道挑活会整批漏掉。编号顺势后延，不改动 L1–L32 的含义。

| 泳道 | 任务 | 负责人 | 产出 |
|---|---|---|---|
| L33 | ✅ **已完成（第 7 轮）** —— 知识卡片与图谱联动高亮 | A | `P-A8` |
| L34 | **教学与边界测试记录**（12 个答疑问题、9 道固定题） | B | `P-B7` —— 与 `P-B14` 同批执行更省事 |
| L35 | ✅ **已完成（第 8 轮）** —— 参赛说明（教学案例、AI 能力与限制） | B | `P-B10` |
| L36 | **图谱归一补测：自依赖 / 循环依赖 / 别名合并** | B | `P-B12` —— 前两项 `verify:all` §6 已覆盖，**别名合并待补** |
| L37 | ✅ **已完成（第 8 轮）** —— `/api/health` 增加构建信息（产物形态自证） | C | `P-C12` |

### ⏳ 需要等依赖

| 泳道 | 任务 | 负责人 | 等谁 |
|---|---|---|---|
| L18 | 契约细化 `ProfileEventInput` | C | 无技术依赖，但**改完 A 必须同步** → 建议排在 L1/L2 之后 |
| L19 | 材料替换语义（用例 E12 完整化） | C | 同上 |
| L20 | `/api/teacher` 聚合实现 | C | 画像事件已有基础，可开始；界面侧等它 |
| L21 | **模型通道扩展**（视觉 / 嵌入 / 语音） | C | **选型已定（`D3` 09-19 拍板：视觉复用已接入通道、嵌入不引、语音优先浏览器内置）→ 可开工**；解锁 `L22`（多模态解析）与 `L23`（语音入口） |
| L22 | 多模态解析 | B | **等 L21** |
| L23 | 语音输入入口 | A | ✅ **已完成（2026-09-22）** —— 走 `D3` 的浏览器内置识别，**前端独立落地**。⚠️ 原写"等 `L21` + `L22`"，但按 `D3` 路线这两项**对语音没有服务端交付物**（`L21` 的语音＝浏览器内置、无通道需求；`L22` 服务端不转写），故字面阻断不成立，经负责人确认后开工 |
| L24 | 图谱视图完善（有边后的布局与高亮） | A | **等 L2** |
| L25 | 教师视图界面 | A | **等 L20** |
| L26 | 多智能体编排与降级 | B | **等 L1**（验证 Agent 是其中一环） |
| L27 | 错题归因 | B | 需诊断 Agent，可与 L26 同批 |
| L28 | 变式题 | B | 画像基础已有，可开始 |
| L29 | 边界用例 E1–E7 / E12 / E14 / E15 执行与记录 | B | 等 L1（验证相关用例） |
| L30 | 边界用例 E8 / E10 / E11 / E13 / E16 / E18 执行 | C | 多为会话与错误相关，多数可立即做 |
| L31 | 联调与发布 | C | 等上述大部分完成 |
| L32 | 演示彩排与计时 | 共同 | 等 L31 |

### 并行时的四条红线

1. **不为了提高"完成度"而把未实现的功能写成已实现** —— 并行推进时最容易越线；
2. **不在契约变更未同步前改消费方** —— 会造出两套并行的事实；
3. **不并行改 `todo.md` / `知识库.md`** —— 冲突难合，且会丢内容；
4. **合并顺序不变**：`#3 → #6 → #5 → #4`，stacked 分支不能乱序合。

---

## 🔴 阻断项（Blocker）

> 不解决则**提交物或核心闭环不成立**。优先级高于所有 Plan。

| 编号 | 事项 | 负责人 | 依据 / 现状 | 验收 |
|---|---|---|---|---|
| **B1** | ✅ **已解决（PR #6）** —— 构建产物无法启动：`npm run build` 成功但 `node dist/index.js` 崩溃，报 `Cannot find module '.../packages/contracts/src/session.js'`。根因：共享包 `exports` 指向 TS 源码、源码内部用 `./x.js` 互相引用，`tsx` 能映射 `.js→.ts` 而原生 node 不能，且 tsup 默认把 `dependencies` 当 external | C | `changelogs/2026-09-17-C-构建产物可运行-B1修复.md` | ✅ 已验收：`verify:dist` 8 项 + **用构建产物跑界面流程全通过**（当时脚本是 **30** 项；A 补识别步骤后已增至 **32** 项，合并态在 J2 下复跑通过）。⚠️ 本条原先写的「修法① tsup 加 `--no-external`」**是错的**（CLI 不支持该参数），实际修法是把两个私有包移到 `devDependencies` 让 esbuild 内联 |
| **B2** | ✅ **已解决（2026-09-17 18:00）** —— 仓库已转为 **public**。核验方式：**匿名态**（不带任何凭据，模拟评委）`GET /repos/...` 返回 `visibility=public`、`/commits/main`、`/contents/README.md`、`/license` 均 HTTP 200；`git ls-remote` 匿名可达 | C | 转可见性前做过**全历史敏感扫描**（38 个提交）：`.env` 从未进入历史、无密钥模式命中、无手机号/身份证号 | ✅ 已验收。⚠️ 遗留：**提交者邮箱随历史公开**（其中一条为队友的 QQ 邮箱）—— 经负责人 2026-09-17 决定**"照转、不重写历史"**，不再作为待办。**该邮箱不在任何文档中明文记录**（文档里写明文等于从「git 元数据」升级为「正文可见」，反而放大暴露面） |
| **B3** | ✅ **已解决（2026-09-20）** —— 符号验证落地：`packages/teaching/src/symbolic.ts`（852 行）+ `packages/teaching/src/latex.ts`（白名单，292 行）实现，`mathjs@15.2.0` 装入并在 `packages/teaching/package.json` 声明 | B | `tech/2026-09-17-符号验证引擎选型-纯TS.md`、`changelogs/2026-09-20-B3符号验证落地.md` | ✅ 验收：§7.1 三案例（x² 在 1 处导数 = 2；切线 y = 2x − 1；x³ − 3x 单调区间）**与人工核验一致**，并进 `FIXED_CASES`（10 例，含负例与越界例，`verify:symbolic` 69 项）；`/api/gap` 端到端可从 `MISSING → SUPPLEMENTED → VERIFIED`（`verify:flow` ⑥/⑦/⑧）。⚠️ **真实通道未实测**；**`P-B2` 失败修正回环未做** |

---

## 🐞 缺陷与不一致（Issue）
| 编号 | 事项 | 负责人 | 说明 | 处置口径 |
|---|---|---|---|---|
| **I1** | ✅ **代码侧已闭合（2026-09-20）；真实通道未实测** —— 图谱关系边 | B/C | 原因：提示词未要求模型输出 `graph.edges`。**已做**：`SYSTEM_KNOWLEDGE` 的 JSON 契约补上 `"graph":{"edges":[…]}`（方向 `from` = 依赖方 → `to` = 被依赖方）＋"没有把握的边不要写、写不出就返回空数组，不要为了凑边而编造"；快照文档 `tech/2026-09-17-B-提示词与工作流配置.md` **同批逐字同步**（`verify:prompts` 8 项通过，常量 706 → 1012 字符）；**mock 通道也产出边**（`model/index.ts` 的 `mockKnowledge`，否则无密钥演示永远看不到边）并断言"mock 产出边 / 方向正确 / 端点与前置关系一致"；端到端断言"边能落盘并从 `/api/graph` 读回"（此前该处因无边而**恒真**）。⚠️ **仍未做：真实模型是否按新 schema 返回 `edges` —— 本轮回归全走 mock，真实通道未实测** | ✅ 验收：`verify:all` graph 53 → **57**（0 失败）、`verify:flow` 42 → **43**（0 失败）、`verify:render` 104 → **107**；`verify:predeploy` 退出码 0。**界面不凭空连线**的口径不变；`P-A4` 的完整人工验收仍待人工 |
| **I2** | 契约冗余：`ProfileEvent.sessionId` | C | 请求侧每个事件都要带 `sessionId`，但服务端 `guardProfileEvents` 会用请求级值**覆盖**它 | 建议细分出 `ProfileEventInput`（不含 `sessionId`）。A 侧已把冗余收敛在 `withSessionId()` 一处 |
| **I3** | 契约缺材料「替换」语义 | C | `POST /api/knowledge` 只支持追加 → 用例 E12「就地纠错」无法完整实现（旧文本仍参与解析，前端只能标记「已修正」） | 评估是否增加按 `materialId` 替换的字段 |
| **I4** | ✅ **已按决定关闭（2026-09-17）** | C | LICENSE 原为占位姓名。**负责人决定：不填真实姓名**，统一用「微积分学伴团队」+ A/B/C 角色代号表达分工 | ✅ 已处理：`LICENSE` 版权方为「微积分学伴团队」；`README.md` 分工表保持角色代号。**后续任何人不要"顺手补上"真实姓名** —— 这是决定，不是遗漏 |
| **I5** | ✅ **已解除（2026-09-20，随 `B3`）** —— `/api/health` 的 `verification.available` 原先固定 `false` | B | 原因是 `B3` 未完成；**代码注释已写明"实现落地前不得改为 true"**。现已改为**由 `SYMBOLIC_ENGINE.available` 推导**（`routes.ts`），不再是手写常量 —— 引擎被摘掉时 health 会跟着变，不会漂移 | ✅ `verify:flow` ⑰ 与 `verify:dist` 均实测 `available: true`、`engine: 'mathjs'` |
| **I6** | 多模态解析（P0） | B | ✅ **图片路径已完成**（2026-09-21，随 PR #14）：服务端识别为文本 + 公式 LaTeX + 定位偏移；**线上真实通道实测 `200` / 2.07 s**。✅ **语音已于浏览器侧接入**（2026-09-22，`P-A7`）—— 但**服务端仍不转写**（`D3`），故 `/api/parse` 的语音路径**依旧"未接入"**、未接入的通道如实列入 `unavailable[]`。**已去掉「【占位】」式假描述** | ~~接入真实图文语音识别~~ → 图片、语音**两侧均已交付**；剩余待办：真实浏览器的人工验证（见 `界面验收清单` §10 的"未覆盖"段） |
| **I7** | 教师视图未实现（P1） | C / A | 服务端返回 `501 NOT_IMPLEMENTED`；**界面不渲染该面板**（空面板比无入口更易被误读为已实现） | 阶段三交付：班级聚合、常见误区、覆盖热力图，只读 + 脱敏（用例 E17） |
| **I8** | 前端无法做浏览器视觉验证 | — | 环境无 `agent-browser` 且不允许 `npm install -g`。当前只到"编译通过 + 模块可加载" | **已知限制，不解决**；提交前由人工在各浏览器实点一遍 |
| **I9** | 🟡 **大部分已修，仍剩一处未闭（2026-09-20 核对）** —— README 面向评委的说明与 main 实况不一致（方向是"低报"） | A | §核心机制仍写**四态**（V2.0 已是六态）；§技术架构接口表只列 **6 个**（V2.0 为 10 个）且仍写 `GET /api/quiz`；§当前状态仍称「图谱算完即丢弃」「无 `POST /api/profile`」「回归 137 项」「V2.0 扩张尚未落地」—— main 上**均已实现**（图谱落盘 ＋ 10 接口 ＋ 190 项）；模型通道说明仍写「依据说明书 V1.4」 | 按代码实况重写 §核心机制 / §技术架构 / §当前状态。**低报同样是失真**，且评委照 4.1 打开源码链接先看 README。✅ **2026-09-20 实况核对**：09-17「文档全量整理」轮已改好其中四处 —— §核心机制**已是六态**、§技术架构**已是 10 接口且为 `POST /api/quiz`**、§当前状态**已写图谱落盘**、回归**已标 425 项**；**唯一残留是行 207**（仍写「**尚未部署**（四件套中唯一未完成的一件）」＋「`verify:serve-web` **28 项**」，实况为 `P-C14` 已于 09-19 交付、专项现为 **43 项**）。⚠️ 这一处是对外材料，**优先级高于本表其余所有项**。✅ **2026-09-20 已闭合**：行 207 的「尚未部署」行已删，改为「已实现」里的在线链接条目并写明三条真实限制；`verify:serve-web` 口径校正为 **43 项**；`grep -n "尚未部署\|28 项" README.md` 无残留，`verify:repo` 35 项 0 失败 |
| **I10** | ✅ **已修复（2026-09-18）** —— `知识库.md` 自身存在过时引用 | C | 原状：§10.2「已合并 / 待合并记录」仍把 #3–#6 记为**待合并**；§9 写 `verify:flow` **30 项**且整表**缺 `verify:render` 42 项**；§2.2 交付状态仍称在线链接「有构建阻断项（`B1`）」 | 三处按当下实况更正；PR 记录**保留历史结论**、只更新状态列。**闭合情况**：§10.2 与 §9 已在第 7 轮改好（§9 现含 `render`/`serve-web` 与合计 376）；§2.2 由 2026-09-18 复核轮补上（在线链接一行改为"载体已定 + 同源托管本地已完成 + 中间页限制"），并把 §10.2 的合并记录补到 **#9** |
| **I11** | ✅ **已修复（2026-09-17「文档全量整理」轮；2026-09-20 复核确认）** —— `docs/README.md` §七「快速索引」章节号错位 | C | 指向 `知识库.md` 的「§6 常量」「§8 协作纪律」「§9 环境限制」，实际为 **§7 / §10 / §13**。按索引去查会翻到无关章节 | 更正为 §7 / §10 / §13 —— 该节是"遇到问题先看哪里"的入口，错位等于入口失效。✅ **现状（09-20 逐条核对）**：§七 现指向 **§7 常量 / §10 协作纪律 / §13 开发环境限制**，与 `知识库.md` 实际章节号一致；本轮只补状态列，**不改写本条当时的问题描述** |
| **I12** | ✅ **已修复（第 8 轮）** —— **依赖声明与许可证三处实错** | C | ① 根 `npm run dev` 使用 `concurrently`，但它**从未被声明、也从未安装** → **别人 clone 下来 `npm run dev` 直接失败**，违反补充说明 §5.2「他人可照着跑起来」；② README 依赖表漏列 6 个实际依赖（`@types/*` 与 `@vitejs/plugin-react`）；③ 5 个 `package.json` 缺 `license` 字段 | ✅ 已修：补进根 `devDependencies`、README 表按实况重写、各包补 `license`。**新增 `npm run verify:licenses` 自动拦截这三类**，不再依赖人记得。✅ **锁文件已于 `6bafa16` 收尾**：`package-lock.json` 现可写，且与五个 `package.json` 逐条对齐（原「本环境不可写」的判断已失效，见 `知识库.md` §13.4） |

### I13–I20 · 2026-09-17 全量复查新增（详见 `reviews/2026-09-17-全量代码与文档复查.md`）

| 编号 | 事项 | 负责人 | 说明 | 处置口径 |
|---|---|---|---|---|
| **I13** | ✅ **已修复（2026-09-17 缺陷修复轮）** —— `/api/tutor` 在"有会话但零材料"时返回 403 `UNAUTHORIZED_CONTENT` | C | 触发路径**可达**：点「开始新学习」或「按我的材料出题」（`loadQuiz('material')` 会静默建会话）都会令前端 `sessionRef` 非空而材料为空，此时提问 → 服务端 `requireAuthorization: sessionId !== null` 为真，mock/模型给的 `ai-supplement` 块无引用 → 403 且 `retryable=false`，学生卡死。**同一条问题走 `sessionId:null` 却正常**（已实测两分支）。与 README「不上传材料也能提问」直接冲突 | ✅ **已修**：`routes.ts` 的判据改为「本次请求是否真的带了可引用来源」（`materials.length > 0`，`materials` 此处已是"学生材料 + 已授权补充"的切片列表，与 `allowedRefs` 同为空／非空）。修复后同一条请求 **403 → 200**、`basedOnMaterial=false`（实测）。**并在 `verify:flow` 补 2 项断言**（`⑬`）专门走这条路径 —— 原 334 项一条都没经过它。⚠️ 前端"无材料时是否该建会话"仍属 A 的取舍，见 `changelogs/2026-09-17-缺陷修复-I13至I20.md` §5 |
| **I14** | ✅ **已修复（2026-09-17 缺陷修复轮）** —— 来源校验**静默丢弃**被拒块 | C | `routes.ts` 只在 `valid.length === 0 && rejected.length > 0` 时报错；一旦有块通过，其余被拒块**无声消失**，学生看到的是残缺答案且无从知道 | ✅ **已修**：`TutorResponse` **加性**增加可选字段 `droppedBlocks?: {count, reasons[]}`（契约变更，见 `tech/2026-09-17-契约变更-TutorResponse-droppedBlocks.md`），服务端在"部分被丢弃"时填入，`TutorPanel` 显著展示"本次回答不完整"。**选"加字段"而非"降级为标注块"**：降级要么泄露被拒内容、要么造一个不属于任何来源类别的假回答块。⚠️ 该字段必须渲染才算修完 —— 渲染侧在 `apps/web`（A 的工件），本轮**代改并请 A 复核** |
| **I15** | 🟡 **部分收口（2026-09-20，随 `B3`）** —— `/api/gap` 把 conceptId 当作 conceptName 送进提示词 | C | `routes.ts` 的 `supplementGap({ conceptId, conceptName: conceptId })` → 真实模型看到的是"需要补齐的前置概念：kp-derivative（kp-derivative）"，而中文名（如「导数」）在客户端 `relation.conceptName` 里、从未上传。mock 掩盖了这一点 | **已做**：改为 `findConceptName(session, conceptId)` **从图谱节点反查名字，查不到退回 `conceptId`**（不编造名字）—— 这条路**不动契约**，因此不占契约串行闸。⚠️ **但它现在通常查不到**：图谱节点只由 `points[]` 构成，而这里要补的是 `prerequisites[]` 里的前置概念（同一根因见 `I42`；`I34` 的"第 2 条判定来源"同样因此是死分支）。**完整修复二选一**：① 让前置概念进 `SessionGraph.nodes`（改 `buildGraph`，牵动节点数与多组断言）；② 契约给 `GapRequest` 加 `conceptName`（占串行闸）。**需 B/C 拍板** |
| **I16** | ✅ **已修（2026-09-20，随 `B3`）** —— `/api/gap` 幂等重放的 `status` 曾是硬编码 `'SUPPLEMENTED'` | C | 重复补充同一缺口时直接返回字面量，不读图谱实际状态。原先恰好无害（恒为 SUPPLEMENTED），但概念一旦转 `VERIFIED`，重放仍报「已补充」就与图谱自相矛盾（低报） | ✅ 改为 `readConceptStatus(session, conceptId)` **从 `session.graph` 入边读实际状态**（多条入边矛盾或无入边时返回 `null`，退回"按已有补充块的验证状态推导"）。`verify:flow` ⑧ 两条断言锁住（重放状态仍为 `VERIFIED`、验证状态仍为 `symbolic`） |
| **I17** | ✅ **已修复（2026-09-19；2026-09-20 复核确认）** —— `/api/health` 的 `version` 为硬编码 `'0.2.0'`，与五个 `package.json`（均 `0.1.0`）全不一致 | C | 全仓无任何出处；无对应 tag、无 changelog 记载。评委若对照源码仓库，会先看到这个数字对不上 | **当时（09-17）⏸ 待决策、有意不拍**：口径未定 —— 跟 `package.json`（`0.1.0`，但它描述的是包版本）还是跟"契约 V2.0"（描述的是契约版本）？两者语义不同，随手对齐成 `0.1.0` 会把两个概念混成一个。✅ **09-19 拍板并落地**：**运行时读 `package.json`，取不到就如实报 `null`**（不硬编码、也不假装知道）；契约 `HealthResponse.version` 由 `string` 放宽为 **`string \| null`** —— 变更记录见 `tech/2026-09-19-契约变更-HealthResponse-version.md`。**09-20 实况复核**：`routes.ts:253` 用 `describeVersion()`，`build-info.ts` 从 `package.json` 解析 `version`，契约侧为 `version: string \| null` ✔（本条原写"本轮未改"是 09-17 的事实，保留不改写，只补后续状态） |
| **I18** | ✅ **已接线（2026-09-20，随 `B3`）** —— 契约里**声明了但从未被引用**的常量 | C/B | `VERIFYING_STATUSES`（`SUPPLEMENTED→VERIFIED` 的硬规则**因此无强制点**）、`SUPPLEMENT_LENGTH`（200—400 字只写在提示词里，返回多长都收）、`EMPTY_GRAPH`、`TEACHER_MIN_SAMPLE`。前两个不是"暂时没用"，是**规则声明了却没接线** | ✅ 全部接线：① `VERIFYING_STATUSES` 成为"通过"的**唯一依据**（`deriveSupplementStatus`）；② `SUPPLEMENT_LENGTH` 既写进提示词（`verify:symbolic` 断言它与常量一致）也在校验层拦超长（**照常返回内容，但不据此标 `symbolic`**）；③ `commitSupplement` 加**一致性断言**：传 `status='VERIFIED'` 而验证不达标 → **抛错拒绝**（并配正向反例，证明断言不是恒抛）。⚠️ **未对 `DISPUTED` 加同类断言**：§3.2 允许"来源冲突"触发 DISPUTED 而未必 `failed`，断言写成"只有 failed 才能 DISPUTED"会挡住合法路径 |
| **I19** | ✅ **已修复（2026-09-17 缺陷修复轮）** —— 死代码：`applyMaterials`、`listSessions`、`ModelError.toFailure`、`EMPTY_GRAPH` | C | `applyMaterials` 是**无 CAS 的旧写入路径**，已被 `commitMaterials` 取代却仍导出 —— 留着最危险的是被误用 | ✅ **已删四处**：`store/index.ts` 的 `applyMaterials` 与 `listSessions`、`model/errors.ts` 的 `ModelError.toFailure`、`contracts/knowledge.ts` 的 `EMPTY_GRAPH`。删前 `grep -rn` 全仓复核（**排除 `apps/server/dist/`**，构建产物会污染结果），确认零调用点；删后 `typecheck` 与全量回归通过。⚠️ 其中 `EMPTY_GRAPH` 是 `packages/contracts` 的导出 → 属契约面变更，已请 B 复核 |
| **I20** | ✅ **已全部修复（09-17 三项 + 09-19 四项）** —— **前端 7 项确定缺陷**（`apps/web`） | A | ①`GraphPanel` 对**没有缺口记录的节点**一律显示「材料已覆盖」——图谱节点恒无 gap 记录，故这是默认路径，且与同面板详情「暂无引用」自相矛盾；②`QuizPanel` 上报的 `topic`/`source` 取**当前开关**而非已加载题目的实际来源，中途换开关即错报画像；③`correctMaterial` **无条件**宣布"已重建"，会覆盖失败提示、留下本地与服务端不一致；④`loadQuiz` 返回 null 时 `setItems(null)` **清空已答题目**（含"上一个操作未完成"这种非失败情形）；⑤`canRetry` 是全局标志，与当前 notice 无关 → 无关提示上会挂「重试」按钮；⑥零材料下点「按我的材料出题」会**静默建会话**并把写死题目标成「基于你的材料生成」；⑦`ProfilePanel` 称"提交一次练习后就会出现"，但服务端 `quiz-attempted` 不写 `mastery`（练习面板同时说"已记入画像"） | ✅ **三项真实性标注已修（①⑥⑦，最高优先）**：①节点状态改由「补充记录 → `/api/knowledge` 的覆盖判定」推导，两者都无则标 **「未判定」**（不是六态之一，因为它是"没有判定"而非一次判定），并给出未判定节点数；⑥`useWorkbench.loadQuiz` 与 `QuizPanel` 在零材料时**拒绝**按材料出题并说清替代路径，服务端同步加 400 兜底（`apps/server` 侧，见 `I13` 同轮的 `/api/quiz` 改动）；⑦`ProfilePanel`/`QuizPanel` 改为如实说明「只有补上这一段会写入掌握状态；练习提交会上报事件但不产生画像条目」。**加 24 项断言**（`verify:flow` 4 + `verify:render` 20）。✅ **②③④⑤ 已于 2026-09-19 全部修复**：② `QuizPanel` 新增 `loaded` 状态，上报用**已加载题目**的 `topic`/`source`（切换开关不等于换题）；③ `correctMaterial` 改为**先看 `submitMaterials` 的返回值再宣告**，失败即回滚本地修正并保留失败提示（原先成功文案会把失败提示顶掉，留下"本地已改、服务端未改"的两套事实）；④ 由 `I32` 一并修掉（`loadQuiz` 返回 `null` 时早退，不再 `setItems(null)`）；⑤ 判定提成纯函数 `shouldOfferRetry(notice, canRetry)` —— 必须**当前这条提示**带 `retryable` 才挂重试按钮，并补 5 条真值表断言。⚠️ ②③ 仍属交互路径，**需人工在浏览器复验**（`I8`） |
| **I21** | ✅ **已修复（2026-09-19）** —— 界面上会显示字面星号 —— `ProfilePanel.tsx:87` 的 `**不等于已验证**`、`QuizPanel.tsx:114` 的 `**不算作你材料的证据**` | A | JSX 文本节点**不处理 Markdown**，这两个 `**` 会原样渲染给学生。两处均为**既有**代码（非本轮引入），本轮修复时（`I20①⑦`）在同一批文件里发现 | ✅ 已改为「」强调；**同步核查了两条依赖这些字符串的既有断言**（`apps/web/scripts/verify-render.tsx` 的 `不算作你材料的证据` 与 `不等于已验证`）—— 两处断言用的是**不含 `**` 的子串**，因此改文案后仍然有效（这正是本条要求"改文案必须同步改断言"的原因：若断言写成含 `**` 的整串，改动会让断言**静默变成永假**）|

### I22–I35 · 2026-09-18 收尾修复轮（源自一次外部审查报告；逐条核实记录见 `reviews/2026-09-18-待核实发现-核实报告.md`）

> 该报告共 15 条 + 2 条"贯穿性问题"。**核实后 13 条成立、1 条不成立、1 条部分成立**；
> 下面只列处置，未成立/需订正的结论一律在 review 文档里留痕（含"AI 输出被否决"的记录）。

| 编号 | 事项 | 负责人 | 说明 | 处置口径 |
|---|---|---|---|---|
| **I22** | ✅ **已修复** —— 同源托管回退判定有**三个漏口**，取不到的资源会拿到 `200` + 首页 HTML | C | ① 判定用的是**未解码**的 `req.path` → `/foo%2Ejs` 看不见那个点；② 扩展名正则带 `{1,8}` 上限 → `/manifest.webmanifest` 判不出"是文件"；③ 结尾带斜杠的 `/assets/` 也没被排除。三者都是这条守卫**被加进来要防的那种失效**（浏览器按 JS 解析 HTML → `Unexpected token '<'`，而状态码是 200） | ✅ 判定改为**先解码再判**、扩展名**不限长度**、结尾带斜杠不算前端路由；解码失败（`%zz`）**倒向严格一侧**（按文件处理）；另外 `req.path` 与解码后路径**都**判一次 `/api`（`/api%2Funknown` 原值不以 `/api/` 开头）。**红/绿证据**：`.learnbuddy/swfix/probe.mjs` 在旧模块上 **7 项失败**（4 个漏口 + `/%zz` + `/api%2Funknown` + 产物消失 500），新模块 **15/15**；`verify:serve-web` 补 6 项断言 |
| **I23** | ✅ **已修复** —— 运行期 `index.html` 消失时返回 **500 `INTERNAL`** 而不是 404 | C | `res.sendFile` 的错误一律 `next(error)`，而 `errorHandler` 没有 `err.status` 分支 → 落成 500 + 一条 error 级 `http.unhandled`。本项目自己的 `npm run build:clean` 就把 `apps/web/dist` 挪走，**整个重建窗口内页面全是 500** | ✅ 模块内区分 `ENOENT`/`ENOTDIR`/`ENAMETOOLONG` → **404** + 一条 warn 级 `web.index.missing`；其余错误仍走统一处理。**旧模块实测 500、新模块 404**（probe 最后一条 + `verify:serve-web` 新增 2b 节 3 项） |
| **I24** | ✅ **已修复** —— `verify-serve-web` 第 3 节是**恒真式断言** | C | 原为 `typeof result.mounted === 'boolean' && result.webDistDir !== undefined`：前者必然为 boolean，后者类型是 `string \| null`，故恒成立。把 `candidateWebDistDirs()` 改成 `return []` 脚本仍然全绿 —— 而**那个函数决定了容器里能不能提供页面** | ✅ 拆成两条真断言：① 返回值必须**自洽**（挂载⇒目录里真有 `index.html`；未挂载⇒`null`）；② **独立重算候选表**再比对（刻意不复用模块里的函数，否则"实现被清空"时两边一起变空）。**实测**：临时把候选表清空 → 新断言报红（27/1），还原后 28/0 |
| **I25** | ✅ **已修复** —— `pack-deploy.mjs` 对 `PACK_DEPLOY_OUT` **没有任何身份校验** | C | ① 输出目录 = 仓库根或它的上级 → `prepareOutDir()` 先 `rm(dir, {recursive:true, force:true})`，等于**删库**；② 输出目录在仓库内部且名字不在 `EXCLUDE_DIRS` → `copyTree` 先建目标再 `readdir` 源，每层都能看见自己 → **无限自嵌套**（上一轮两次复现：819 层 / 473 MB）。脚本有体积闸门、有自检，唯独对这个**用户提供的路径**没有校验 | ✅ 新增 `assertUsableOutDir()`，在**任何删除动作之前**拒掉两种危险路径。**实测 4 个用例**：=仓库根 → 拒（exit 1）；仓库内未排除名 `pkg` → 拒；仓库的上级目录 → 拒；默认/被排除的首层（`.learnbuddy`）→ 正常通过。拒后夹具完好无损 |
| **I26** | ✅ **已修复** —— 排除名单是 `.gitignore` 密钥规则的**手抄子集**，且自检与拷贝**不同源** | C | `EXCLUDE_FILES` 只有 4 个精确名字、自检用的 `FORBIDDEN_NAMES` 更只认 `.env`/`.env.local` → `.env.bak`（被 `.gitignore` 的 `.env.*` 覆盖，故 `git status` 干净）、`secrets.json`、`service-key.pem` 全被拷进上传包，而末尾**仍打印 ✅ 可以上传** | ✅ 收敛为**一份**规则 `isForbiddenFile()`（`shouldCopy` 与 `scan` 共用），覆盖 `.gitignore` 全集：`.env` / `.env.*` / `*.pem` / `*.key` / `*.p12` / `secrets.json`，例外 `.env.example`。**红/绿实测**：旧脚本产物 **8 个文件**（含 `.env.bak`、`secrets.json`、`service-key.pem`）且打印 ✅；新脚本 **5 个**、零泄漏；**真实仓库** 152 文件 / 1.53 MB ✅（未误伤） |
| **I27** | ✅ **已修复** —— `check-sensitive.mjs` 的计数会随环境漂移，而文档把总数写死 | C | `warn()` 只累加 `warned`、不计入 `passed`，而 4 处是 `ok()`/`warn()` 二选一 —— 其中"本机存在 `.env`"那处恰好由**照 `.env.example` 的说明做真实通道联调**触发：**按文档做事反而让计数少 1**（`sensitive` 15→14、`repo` 33→32） | ✅ `warn()` 也计入 `passed`（它是一条**已完成**的检查），汇总改为"N 项通过（其中 M 项注意）"。**实测同一 warn 分支下**：旧脚本 **13**、新脚本 **15**。现值 `sensitive` **16** |
| **I28** | ✅ **已解决** —— 这棵树是一次"**只落了一半**的合并" | C | 本树缺 `Dockerfile`、`.dockerignore`、`docs/tech/2026-09-17-云托管部署说明.md`，以及两个 `package.json` 的并集改动（`pack:deploy`、`verify:predeploy` 里缺 `verify:serve-web`、server 侧缺 `verify:serve-web`），而 `README`/`知识库`/`todo` 都把它们当成既成事实 → `npm run pack:deploy` 与 `verify:serve-web -w @lc/server` 直接 'Missing script' | ✅ 按**远端 main 的原文**补齐 3 个缺失文件 + 2 处脚本项；**逐文件核对：远端 149 个文件全部逐字节一致（149/149，缺失 0、不同 0）**。⚠️ 修此条**不需要** `git reset --hard`：本地未提交内容与远端 main 本就逐字节相同，补文件是零风险操作（详见 review 文档对"零损失"那条的订正） |
| **I29** | ✅ **已解除（2026-09-19，随 `I31` 一起）** —— 零材料 + 已授权补充块的会话上提问会掉进 `403 retryable=false` | C | 复现脚本：`.learnbuddy/repro-2026-09-18/zero-material-supplement.mjs` → `/api/session` → `/api/gap`（**materialVersion 0 下成功**，这是合法状态）→ `/api/tutor` **403 `UNAUTHORIZED_CONTENT`**、文案「AI 补充内容未经学生授权」。这正是 `I13` 要消灭的死路，只是换了个入口 | ✅ **拍板口径：不放宽判据，改 mock 保真**（详见 `I31`）。理由：守卫的语义是「回答必须能追溯到**真实存在**的来源，无引用即拒」，这是防线本身的定义；把"无引用也收"当修法，等于接受无法核对来源的内容。**修复后实测**：同一脚本 ③ 步 **403 → 200**，且响应里确实引用了那个补充块（`citations[0].refId === supplementBlockId`）；`verify:flow` 补 4 项断言（⑮）守住这条路径 —— 原 36 项一条都没经过它 |
| **I30** | ✅ **已修复（2026-09-19，文案口径）** —— `/api/quiz` 与 `/api/tutor` 对"有没有材料"给出**相反答案** | C | 同一会话：`POST /api/quiz {source:'material'}` → **400**「还没有材料……请先提交讲义」；而 `/api/tutor` 把那个补充块经 `toSlices(session)` 当作可引用来源。`:530` 数 `session.materials.length`，`:540` 用的是 `toSlices(session)` | ✅ **拍板：不改契约，只统一文案**。两侧判据答的是**两个不同问题** —— `/api/tutor` 问「有没有可引用的来源」（学生材料 **或** 已授权补充块）；`/api/quiz` 问「有没有**学生自己的材料**」，因为返回的题必须标 `source: 'material'`，而 `QuizSource` 只有 `'fixed' \| 'material'` 两个取值，拿补充块出的题标 `'material'` 就是**新的真实性红线**（加 `'supplement'` 取值属契约变更，会牵动 A/B 同步，本轮不引）。修复：会话里只有补充块时，400 文案明说「只有 N 段 AI 补充内容，没有你自己的讲义……」（原先笼统说"还没有材料"，会让学生以为会话是空的）。`verify:flow` 补 1 项断言（⑯） |
| **I31** | ✅ **已修复（2026-09-19）** —— mock 保真度缺口：`mockTutor` 忽略 `supplementIds` | C | `parseRefIds()` 已经能把 `【系统补充 <id>】` 解析出来，但 `mockTutor(materialIds, zeroMaterial)` **只接收 materialIds** → 只要有补充块、没有学生材料，mock 恒返回**无引用**的 `ai-supplement` 块。于是"从已授权补充块作答"这条路径在 mock 通道上**永远走不通**（`I29` 就是它的产物） | ✅ 已修：`mockTutor(materialIds, supplementIds)`，无学生材料时**引用已授权的补充块**并把 `sourceType` 标对；文案分三支（有材料 / 只有补充块 / 纯通用知识），第三支明确说「不是你的讲义原文」。同时：`mockKnowledge` 也改为**如实带上材料引用**（`I34` 的前置 —— 否则图谱永远判不出「材料已覆盖」）。⚠️ mock 自报的 `basedOnMaterial` 字段**实际不被采用**：教学层 `tasks.ts:410` 会按 `input.materials.length` 重算，路由层再按会话重算一次（见 `I36`）|
| **I32** | ✅ **已修复（2026-09-19）** —— 「再来一组」会抹掉刚提交的作答 | A | `QuizPanel.tsx:211` 的按钮只判 `disabled={busy}`，没判 `materialBlocked`（后者只挂在主按钮 `:99` 上）。学生答完题 → 把来源切到「按我的材料出题」（零材料）→ 警示出现、主按钮变灰，但「再来一组」仍可点 → `load()` 拿到 `null` → `setItems(null)`/`setPicked({})`/`setSubmitted(false)`（`:49-54`）把题与答对结果**整片抹掉** | ✅ 已修**两处**（只修一处都不够）：① 该按钮 `disabled={busy \|\| materialBlocked}`；② `load()` 里 `if (result === null) return;` —— `null` 表示"**没取到题**"（被拦或请求失败），不等于"这次没有题"，拿不到就不该动现有状态（失败提示已由 hook 抛出）。同时这一并修掉了 `I20④` |
| **I33** | ✅ **已修复（2026-09-19）** —— 面板声称「已作为画像事件上报」，但默认来源下根本没有请求发出** | A | `QuizPanel.tsx:219` 写「本次提交已作为一条画像事件上报（答对 X / Y）」，而 `useWorkbench.reportQuizAttempt` 开头是 `if (!id) return;`（`:678-679`，`id = sessionRef.current`）；默认的「项目自编题」来源**不会建会话**（`:654` 只在 `source === 'material'` 时建）→ 新访客提交后没有 `POST /api/profile` | **与 `I20⑦` 同一类无据声明**（本轮在别处已修）。✅ **已修**：`reportQuizAttempt` 的返回值改为 `QuizReportOutcome = 'sent' \| 'no-session' \| 'failed'`（**如实告诉界面到底发生了什么**），面板按结果分三支文案 —— 自编题路径明说「**没有**上报画像事件：这一组不需要学习会话，因此没有发出任何请求」。同类无据声明在本项目已出现三次（`I20⑦`、`I33`、`I30` 文案），共同教训：**"听起来合理的话"必须能对应到一次真实发生的动作** |
| **I34** | ✅ **已修复（2026-09-19）** —— 图谱节点 id 与覆盖判定的 id 空间不重叠 → 每个节点永久「未判定」 | A | `GraphPanel.tsx:42-51` 的 `nodeStatus` 用 `node.id` 去查 `gaps[node.id]` 与 `prerequisites[].conceptId`，而生成数据里 `points[].id = 'kp-monotonicity'`、`prerequisites[].conceptId = 'kp-derivative'`（`apps/server/src/model/index.ts:88/103`）—— 两者**从不重叠** → 查询恒落空 → 顶部「有 N 个节点本会话没有覆盖判定记录」永久常驻（本会话其实做过判定） | ✅ 已修：`nodeStatus` 增补第三个来源 —— `points[].id === nodeId` 且 **`citations` 非空** → `LOCAL`（契约对 LOCAL 的定义就是"能定位来源"）。只在有引用时才判 `LOCAL`：没有引用仍标「未判定」，**不把"模型没给引用"说成"材料未覆盖"**（那也是一次它没做过的判定）。同时 `mockKnowledge` 改为如实带上材料引用，否则这条修复在演示里看不到效果。`verify:render` 新增 **4e** 一节（真实 id 形态 + 反例，3 项断言 + 2 项渲染断言）—— 原 4d 用例把 id **手工对齐**了，**锁不住真实场景**，这是它长期为绿的原因 |
| **I35** | ✅ **已修复（2026-09-19，服务端侧）** —— `I14` 的提示条会把内部 refId 渲染给学生 | A（+B） | `TutorPanel.tsx:118` 把 `droppedBlocks.reasons` 原样拼进学生可见文案，而 `packages/teaching/src/validate.ts:69/72/75` 的三种 reason 都内插了 `citation.refId`（补充块用 `randomUUID`，`routes.ts:472`）→ 界面会显示「引用的来源不存在：9f1c2b7a-…」 | ✅ 选择**服务端改写**（契约对 `reasons` 的定位本就是"给学生看的说明"，就该由产出方保证可读；前端截断是"按形态猜"，文案一变就静默失效）：在 `routes.ts` 加 `toStudentReason()`，三类 reason 改为面向学生的话术；**原始文本不丢** —— 丢弃发生时记 `tutor.blocks.dropped` 日志（含 refId）供排查。⚠️ 改写点在 C 的 `routes.ts`，**未触碰 `packages/teaching`**（B 的工件）。既有断言用的是**不含 id** 的那条 reason，故不受影响；但"带 id 的那种 reason"目前仍**没有断言覆盖** —— 已如实登记，见 §已登记断点 |
| **I36** | ✅ **已修复（2026-09-19，本轮核实中新登记）** —— `basedOnMaterial` 把「只有 AI 补充内容」报成「基于你的材料作答」 | C | 契约语义是「是否基于**学生材料**作答；轻路径为 false」（`packages/contracts/src/tutor.ts`），而实现链上算的是 `toSlices()`（学生材料 **+ 已授权补充块**）：`packages/teaching/src/tasks.ts:410` 按 `input.materials.length > 0` 算，`routes.ts` 又直接透传 `result.basedOnMaterial`。于是"点过补缺口、但没交讲义"的会话会被标成基于材料 —— 界面据此显示「基于你的材料」，与学生实际提交的东西不符。与 `I20⑦`/`I30`/`I33` 同属**无据声明**一族 | ✅ 已修：路由层改为按会话重算 `basedOnMaterial: hasStudentMaterial`（`session.materials.length > 0`），教学层的值不再被采用。`verify:flow` 补 1 项断言（⑮）守住"只有补充块 → `false`"。⚠️ 顺带记录一条事实：mock 自报的 `basedOnMaterial` **从来不被采用**（教学层会重算），所以 `I31` 里对 mock 该字段的改动是"顺手对齐语义"，不产生行为 |
| **I37** | 🟡 **前置全部就绪（2026-09-21 复核），待人工在云端重做一次部署** —— 线上源码快照落后于 `main` | C | 线上实测（2026-09-19）：`/nope.webmanifest`、`/assets/` 仍返回 `200` + 首页 HTML —— 正是 `I22②③` 修掉的两个回退漏口，说明镜像源码快照停在 `#9` 之后、`#10` 之前。`/api/health` 的 `builtAt` **不能**用来判断版本：它是产物文件的 mtime（= 镜像构建时刻），不是源码版本。**09-20 复测补充**：安全响应头 **0/7**（阶段 0 那一组头一条都没上线）＝更硬的"快照旧"证据；`/nope.webmanifest` 仍 200；`/assets/nope.js` 实测 **404**（与 09-19 记录有出入，疑平台/网关层拦截，不足以推翻本条） | **处置**：用当前 `main` 重新走一次「打包 → 上传 → 重建」，步骤见 `tech/2026-09-17-云托管部署说明.md`。⚠️ **重做前必须先让本地树与 `main` 对齐** —— **已于 2026-09-21 重新对齐**（`HEAD` = `main` = `origin/main` = **`72ae5ba`**、工作区干净、树 `901327e2`；本批改动已随 PR #12 并入 `main`），可直接打包。**逐项前置检查 + 打包文件数核对 + 线上验收判据（含阶段 0 响应头）见 `plans/2026-09-20-I37重部署前置检查清单.md`**。验收：重部署后 `/nope.webmanifest` 与 `/assets/` 必须是 **404**，`/nope` 仍是 200（与 `verify:serve-web` 同一组判定）；另需 7 条安全响应头齐备。**优先级高于阶段 0 的全部工作** —— 评委点开的正是这条链接。**09-21 复核：⚠️ 09-20 打的那个包已作废** —— `D:\smallbox\Projects\LearnBuddy-deploy-0920`（163 文件 / 2.01 MB / 自检 ✅）**打在 `B3` 之前，不含符号验证与图谱边，不要拿它上传**；前置既已满足（见左）→ ✅ **已重打（2026-09-21）**：`D:\smallbox\Projects\LearnBuddy-deploy-0921b`（**175 文件 / 2.25 MB / 自检 ✅**）→ **只剩控制台上传与重建这一步**（另注：旧 `deploy-dist/` 只能在资源管理器里手删，工具侧删不掉；打包请用 `PACK_DEPLOY_OUT` 指到新目录） |
| **I38** | 🆕 **新登记（阶段 0 卡 5 的工具抓出来的第一条）** —— `apps/server/src/model/deepseek.ts` 存在循环依赖 | C | `npm run lint:boundaries` 首次运行即报 `no-circular: apps/server/src/model/deepseek.ts → …`。循环依赖的典型危害是"其中一个模块在运行时是 `undefined`"，且表现随打包顺序漂移 —— 这类缺陷最难查。**工具第一次运行就产出真发现，说明这道门禁不是摆设** | 建议按 depcruise 报告的环逐个拆（把共享常量/类型提到独立文件）。**本轮不修**：阶段 0 的口径是"先看见"，把它作为阶段 1 的输入；修之前先补一条断言（否则改完无法证明环消失）。**09-20 实况：`npm run lint:boundaries` 现报 7 条 warn** —— 本条这 1 条环（`deepseek.ts ↔ model/index.ts`）＋ **6 条 `panels-should-not-import-state-internals`**（`TutorPanel`/`QuizPanel`/`ProfilePanel`/`MaterialPanel`/`KnowledgePanel`/`GraphPanel` → `hooks/useWorkbench.ts`）。后 6 条正是**阶段 1 的门面**（`app/model/workbench.ts`）要消掉的 → **建议与阶段 1 同批处理，并把"7 → 0"当作该阶段的量化验收**。✅ **2026-09-20 环已拆分**：`ModelAdapter` 契约下沉到叶子模块 `apps/server/src/model/adapter.ts`，`deepseek.ts ↔ model/index.ts` 的环消失。⚠️ 但**"7 → 0"不能单独当阶段 1 的验收**（见《可行性订正与实施卡片》订正 2：阶段 1 搬路径会让规则两端同时失效、报 0 条是"匹配不到模块"而非"耦合消失"）；正确判据是 `grep -rn "useWorkbench" apps/web/src` 的计数（阶段 1 后仍应是 11，阶段 4 才是 0）。**本条已按"逐条转阻断"处理：`no-circular` 已转 `error`** |
| **I39** | ⏸ **待处置（本轮实测踩到，影响本地构建）** —— npm 可选依赖缺陷导致 `@rollup/*`、`@esbuild/*` 平台包被剪掉 | C | 现象：在仓库根跑 `npm install -D <新依赖>` 后，`node_modules/@rollup/`（平台二进制）与 `node_modules/@esbuild/` **整个目录消失** → `tsup src/index.ts` 报 `Cannot find module @rollup/rollup-win32-x64-msvc`，`npm run build` 直接失败。**注意：`package-lock.json` 里其实有 win32 条目**（`grep -c rollup-win32-x64-msvc` = 1），所以这不是"锁缺条目"那么简单 —— 是 npm 已知缺陷（npm/cli#4828）在重解析时把已安装的平台包剪掉 | 临时处置（**2026-09-21 更正：原记的 npm 装法在本环境不可用**）：原记的 `npm install --no-save --no-package-lock @rollup/rollup-win32-x64-msvc@<版本> @esbuild/win32-x64@<版本>` **实测跑了 28 分钟并失败**，平台包一个没装上（不要再照抄这条）。**有效手法是绕开 npm、直接取 tarball 解压**：`curl -sSL -o .tmp/r.tgz https://registry.npmjs.org/@rollup/rollup-win32-x64-msvc/-/rollup-win32-x64-msvc-<版本>.tgz` → `mkdir -p node_modules/@rollup/rollup-win32-x64-msvc` → `tar -xzf .tmp/r.tgz -C node_modules/@rollup/rollup-win32-x64-msvc --strip-components=1`（约 1 MB，**几秒完成**）。版本跟 `node_modules/rollup/package.json` 走（本次 **4.63.3**）。**只需补 rollup 一个**：`@esbuild/win32-x64` **不必补**（实测 `require('esbuild')` 与 `vite build` 均正常）。此法**不写锁文件**，属本机临时修复。**根治**：重新生成一份含全平台可选依赖的锁文件（`Dockerfile` 里那条注释说的同一件事，现在证明它**不止影响 Linux 容器，也影响本地 Windows**）→ 方案见 `tech/` 待补。（**2026-09-21 核实，防止照"补齐 musl"做错**：**esbuild 没有 musl 专用包** —— npm 上 `@esbuild/linux-x64-musl` 返回 `Not found`，其 Linux 版是静态二进制、Alpine 上直接用 `@esbuild/linux-x64` 即可（现网容器即如此）。故"补 musl 条目"**只需针对 rollup**，**不要**去给 esbuild 补不存在的包。）**09-20 复核（含精确基线）**：**当前不阻断** —— `node_modules/@rollup/rollup-win32-x64-msvc` 与 `@esbuild/win32-x64` 都在，`npm run verify:predeploy`（含 `build:clean` → `build`）全绿，**不要读成"现在本地构建会挂"**。但根因**仍在且已量化**：锁文件 `packages` 段里 `node_modules/@rollup/rollup-*` 与 `node_modules/@esbuild/*` 的平台键**各 0 个**，`@rollup/rollup-linux-x64-musl` 只作为 `optionalDependencies` 的**名字**出现 1 次（本条原写的"锁里其实有 win32 条目"指的就是这处名字）→ Linux 上 npm 认为"已满足"却拿不到包，`Dockerfile` 的 `rm -f package-lock.json` 正是为此。**验收判据**：锁文件里出现目标平台的 `packages` 键（如 `node_modules/@rollup/rollup-linux-x64-musl`），且容器**去掉** `rm -f package-lock.json` 后仍能构建 |
| **I41** | ✅ **已处置（2026-09-21）** —— **决策编号重号**：`知识库.md` §14 内部 `D1`/`D3` 各出现**两次**，且与 `todo.md` 的 `D1`–`D9` 命名空间不同 | C | `知识库.md` §14 里既是「`D1` 符号验证引擎 = 纯 TS」又是「`D1` 赛事开源表述 ✅ 关闭」，既是「`D3` 材料上限 = 5 份/15000 字」又是「`D3` 模型通道选型」；而 `todo.md` 的 `D1` 是"赛事开源表述"、`D3` 是"通道选型" —— **两套编号指的不是同一批事** | **危害**：引用"按 `D3` 执行"时**无法判断**说的是材料上限还是通道选型（本轮写后续推进文档时就撞上了）。**处置建议**：`知识库.md` §14 改为带前缀的稳定编号（如 `K-1…`）或按主题命名，并让 `todo.md` 的 `D` 号与之互指；**在此之前，凡引用决策必须连内容一起写**。✅ **2026-09-21 处置**：在 `知识库.md` §14 表**之前**加「编号重号提示」段（写明两套来源、各自的含义与消歧规则），并在 4 条重复项后**注明来源轮次**（"第 6 轮裁定" ／ "09-19 拍板"）；编号本身按"不涂改历史"保留。**引用规则：凡引用决策必须连内容一起写。** 本项**不阻塞任何工作**（后面还要引用 `D5`/`D7`） |
| **I42** | 🆕 **新登记（2026-09-20，`I1` 落地时发现）** —— 图谱关系边的**端点可以不是节点**，界面此前**静默丢边** | A（界面）／ B·C（契约判断） | 契约里边的 `to` 是"被依赖的**前置概念**"，而图谱节点只来自 `points[]`（材料抽出的知识点）。演示案例正是这种形态：边 `kp-monotonicity → kp-derivative`，而 `kp-derivative` 因材料未覆盖（`MISSING`）**不是节点**。`GraphPanel` 用 `position.get(edge.to)` 取坐标，取不到就 `return null` → 头部显示"1 关系"却**一条线都没有**，看的人只会以为界面坏了。**已处置**：界面改为**如实说明**"另有 N 条关系指向尚未抽取为节点的概念（通常是材料未覆盖的前置缺口），因此这里画不出连线"，并配**防恒真的反例断言**（端点都是节点时不出现该说明）。**未决（属契约判断，本轮不擅自改）**：前置概念**应否**补为图谱节点 —— 若补，节点数、`nodeStatus`（会落 `MISSING`/`SUPPLEMENTED`）与多组断言都会变，需 B/C 拍板后统一改 | 已处置部分：`verify:render` 104 → **107**（0 失败）。未决部分：等 B/C 决定是否让"前置概念"进入 `SessionGraph.nodes`。⚠️ **09-20 `B3` 轮补记（同一根因的第三处）**：`I34` 给 `GraphPanel.nodeStatus` 加的"第 2 条判定来源"（按 `prerequisites[].conceptId` 找节点）**因此是死分支**；`I15` 的"反查概念中文名"也因此**通常查不到**（`I15` 已如实退回 `conceptId`）。⇒ **这一处决定牵动 `I34` / `I15` / `I42` 三件**，越早定越省 |
| **I43** | 🆕 **新登记（2026-09-20，`B3` 落地轮）** —— **体积闸门基线由 5 → 7 个超限文件** | B/C | `npm run verify:file-size` 现报 **7 个**：`useWorkbench.ts` 994、`routes.ts` **868**（+70）、`symbolic.ts` **852**（新增）、`tasks.ts` 448（+35）、`store/index.ts` 401（+20）、`model/index.ts` 361（过线）、`GraphPanel.tsx` 333（+19）。⚠️ 这正是 `I40` 的教训重演：**"告警常态非零"会稀释信号** —— 所以必须登记而不是让它悄悄涨。**拆分属阶段 4 的活**（结构冻结线内不动）；`model/index.ts` 的文件头本来就写着"mock 应移到 `./mock.ts`"，那是顺手的下一步 | 阶段 4：任一文件 ≤300 行后转阻断。在此之前**基线数字要跟着改，别让它无声上移**（本条就是为此存在） |
| **I40** | 🆕 **新登记（2026-09-20，状态核对时发现）** —— `npm run lint` 残留 **3 条 warning**，工具链自身不干净 | C | 阶段 0 装了 `eslint` 并定为"只告警"，但告警一直没人看：`scripts/check-file-size.mjs:120` 的 `rows`、`scripts/check-licenses.mjs:66` 与 `:229` 的 `file` 均为**声明后未使用**。**危害不大但会稀释信号** —— 一旦 warning 常态非零，"新增一条 warning"就再也没人发现（这正是 `I24`"恒真断言"的同类教训：判据失去分辨力）。`eslint` 当前 **0 error / 3 warning** | 删掉三个未使用的绑定（或按规范加 `_` 前缀）后回到 **0 warning**；此后 lint 的新告警才看得见。**低优先** —— 排在 `I9` 残留与 `I38` 之后，但**不要长期不管**。✅ **2026-09-20 已闭合**：三处未使用绑定已删（`const rows = await report(limit)` → `await report(limit)`；两处 `for` 解构去掉 `file`），`npm run lint` **0 error / 0 warning**。⚠️ 这三条检查**不纳入回归项数**（`知识库.md` §9 写明），故未牵动 433 |

---

## 🧭 拆分路线图（阶段 0–8 + V/W，引自计划书）

> **唯一依据**：`plans/2026-09-18-单页面Web应用拆分计划书.md` §14（逐阶段的工时、负责人、验收、回滚）
> 与 `plans/2026-09-19-阶段0实施准备（可执行）.md`（阶段 0 的落地细节）。
> 本表只做**认领入口**，不重复计划书的论证与验收标准 —— 认领前请读那两份。
> ⚠️ **冻结线**（`T-07` 已接受）：**A 线 09-25→09-26**只修缺陷、不动结构；
> **B 线 10-10 起**停止一切结构改动。

| 阶段 | 内容 | 负责人 | 状态 |
|---|---|---|---|
| **0** | 兜底与边界：安全头 / `ErrorBoundary` / 超时取消 / sessionStorage 清理 / 边界工具 / 目录骨架 | A+C | ✅ **已完成（2026-09-19）**，见 `changelogs/2026-09-19-拆分阶段0-兜底与边界.md` |
| **1** | 装配层与目录骨架：`App.tsx` 拆分、面板移入 `widgets/`、门面 `app/model/workbench.ts`、`@/` 别名、规则**转阻断** | A | ⬜ 未开始（计划书 §14.2）。**09-20 基线（供验收对照）**：`lint:boundaries` **6 条 warn / 0 error**（09-20 起 `no-circular` 已清零并转 `error`；余 6 条是 6 个面板越界）、`verify:file-size` **5 个文件超 300 行**（最大 `useWorkbench.ts` **994**；09-20 `GraphPanel.tsx` 仍在其中）、`lint` **0 error / 0 warning**（`I40` 已闭）。⚠️ 冻结线 A 线 09-25 → **实际窗口只剩 5 天** |
| **2** | `entities` 切片：7 个实体 + 引入 **vitest** + 19 条单测（护栏首次可测） | A | ⬜（§14.3） |
| **3** | `features` 切片：7 个 feature + 13 条单测；`verify:flow` 扩展为完整学生动线 | A | ⬜（§14.4） |
| **4** | 接线与**门面删除**（硬性终态：`grep useWorkbench` 必须为 0，任一文件 ≤300 行） | A | ⬜（§14.5） |
| **5** | 样式分片与设计系统（`index.css` 951 行 → tokens + base ≤150 行） | A | ⬜（§14.6） |
| **6** | 受限抽包：`packages/ui` + `packages/api-client`（**只抽两个**） | A（C 会评） | ⬜（§14.7） |
| **7** | 路由化（`/login`、`/teacher` 两条路由 + 两层守卫）—— **已确认要做**，排在登录实现之前 | A | ⬜（§14.8） |
| **8** | 质量基建：CI、E2E/视觉/可访问性、体积门禁、SBOM、多阶段 `Dockerfile` | C+A | ⬜（§14.9） |
| **L** | 契约阶段：接口 10 → 约 14、角色、错误码（**必须先于登录实现**） | C | ⬜ 未开始（§14.12） |
| **V** | 登录实现（自建演示级、内存态、不做长效 cookie；**归属校验 + CSRF 必须同批上线**） | C+A | ⬜ **等阶段 L**；`D7` 已定，`D8` 排期可调 |
| **W** | 教师视图**形态 A（真数据）**（`TEACHER_MIN_SAMPLE` 的"样本不足"、`audit.teacher.read`、**不得出现未标注来源的数字**） | A | ⬜ 等 `L`/`V` |

---

## 📋 计划项（Plan）

### P-A · 成员 A（`apps/web`）

| 编号 | 事项 | 状态 | 验收 |
|---|---|---|---|
| P-A1 | 确保前端 demo **公网可访问** | ✅ **已达成（2026-09-19）** | 随 `P-C14` 一起落地：`https://weijifen-xueban-315516-12-1490356402.sh.run.tcloudbase.com/`，页面与 `/api` 同源。⚠️ 前端侧**无需改动**（`P-A16` 已定"同源直连"），但**"无痕窗口打开即用"这条验收要人工做一次**，且要连"访问提示中间页"一起确认（本机做不了浏览器验证，`I8`） |
| P-A2 | 准备 **mock 入口与真实调用入口两条路径**并在界面明确区分 | ✅ | 页头横幅说明「不代表真实模型输出质量」+ **每条答疑回答带 `mock 演示数据` 标记**；真实通道下标记不出现（有断言）。PR #4 |
| P-A3 | 材料路径工作台（卡片 / 六态 / 缺口交互 / 依据详情 / 练习 / 画像） | ✅ | PR #4 |
| P-A4 | 图谱视图（只读，缺口语义高亮） | 🟡 | **渲染路径已锁住**：第 8 轮补 10 项断言（边渲染 / 中文关系标签 / 「（推断）」标注 / **有边时不再显示"没有关系边"** / 按依赖深度分层为多列 /**无边时不假装分层**）。**完整验收仍等 `I1` 提供边**（B 侧） |
| P-A5 | 画像视图（会话内掌握状态、缺口历史） | ✅ | PR #4 |
| P-A6 | 教师视图界面 | ⬜ | 依赖 I7；无数据前不渲染 |
| P-A7 | 语音输入入口 | ✅ **已实现（2026-09-22）** | 走 `D3` 定的**浏览器内置识别**，前端独立落地：`shared/lib/voice-input.ts`（会话 + 60 s 超时 + 异常映射）、`shared/lib/voice-support.ts`（能力探测与文案）、`components/VoiceInputButton.tsx`（按钮）。识别结果**填入输入框、学生确认或就地修改后才提交**（语音错字多，不自动提交以免污染图谱）。`/api/parse` 语音路径**仍未接入**（服务端不转写），这条边界在界面与 `/api/health` 都如实标注。`verify:voice` **61 项**锁住九类分支。PR #4 只补了入口，本轮接上真能力 |
| P-A8 | 知识卡片与图谱联动高亮 | ✅ | 选中状态提升到状态层（`wb.focusedNodeId` + `focusNode`），卡片与图谱**共用一份选择**：点卡片「在图谱中查看」→ 节点高亮并自动滚入视区；点节点 → 卡片同时高亮；选中项不在图谱时**如实说明**而非假装定位。`verify:render` 新增 16 项断言。第 7 轮 |
| P-A9 | **界面验收清单**（每项：入口、操作、期望结果） | ✅ | `specs/微积分学伴_界面验收清单.md` V1.0：三入口 / 九步闭环 / 联动 / 真实性 / 错误态 / 窄屏六组，含走查记录表。**明确写出"不能替代浏览器视觉验证"**。第 7 轮 |
| P-A10 | 演示材料：3 分钟视频脚本、录屏、PPT 截图 | 🟡 **阻塞已解除，待人工录制** | 脚本已就绪（见 `P-A14`）；**录屏所需的在线链接已于 2026-09-19 交付**（`P-C14`）。⚠️ 录制前建议先处置 `I37`（线上快照落后），否则录进去的是缺 `I22` 修复的版本 |
| P-A11 | 未实现能力的**标注复查**（图片/语音/教师视图/图谱边/错题归因） | ✅ | 逐处复查：图片/语音入口提示、未接入识别环节列表、图谱无边说明、教师视图不渲染、错题归因说明；`verify:render` 锁住这些标注。PR #4 |
| P-A12 | 窄屏与可用性自检 | ✅ | ① 双栏在 `max-width: 900px` 折叠为单列（原有）；② 新增 `max-width: 560px` 手机竖屏：单列 + 留白收紧 + 面板头可换行；③ **新增长文本防御**：对公式 / 讲义原文 / 引用编号等长 token 统一 `overflow-wrap: anywhere`（用 `anywhere` 而非 `break-word`，否则 grid/flex 仍按不可断行算宽度）；④ `code` / `img` / `.graph-scroll` 不撑破页面。⚠️ **视觉验证仍需人工**（`I8`），验收清单 `F-1`–`F-6` 标注为👁 |
| P-A13 | 错误态与重试走查 | ✅ | **修掉真 bug**：忙碌态原为单值，两动作互相覆盖 → 改 `pendingRef` + `begin/end` + `isBusy(key)`（单飞）；新增真正的**重试按钮**（仅 `retryable` 才记）；失败保留输入。PR #4 |
| P-A14 | 视频**分镜与旁白**（可先写脚本，后补录屏） | ✅ 脚本完成 | `plans/2026-09-17-A-3分钟演示分镜与旁白.md`：按 §8.2 九步闭环排 9 个镜头、合计 180 秒，含逐镜旁白、通道选择（真实优先，用 mock 则**必须保留横幅与逐条标记**）、录制纪律（只许裁等待时间，不许裁标注）。**关键处置：第 6 步符号验证不可演示（`B3`），分镜里如实演"未验证"而非跳过**。录屏仍属 `P-A10` |
| P-A15 | 前端冒烟脚本（弥补无法做浏览器视觉验证） | ✅ **107 项** | `npm run verify:render`：用 `react-dom/server` 渲染六个面板，断言渲染不抛异常 + 关键文案在 HTML 里。**不替代视觉验证**。PR #4 建（42）；第 7 轮 P-A8 补 16 → 58；第 8 轮 P-A4 补 10 → 68；缺陷修复轮补 20 → 88；**09-19 I 类推进轮补 10 → 98**（`I34` 真实 id 形态 3 项、`I20⑤` 真值表 5 项、新渲染用例自带 2 项）；**09-19 拆分阶段 0 补 6 → 104**（`ErrorBoundary` 纯逻辑 2 项 + 渲染子树 1 项、`describeAbort` 真值表 3 项）；**09-20 `I1` 轮补 3 → 107**（端点不是节点的边要如实说明 1 项 + 防该断言恒真的反例 1 项 + 新渲染用例自带的"渲染未抛异常" 1 项）。⚠️ 阶段 0 的 `ErrorBoundary` 断言**刻意不写"渲染抛错 → 出现兜底文案"**：`renderToStaticMarkup` 下错误边界不生效，那条写法要么假失败、要么被绕过后恒真（同 `I24` 的教训） |
| P-A16 | 前后端**同源部署**的前端侧配合（`/api` 代理或直连） | ✅ 口径已定：**同源直连，前端无需改动** | `apps/web/vite.config.ts` 的注释本就写着「前端只与同源 `/api` 通信，密钥与模型调用全部留在服务端」，`apps/web/src/api.ts` 也一律相对路径 `/api/*` —— 开发态走 vite 代理、部署态走同源托管（`P-C13`），**两条路都是同源**，所以"代理或直连"在本项目里是同一个答案。⚠️ 结论来自代码与配置，**未做浏览器实测**（本环境做不了） |

### P-B · 成员 B（`packages/teaching`）

| 编号 | 事项 | 状态 | 验收 |
|---|---|---|---|
| P-B1 | **符号验证最小闭环** | ⬜ | 见 B3（阻断项） |
| P-B2 | 六态与验证回环（失败触发修正回环而非直接输出） | 🟡 | `MISSING→SUPPLEMENTED→VERIFIED` 可达；失败标「验证未通过」（用例 E14） |
| P-B3 | 图谱构建：抽取 → 归一 → 验证 → 增强 → 整体提交 | 🟡 | 见 I1；仅提及不算解释；不误判缺失（E1/E2）；循环依赖被排除（E15） |
| P-B4 | 边界与来源：六类问题分类、三类来源 | 🟡 | 部分可答分开处理（E5）；越界引导回当前主题 |
| P-B5 | 多模态解析（文字/公式/图像/语音 + 低置信度标记） | ⬜ | 见 I6；6 份素材人工比对；模糊内容**不补全** |
| P-B6 | 练习与辅导：固定题唯一、按材料出题附依据、变式题 | 🟡 | 解析注明所用规则与验证状态 |
| P-B7 | 教学与边界测试记录 | ⬜ | 12 个答疑问题、9 道固定题、E1–E7/E12/E14/E15 主测；数学结论**人工核验 + 符号验证双重通过** |
| P-B8 | 多智能体协作与降级机制（P1） | ⬜ | 任一 Agent 失败不影响主流程并如实标注（E18） |
| P-B9 | 错题归因（P1） | ⬜ | 四类归因各有可复现样例；**须给出可核对理由，不得仅凭答案对错断言** |
| P-B10 | 参赛说明：教学案例、AI 能力与限制 | ✅ | `specs/微积分学伴_参赛说明.md` V1.0：教学案例（六步）、AI 的四个调用点、**两道不信任处理**（归一 + 校验）、三条界面明说的边界、能力边界三分类（已实现 / 已设计未实现 / 后续）。**明确写出"符号验证未实现 → §2.4 三个固定案例目前没有自动核对"**。第 8 轮 |
| P-B11 | **提示词与工作流配置整理入库** | ✅ | `tech/2026-09-17-B-提示词与工作流配置.md`：**六段提示词全文逐字照录**、调用链、归一规则表、四条输出拒绝规则、路由与降级规则（**并如实写明多智能体"未实现"**）。配 `npm run verify:prompts` 逐字比对代码，防止快照漂移。第 8 轮 |
| P-B12 | 图谱归一补测：自依赖 / 循环依赖 / 别名合并 | ⬜ | 用例 E15 有断言；同义概念合并后不产生自环 |
| P-B13 | **LaTeX → 表达式白名单映射表** | ⬜ | 符号验证的前置：转换失败即标 `unverified`，**不做猜测性修复** |
| P-B14 | 边界用例 E1–E7 / E12 / E14 / E15 逐条执行并留痕 | ⬜ | 每条：输入、期望、实际、结论；失败案例也要记 |
| P-B15 | **教学素材 9 份**（3 主题 × 文字/图片/语音） | ⬜ | 单调性素材保留导数前置缺口（演示案例的前提） |
| P-B16 | 符号验证**固定案例基线断言** | ⬜ | §7.1 三案例的期望值与回归脚本共用同一份，任何改动都必须仍然通过 |
| P-B17 | 变式题（按画像弱点生成 1—2 道） | ⬜ | 解析附依据与验证状态 |

### P-C · 成员 C（`apps/server`、`packages/contracts`）

| 编号 | 事项 | 状态 | 验收 |
|---|---|---|---|
| P-C1 | 图谱落盘 + `GET /api/graph` | ✅ | PR #3 |
| P-C2 | 契约迁移 V2.0（十接口 / 六态 / 验证状态） | ✅ | PR #3；与 `tech/` 记录逐字一致 |
| P-C3 | 画像最小实现（会话内聚合） | ✅ | 事件可追溯；补充内容不迁移（E16） |
| P-C4 | 模型通道补全（视觉 / 嵌入 / 语音经 `ModelCaller` 抽象） | ⬜ | `/api/health` 如实报通道与验证引擎状态；缺密钥明确报错 |
| P-C5 | 回归脚本扩展 | 🟡 | 现有 **270 ＋ 46 ＋ 11 ＋ 107 ＋ 43 ＝ 477**（`all` / `flow` / `dist` / `render` / **`serve-web`**），另有仓库级检查 **36 项**（`verify:repo` ＝ sensitive 17 + licenses **11** + prompts 8），**合计 513**；全部不消耗额度。⚠️ 数字随改动同步更新：09-17 缺陷修复轮 `flow` 32→36、`render` 68→88；09-18 合并轮新增 `verify:serve-web` 18 项，合计 376；09-18 收尾修复轮 `serve-web` 18→28、`repo` 33→34，合计 387；**09-19 I 类推进轮 `flow` 36→42、`render` 88→98，合计 403**；**09-19 拆分阶段 0 `serve-web` 28→43（安全响应头 15 项）、`render` 98→104（ErrorBoundary 与中止分类 6 项）、`repo` 34→35（前端产物无密钥 1 项），合计 425**；**09-20 计划落地轮 `all` 190→194（graph 53→57，`I1` 的 mock 边 4 项）、`flow` 42→43（`/api/graph` 读回关系边的正面路径 1 项，此前该处因 mock 无边而恒真）、`render` 104→107（悬空边如实说明 2 项 + 自带渲染用例 1 项），合计 433**；**09-20 `B3` 落地轮新增 `verify:symbolic` 69 项并接进 `verify:all` 链 → `all` 194→263；`flow` 43→46（⑥/⑦/⑧ 的状态期望随 `B3` 改为 `VERIFIED`/`symbolic`，并补"重放状态仍为 VERIFIED"与"长度落在区间内"）；`repo` 35→36（`licenses` 10→11，`mathjs` 转真依赖后那条"计划引入"告警消失），合计 433→506**；**09-21 远端审查轮 `all` 263→**270**（`guards` 81→**88**：`W0-7①②` 把 `toStudentReason` / `buildDroppedBlocks` 提到无副作用的 `http/student-reason.ts`，补上"含未知 `refId` 的 reason 改写后不得含 uuid"与"部分通过时 `droppedBlocks` 的正面路径"共 7 项），合计 506→**513****。另有阶段 0 新增的**只告警**检查：`npm run lint`（09-20 起 **0 / 0**）、`npm run verify:file-size`（**09-20 起 7 个超限文件，见 `I43`**）；`npm run lint:boundaries` 的 `no-circular` **已转 `error`**，其余仍只告警（这些**不纳入"回归项数"**：它们不产生通过/失败计数）。⚠️ **`verify:repo` 的项数现已随检查项变化（36 = 17+11+8）**，但**每一子脚本内部的计数仍是恒定的**（两轮修正过：断言无条件、`warn` 计入 `passed`） |
| P-C6 | 联调与发布（组合教学模块、页面、验证引擎） | ⬜ | 闭环走通、阻断清零 |
| P-C7 | **部署与公开前置** | 🟡 | 公开侧 ✅（`B2` 已转 public）；**部署侧**：同源托管（`P-C13`）**本地部分已实现并验证**（PR #9），`P-C20` 已把 `verify:serve-web` 接进 `verify:predeploy`；**剩**云端执行与**在线链接**（`P-C14`）。姓名按决定不填（`I4` 已关闭） |
| P-C8 | 材料与提交：PPT、源码归档、启动说明、回执 | ⬜ | 按时提交并核查回执 |
| P-C9 | 契约细化 `ProfileEventInput`（去掉请求侧冗余 `sessionId`） | ⬜ | 见 I2；改完 **A 必须同步** |
| P-C10 | 材料替换语义（让用例 E12 可完整实现） | ⬜ | 见 I3；契约变更，需 A/B 确认 |
| P-C11 | `/api/teacher` 班级聚合实现 | ⬜ | 只读 + 脱敏（E17）；样本不足提示「样本不足」 |
| P-C12 | `/api/health` 增加构建信息（版本、构建时间、产物形态） | ✅ | `HealthResponse.build`（**可选字段，加性**）：`mode`（`dist`/`dev`）、`entry`（**只报文件名**）、`builtAt`（取不到即 `null`）。`verify:dist` 新增 3 项断言 —— 其中一项专断 **产物态必须自报 `dist`**，另一项断 **不得泄漏本机绝对路径**（health 是公开接口）。第 8 轮 |
| P-C13 | **前后端同源部署**（静态资源托管 + `/api`） | 🟡 **本地已实现并验证；云端待执行** | 实现：`apps/server/src/http/serve-web.ts`（挂静态目录 + SPA 回退）。回退范围**收紧到"只服务前端路由"**：`/api` 前缀与**任何带扩展名的路径**一律不回退 —— 否则取不到的 `/assets/x.js` 会返回 200 + 首页 HTML，浏览器按 JS 解析就报 `Unexpected token '<'`，而"资源没打进镜像"看起来像"页面正常"。**09-18 收尾修复轮又补住三个漏口**（见 `I22`）：判定必须**先解码**（`/foo%2Ejs`）、扩展名**不设长度上限**（`/manifest.webmanifest`）、结尾带斜杠的目录式请求（`/assets/`）也不算前端路由；产物在运行期被移走时回 **404 而非 500**（`I23`）。专项验证 `verify:serve-web` 已接进 `verify:predeploy`（**项数现值 43** —— 本行为 09-18 所写，当时是 **28**；09-19 阶段 0 补了 15 项安全响应头断言后增至 43，数字以 `P-C5` 行与 `知识库.md` §9 为准）。载体 = **腾讯云 CloudBase 云托管**（`D4` 已拍板），步骤见 `tech/2026-09-17-云托管部署说明.md`。**未做**：真机部署 —— 已于 2026-09-19 执行（属 `P-C14`）；线上快照落后一事见 `I37` |
| P-C14 | **在线链接** | ✅ **已交付（2026-09-19）** | `https://weijifen-xueban-315516-12-1490356402.sh.run.tcloudbase.com/` —— 页面与 `/api` 同源可用；真实通道（`deepseek`）端到端冒烟 `HTTP 200` / 6.96 s，且如实标注 `verification: unverified` 与「未基于材料」。验收矩阵见 `changelogs/2026-09-19-C-在线链接验收.md`。**两条已知限制（必须写进交付说明）**：① 默认域名在浏览器直接访问时会先出现「访问提示中间页」（须点一次「确定访问」才进入），且官方明确「不建议用于正式生产或**分发给大规模用户**」；要消除它只能绑定**自定义域名**（须 ICP 备案，工期不够）→ 验收口径是「打开 → 点确定 → 可用」，**不是"点开即用"**。② **线上源码快照落后于 `main`**（`/nope.webmanifest`、`/assets/` 仍回退成首页）→ **需重新部署**，见 `I37`。另：会话在内存里，演示当天最小副本设 1，否则缩容会丢会话 |
| P-C15 | 第三方依赖与许可证声明核对 | ✅ | 新增 `npm run verify:licenses`（10 项）：声明↔安装↔README 表**双向**比对 + 许可证逐条比对 + **脚本命令的提供方是否已声明**。本轮据此修掉三处实错：`concurrently` 未声明、README 少列 6 个依赖、5 个 `package.json` 缺 `license` 字段（见 `I12`） |
| P-C16 | 仓库转 public + 替换占位姓名 | ✅ | **2026-09-17 18:00 转 public**，匿名态（无凭据）可读 README、可 `ls-remote`；姓名按负责人决定不填——`LICENSE` 用「微积分学伴团队」、`README` 用 A/B/C 角色代号 |
| P-C17 | 提交前敏感信息检查 | ✅ | 新增 `npm run verify:sensitive`（15 项）：**工作区被跟踪文件 + 待提交的未跟踪文件 + 全部历史 blob** 三处扫描（密钥特征 / PII / 个人邮箱 / 禁止入库路径 / `.gitignore` 覆盖 / `.env.example` 只许空值）。历史扫描刻意**不用 `git grep`**（POSIX ERE 不支持 `(?:…)`，会逼出两套规则、而"历史扫描悄悄失效"最危险），改用 `git cat-file --batch` 取 blob 在 JS 里扫。**首次运行即抓到我自己把队友邮箱写进了文档** |
| P-C18 | LearnBuddy 对话记录归档 | ⬜ | 与四类文档互相溯源；**不得事后补造**。导出需人工从 LearnBuddy 侧操作，不由脚本代劳 |
| P-C19 | 模型通道扩展的选型与实现（视觉 / 嵌入 / 语音） | ⬜ | **决策材料已出**：`tech/2026-09-17-托管载体与模型通道选型建议.md` §2（建议视觉复用已接入通道、嵌入不引、语音优先浏览器内置）。等 `D3` 拍板后实现 |
| P-C20 | 部署流程固定加入 `verify:dist` | ✅ | `npm run verify:predeploy` ＝ `build:clean` → `build` → `verify:dist` → **`verify:serve-web`** → `verify:repo`。**不靠人记得**：一条命令覆盖"构建成功≠可运行"、"部署上去是不是白屏"与"敏感项/声明/提示词快照"三类检查。第 8 轮；09-18 合并轮把 `verify:serve-web` 也接进来 |

### J · 三人共同

| 编号 | 事项 | 状态 | 验收 |
|---|---|---|---|
| J1 | 合并 PR：**#3** ✅ → **#6** ✅ → **#5** ✅ → **#4** ✅ | ✅ **已完成** | **#1–#7 七个 PR 全部 merged**（17:05）。`main` 头 = `a868d923`，已含 #3＋#6＋#5＋#4 |
| J2 | 合并后重跑 `verify:dist` + `verify:flow` | ✅ **已完成** | 在合并树上跑：`verify:all` **190**、`verify:flow` **32**（在构建产物上跑）、`verify:dist` **8**、`verify:render` **42**，全通过；**main 树 `ac35ce98` 与该验证树同 sha**。⚠️ 该行的 `dist 8`／`render 42` 是**当时**的数字，第 8 轮已增至 **11**／**68** —— **不要把这两处数字当成现值，"结论可沿用"仅指同 sha 的那棵树** |
| **J13** | 把 `feat/v2-contract-and-graph` 合并回 `main` | ✅ **已完成** | 新建 **PR #7**（集成分支 → main）并合并 → `main` = `a868d923`；`compare 50606d6...main` **差异文件 0**、树 sha 同为 `ac35ce98`，确认 main 已含 #5＋#6＋#4 |
| J3 | 确认提交物四件套齐备 | ✅ **四件套已齐（2026-09-19）** | 在线链接（`P-C14` ✅ 09-19 交付）＋ 源码仓库（`B2` 已 public）＋ LearnBuddy 记录（`P-C18`）＋ 可交互作品（即在线链接）。⚠️ "齐备"是**四件都有了**；**`P-C18` 的记录导出仍需人工做**，浏览器可用性验收见 `I8`，四件套逐项签字见 `J12` |
| J4 | 逐项核对「已宣传能力必须真实可演示」 | ⬜ | 依据 `specs/初赛补充说明` §3.4 状态基线 |
| J5 | 确认核心闭环可在一次演示中走通 | ⛔ **被 `B3` 卡住（第 6 步）** | 零材料提问 → 上传讲义 → 图谱构建 → 缺口发现 → 一键补充 → **符号验证（`B3` 零实现，当前走不通）** → 继续答疑 → 练习 → 画像更新。**处置口径：不跳过、不假装** —— 分镜 `P-A14` 已定为"如实演『未验证』"；`B3` 落地后再做本条的完整彩排（`J9`）。**这就是"当前唯一技术阻断项"在演示侧的表现** |
| J6 | ~~填入真实姓名并核对最新契约~~ | ✅ **已关闭（与 `I4` 冲突）** | 负责人已决定**不填真实姓名**（`I4` / `知识库.md` §11），本条原为「填入真实姓名」的残留待办，会诱导"顺手补上"。**保留此行仅为说明它为何不应再做**，不要当成待办认领 |
| J7 | 部署流程固定加入 `verify:dist` | ✅ **已完成（与 `P-C20` 是同一件事，见该行）** | 已由 `npm run verify:predeploy` 落地：`build:clean` → `build` → `verify:dist` → `verify:serve-web` → `verify:repo`。**不靠人记得**。⚠️ 本条长期与 `P-C20` 重复，2026-09-18 复核时按已完成处理 |
| J8 | 边界用例 **E8 / E10 / E11 / E13 / E16 / E18** 执行并留痕 | ⬜ | C 主测（会话、串扰、降级）；A 复核界面表现 |
| J9 | 演示彩排与计时 | ⬜ | 3 分钟内走完核心闭环；卡壳处即为要修的阻断项 |
| J10 | 提交回执核对 | ⬜ | 确认附件与版本；留存回执 |
| J11 | 向组委会确认「赛道二作品需开源」表述（同 D1） | ⬜ | 早问早踏实，避免按错误理解准备仓库 |
| J12 | 提交物四件套**逐项签字确认** | ⬜ | 在线链接 / 源码仓库 / 对话记录 / 可交互作品，四件缺一不可 |

---

## ❓ 待确认（Decision Needed）

| 编号 | 事项 | 影响 |
|---|---|---|
| D1 | ✅ **已关闭（2026-09-19 拍板）** —— 赛事手册「赛道二作品需开源」的表述与赛道设置不一致 | **不影响方向一的准备口径**（仓库已 public，`B2` 已解除）。⚠️ 本文档正文仍写"建议向组委会确认"——那是 09-17 的建议，**保留不改写**；该动作降级为 `J11` 的可选项，不再是决策项 |
| D2 | ✅ **已补写（2026-09-21）** —— `docs/designs/` 中方案母本已加「来源与定位说明」 | 该文件为 V2.0 的上游输入。已注明：它是**设计参考、不是本项目实现方案**，并逐条列出文中提到而**本项目未采用**的技术（LangGraph / GraphRAG / MinerU / Nougat / VLM / ASR 等）。⚠️ **具体出处（外部资料的名称/链接/作者）在项目记录中无留痕**，按"不得事后补造"**未代为填写**，待负责人补充（`L17`） |
| D3 | ✅ **已拍板（2026-09-19，"剩下按建议执行"）** —— 视觉 / 语音 / 嵌入通道的选型 | **不再是阻塞项**：**按建议执行** —— 视觉复用已接入通道、嵌入不引、语音优先浏览器内置（结论记在 `知识库.md` §14 的「`D3` 模型通道选型」行）。→ `P-C4`/`P-C19`（`L21`）**可开工**，下游解锁 `P-B5`（多模态解析，`L22`）→ `P-A7`（语音入口，`L23`）。⚠️ 本文档正文仍写"等拍板"，那是 09-19 之前的状态，保留不改写 |
| D4 | 在线链接的托管载体（Vercel / Netlify / 腾讯云 / 隧道） | ✅ **已拍板：腾讯云 CloudBase 云托管（持续运行模式）** —— 负责人 2026-09-18 决定。决策材料 §1 给的候选中**明确排除隧道（依赖本机开机 3 周）与 Serverless（本项目内存态装不进去）**；实现见 PR #9（`Dockerfile` + `scripts/pack-deploy.mjs` + `tech/2026-09-17-云托管部署说明.md`）。**随决定一并接受的限制**：默认域名有「访问提示中间页」、官方不建议分发给大规模用户 —— 处置写在该文档 §一与 `P-C14` 行。**执行结果（2026-09-19）**：已部署并验收，四件套齐备；另发现线上快照落后需重部署（`I37`）、以及一个待人工确认的 `content-disposition` 头 |
| **D5** | **客户端请求超时：取多少秒、放哪里**（计划书 `T-08` 的上游） | **阻塞阶段 0 卡 3**（见 `plans/2026-09-19-阶段0实施准备（可执行）.md` §1）。服务端 `MODEL_TIMEOUT_MS = 90_000` 是**单次模型调用**上限，而一次用户动作可能含多次调用 → 客户端超时必须**大于**服务端，否则"客户端先断、服务端还在烧额度"。**建议：120 s，先放前端常量并在 `知识库.md` §7 登记**（进契约常量属契约变更，需 C 流程 + A/B 确认） |
| **D6** | **阶段 0 的边界工具：装 `eslint`/`dependency-cruiser`，还是只上两个零依赖脚本** | **计划书 §14.1 内部冲突**：同节既要求"写 `dependency-cruiser` 规则文件 + `eslint.config.js`"，又写"❌ 不装新依赖"。**建议：阶段 0 只上 `check-file-size.mjs` + `check-import-boundary.mjs`（零依赖、只告警），两个依赖移到阶段 1**（那时有装依赖窗口且规则要转阻断） |
| **D7** | **登录与教师视图的形态**（＝计划书 `T-11`/`T-12`/`T-13`/`T-14`，含 `T-15` 口令注入方式的执行确认） | **阻塞阶段 V/W（登录与教师视图）**，且**冻结线 B（10-10）前必须定**。四项详情与建议默认见计划书 §18.8：登录方式（建议自建演示级）、账号是否持久化（建议内存态 + 环境变量注入种子账号）、是否"记住我"（建议不做）、教师视图形态 A 真数据 6–9 人日 / B 骨架+显著标注 3–4 人日（两种都**不得用假数据**，`C-3`） |
| **D8** | **登录 + 教师视图这 17–25 人日是否确认排期**（10-06→10-22 窗口） | 决定阶段 1（目录骨架）是否与"登录/教师的契约设计"并行（计划书 §1.7.2 推荐并行）。**未确认前不要把登录写进 README 的能力叙述** |
| **D9** | 计划书 `T-03`（是否作为模板被收录）/ `T-04`（决赛是否多学生并发）/ `T-05`（是否引外部可观测）/ `T-09`（云托管灰度能力）/ `T-10`（是否新增 `/api/telemetry`） | **均已给出建议默认，无异议即按默认执行，不阻塞任何阶段**：`T-03` 假设仅自用（不引 Changesets）；`T-04` 假设 3–5 人（限流列 P1）；`T-05` 不引外部可观测（只自建/零外发）；`T-09` 未核实 → 按"版本回滚"设计；`T-10` **不新增**端点（前端错误留控制台） |

---

> ✅ **2026-09-19 更新：上表 `D5`–`D9` 已全部拍板**（`D1` 一并关闭：仓库已 public）。
> 结论与逐项处置记在 **`知识库.md` §14 重大决策索引**（唯一汇总，勿在此重复抄写）。摘要：
> `D5` 客户端超时 **120 s**（前端常量）｜`D6` **装** `eslint` + `dependency-cruiser`（推翻"先只上零依赖脚本"的临时建议）｜
> `D7` **自建演示级账号 · 内存态 · 不做长效 cookie · 教师视图形态 A 真数据 · 口令不入仓库**｜`D8` **排期可调整**｜
> `D9` 其余默认假设照建议执行。

## 🧾 已登记断点（保留，不补造）

| 断点 | 说明 |
|---|---|
| 2026-09-16 三条代码提交 | **有 changelog、缺对应 plans 与 reviews**。按"不得事后补造"，空缺保留。若评审追问，以本条为准 |
| ~~**本地 `main` 落后于远端（2026-09-17 第 8 轮）**~~ ✅ **已收尾（2026-09-17 复查）** | 当时本环境的 git 提交链路被 ACL 拒写（`.git/index`／`COMMIT_EDITMSG`／`refs/heads/main`／`logs/**` 全被拒），改用**底层对象路径**提交并直接推对象：`git write-tree` → `git commit-tree` → `git push origin <sha>:refs/heads/main`；本地 `main` 一度停在 `1bbf262`。**现已前进到 `6bafa16`，工作区干净**，`git status` 不再有"落后造成的未提交文件"。ACL 绕过办法保留在 `知识库.md` §13.1，以备复发 |
| ~~**本地 `main` 再次落后于远端（2026-09-18 合并轮后）**~~ ✅ **已收尾（2026-09-20）** | 合并 #8/#9 时提交是在**服务端**造的（Git Data API），因此本地 `main` 仍是 `1957fc2`，远端已是 **`77c2e9ed`**；本地工作区另有 `scripts/check-sensitive.mjs` 一处已改（内容与 main **逐字节一致**）与 4 个未跟踪文件（其中 `scripts/pack-deploy.mjs`、`docs/reviews/2026-09-18-PR8与PR9-待合并审查.md` 的内容也已在 main 上）。**处置：在自带终端跑 `git fetch origin && git reset --hard origin/main`**；已实测**"本地未跟踪、远端已有"的文件不会让 `reset --hard` 失败**（直接覆盖，exit 0），且那几个文件与 main 逐字节一致，故该操作零损失。**这不是缺陷，只是本地未同步**。—— ✅ **2026-09-20 收尾**：`git fetch --no-write-fetch-head origin` → `git add -A` → `git update-ref refs/heads/main 3da6f4d` 三步跑通（**此前记录的"本环境写不了 `.git/index` 与 ref"已失效** —— `git add` 与 `git update-ref` 现在都能直接写）；现 `HEAD` = `origin/main` = **`3da6f4d`**、index **162** 条、`git status` 干净；另用临时索引干跑 `write-tree` 得 `30186176…`，与远端树哈希相等 → **工作区逐字节等于远端 `main`**。原建议的 `reset --hard` **已不需要**（"零损失"的判断仍成立，只是换了更稳的走法） |
| 前端浏览器视觉验证 | 环境不支持（`I8`）。记录为已知限制，不记为已完成。**09-20 复核：仍不成立** —— 无 `agent-browser`、不许 `npm install -g`；本环境只能做到"编译通过 ＋ 渲染断言"（`verify:render` **107 项**，且它**不替代视觉验证**）。**线上链接的浏览器实测仍待人工**，含 `content-disposition` 那个待判项 |
| 真实模型路径联调 | 本行写于 09-17：**当时**全部走 `MODEL_PROVIDER=mock`，真实 DS 通道未跑（无密钥）。**09-19 已补上一半**：线上实例（密钥只填平台环境变量）用真实 `deepseek` 通道跑通端到端冒烟 **`200` / 6.96 s**，如实标注 `verification: unverified` 与「未基于材料」（`changelogs/2026-09-19-C-在线链接验收.md`）。**仍未做**：本机 `.env` 下的真实通道联调 —— 仓库内只有 `.env.example`，"密钥不入仓库"是决定（`I4`/`D7`）而非遗漏 |
| ~~**⚠️ 全量复查轮的产出未提交（2026-09-17 缺陷修复轮发现）**~~ ✅ **已解决（2026-09-18 合入 main）** | 原状：全量复查（`reviews/2026-09-17-全量代码与文档复查.md`）**自己的工作区收尾结论不成立** —— 该文 §5.1 写"工作区现已完全干净"，但修复轮开工时实测 `git status --short` 有 **6 个路径**（5 个已改的文档 ＋ 该 review 文件本身），**即复查轮的文档订正与缺陷登记从未落进任何提交**。**处置**：修复轮把它们连同 `I13`–`I20` 的修复一起提交（`279c546` 补交文档订正、`1957fc2` 修缺陷），并随 **PR #8** 合并进 `main`（`3299d647`，最终 `main` = `77c2e9ed`）。**当前 `main` 已完整包含那 6 个路径，风险解除。** 保留本条以说明它曾经存在 |
| ~~**A 侧 `verify:render` 有一条断言偏弱（2026-09-18 审查发现）**~~ ✅ **已修（2026-09-19）** | 原状：断言"零材料时出题按钮被禁用"用的是**页面级宽正则** `/<button class="btn"[^>]*disabled/` —— 页面上**任意一个** `.btn` 被禁用都会让它通过。**处置**：改为**按按钮文案定位**（先取出所有 `<button>` 标签，找到含「开始练习」的那一个，再断它带 `disabled`），并加了说明注释防止有人改回宽匹配。 |
| ~~**I35 的"带 id 的 reason"仍无断言覆盖**~~ ✅ **已关闭（2026-09-21，`W0-7①`）** | `I35` 已把 `droppedBlocks.reasons` 改为学生话术（`toStudentReason()`），但"带 id 的那种 reason"在 **mock 通道上构造不出来**（mock 的回答恒能通过校验），因此改写逻辑只有"三类前缀 + 兜底"的代码审查，没有端到端断言。**如实登记，未造假断言**。构造办法（留给后续）：直接用 `validateAnswerBlocks` 造一条含未知 refId 的块，断言 `reason` 里**不出现 uuid** —— 这是纯函数层，可进 `verify:all` |
| ~~**`I14` 的服务端分支无端到端断言**~~ ✅ **已关闭（2026-09-21，`W0-7②`）** | mock 通道的回答**恒为单块**，构造不出"部分通过、部分被拒"的输入，因此 `droppedBlocks` 的**填充逻辑**只被一条否定断言覆盖（正常回答不带该字段），正面路径未自动断言。**如实登记，未造假断言**。渲染侧已由 `verify:render` 4 项覆盖。详见 `tech/2026-09-17-契约变更-TutorResponse-droppedBlocks.md` §5 |

---

## ✅ 最近完成（滚动窗口，仅供对照）

| 日期 | 事项 | 记录 |
|---|---|---|
| 2026-09-17 | 错误层自洽、入参守卫、可观测性收口（PR #2） | `changelogs/2026-09-17-C-PR2收口与错误层自洽.md` |
| 2026-09-17 | 契约迁移 V2.0 + 图谱落盘（PR #3） | `changelogs/2026-09-17-第6轮-C-契约迁移与图谱落盘.md` |
| 2026-09-17 | 学习工作台（PR #4） | `changelogs/2026-09-17-第6轮-A-学习工作台.md` |
| 2026-09-17 | 说明书 V2.0 对齐盘点、第 6 轮计划、两份技术记录 | `plans/2026-09-17-第6轮-V2.0全量对齐.md` |
| 2026-09-17 | docs 目录整理（→ `specs/`），新建知识库与待办 | `docs/README.md` §三 |
| 2026-09-17 | **解除 B1**：构建产物可运行 + 新增 `verify:dist`（PR #6） | `changelogs/2026-09-17-C-构建产物可运行-B1修复.md` |
| 2026-09-17 | 提交物四件套与验收口径澄清（补充说明 V1.0） | `specs/微积分学伴_初赛补充说明.md` |
| 2026-09-17 | A：工作台健壮性与真实性标注（忙碌态 bug / 重试 / mock 标记 / 语音入口 / 渲染冒烟 42 项） | `changelogs/2026-09-17-第6轮-A-工作台健壮性与真实性标注.md` |
| 2026-09-17 | **项目现状体检**：核对 main 与远端一致、重跑 190＋8＋42 项；确认 `B2`/`B3` 未解除 | `.learnbuddy/memory/2026-09-17.md`（本轮对话记录，非 `docs/` 四类文档） |
| 2026-09-17 | **todo 全量复查**：补挂 L33–L37（原泳道表漏挂）、新增 I9/I10/I11、更正 `B1` 残留引用与 `verify:flow` 计数 | 本文件 |
| 2026-09-17 | **文档全量整理**：README §核心机制改六态、§技术架构补到 10 接口、§当前状态按实况重写；知识库 §9/§10.2/§2.2 更正；`docs/README.md` §七 章节号归位；新增第 7 轮计划 | `plans/2026-09-17-第7轮-todo复查与A侧界面收口.md` |
| 2026-09-17 | **A 第 7 轮四项**：卡片↔图谱联动（`P-A8`）、界面验收清单 V1.0（`P-A9`）、窄屏与长文本防御（`P-A12`）、3 分钟分镜与旁白（`P-A14`） | `changelogs/2026-09-17-第7轮-todo复查与A侧界面收口.md` |
| 2026-09-17 | **解除 `B2`**：仓库转 public（转前做过全历史 38 提交的敏感扫描）；`I4` 按负责人决定关闭（不填真名） | 同上 §6 |
| 2026-09-17 | **第 8 轮 · P 类待办全量清理**：能做的一次做掉 8 项 —— `C17` 敏感检查 / `C15` 依赖与许可证 / `C12` health 构建自证 / `C20` 部署前检查固定化 / `B10` 参赛说明 / `B11` 提示词入库 / `A4` 图谱边渲染回归 / `D3·D4` 决策材料。新增 3 个仓库级检查脚本，回归 **288 → 334 项** | `plans/2026-09-17-第8轮-P类待办清理.md`、`changelogs/2026-09-17-第8轮-P类待办清理.md` |
| 2026-09-17 | **缺陷修复轮（I13–I20）**：修 `I13`（零材料提问被误判 403，唯一实测复现的用户可达缺陷）/ `I14`（被拒块静默丢弃）/ `I19`（四处死代码）/ `I20①⑥⑦`（三项真实性标注）；`I15`/`I16`/`I18` 按交接要求留给 `B3`，`I17` 作为待决策项上报。回归 **334 → 358 项**（`flow` 32→36、`render` 68→88） | `changelogs/2026-09-17-缺陷修复-I13至I20.md`、`reviews/2026-09-17-缺陷修复-I13至I20评审.md` |
| 2026-09-18 | **合并轮（PR #8 → PR #9）**：先合 #8；#9（同源托管 `P-C13`）解冲突后合入。合并前做了独立审查并落四处修正 —— ① `pack:deploy` 排除名单 `npm-cache` → **`.npm-cache`**（名字差一个点，实测会把 366 MB 缓存打进上传包，而自检仍打 ✅），补 `*.log` 与体积闸门；② SPA 回退**收紧到"只服务无扩展名的前端路径"**（原先 `/assets/缺失.js` 会返回 200 + 首页 HTML）；③ 部署说明补**默认域名「访问提示中间页」**这一限制；④ `check-sensitive.mjs` 的计数断言改为无条件（消除 `repo` 32/33 与合计 357/358 的漂移）。回归 **358 → 376 项**（新增 `verify:serve-web` 18 项）。`D4` 同时拍板为**腾讯云 CloudBase 云托管** | `changelogs/2026-09-18-合并PR8与PR9.md`、`reviews/2026-09-18-PR8与PR9-待合并审查.md` |
| 2026-09-18 | **todo 与文档复核轮**：先取实况再读文档，逐处核对后修正 —— ① `todo.md` 的 L 泳道表（"可立即开始"）有 **9 条早已完成的条目仍以待做形态挂着**（`L3`/`L7`/`L8`/`L10`–`L15`）、`L6` 的"静态托管"表述过时、`J7` 与 `P-C20` 重复、`I10` 未标闭合、§已登记断点里"全量复查轮产出未提交"**其实早已解决**；② `知识库.md` §2.2 交付状态（在线链接一行低报）与 §10.2 合并记录（只到 #7）；③ `README.md` 未实现表同一条低报。另修掉上一轮我在 `D4` 行**多写的一列**（16 张表逐表核对列数时抓到）。**本轮实测 5 个套件 365 项 0 失败**（`serve-web` 18 / `all` 190 / `flow` 36 / `render` 88 / `repo` 33），并用**真实前端产物**跑了同源托管的端到端复核 | `changelogs/2026-09-18-todo与文档复核.md` |
| 2026-09-19 | **I 类缺陷推进轮**：先验收 `P-C14` 在线链接（**四件套最后一件落地**：页面与 `/api` 同源、真实 `deepseek` 通道端到端 `200`/6.96 s），并**发现线上快照落后于 `main`**（`/nope.webmanifest`、`/assets/` 仍回退成首页）→ 新登记 `I37`。随后修 8 条 I 类：`I29`/`I31`（mock 保真：`mockTutor` 如实引用 `supplementIds`，零材料+已授权补充块 **403 → 200**）、`I30`（quiz 拒绝文案与判据口径一致）、`I17`（`health.version` 运行时读包版本，契约 `string → string \| null`）、`I36`（新登记：`basedOnMaterial` 误报"基于材料"）、`I21`（两处字面 `**`）、`I20②③④⑤`（上报来源取已加载题、修正失败即回滚、`shouldOfferRetry` 纯函数化）、`I32`/`I33`（「再来一组」抹掉作答、画像上报有无据声明）、`I34`（图谱节点状态增补"知识点自身引用"来源）、`I35`（丢弃原因改学生话术）。回归 **387 → 403**（`flow` 36→42、`render` 88→98） | `changelogs/2026-09-19-I类缺陷推进.md`、`changelogs/2026-09-19-C-在线链接验收.md` |
| 2026-09-19 | **拆分阶段 0 · 兜底与边界（代码项全部完成）**：按 `D5`/`D6` 拍板落地 —— 安全响应头 **0 → 7 条**（+HTTPS 才发 HSTS；CSP 用实测支持的更严版本，不需要 `unsafe-inline`；挂在注册路由之前并用**源码级断言**守住顺序）、`ErrorBoundary`（整页一层）、请求**超时 120 s + 可取消**（`describeAbort` 区分"超时/取消"）、sessionStorage **三处清理**、目录骨架 + `lib/`→`shared/lib/`（只改导入）、装 `eslint`+`dependency-cruiser` + 体积检查（**三条均只告警**）。回归 **403 → 425**（`serve-web` 28→43、`render` 98→104、`repo` 34→35） | `changelogs/2026-09-19-拆分阶段0-兜底与边界.md` |
| 2026-09-19 | **拆分计划实施方案**：按计划书 §14.1 与「下一步行动清单」第 2 步，出可执行方案 —— 九张实施卡（落点文件/改动/断言/验收/回滚/批次）+ 实测钉死计划书里四处"待实测"（内联脚本、`style={{}}`、外链资源全为 0 → CSP 可用更严版本）+ 点出计划书内部冲突一处（§14.1 要求写 `eslint`/`dependency-cruiser`，同节又写"不装新依赖"）并给出处置 | `plans/2026-09-19-阶段0实施准备（可执行）.md` |
| 2026-09-18 | **收尾修复轮（`I22`–`I35`）**：核实一份外部审查报告的 15 条 + 2 条贯穿性问题（**13 条成立、1 条不成立、1 条部分成立**），修掉 6 条 C 侧缺陷 —— ① `I22` 同源托管回退判定的**三个漏口**（未解码的 `req.path`／扩展名 `{1,8}` 上限／结尾斜杠，取不到的会拿 200 + 首页 HTML）；② `I23` 产物消失时 **500→404**；③ `I24` 把**恒真断言**换成真断言；④ `I25` `pack:deploy` 补**输出目录身份校验**（原先 `PACK_DEPLOY_OUT=仓库根` 会先 `rm -rf` 整个仓库）；⑤ `I26` 排除名单对齐 `.gitignore` 全集并**与自检同源**（原先 `.env.bak`/`secrets.json`/`*.pem` 会被打进上传包而自检仍打 ✅）；⑥ `I27` `warn()` 计入 `passed`，消除"照 `.env.example` 联调反而让计数少 1"。另按远端 main 原文补齐半落地合并缺的 3 个文件与 2 处脚本项（`I28`，**149/149 逐字节一致**）；`I29`/`I30` 复现后作为**待决策**上报，`I31` 为核实中新登记的 mock 保真度缺口，`I32`–`I35` 转 A。回归 **376 → 387 项**（`serve-web` 18→28、`repo` 33→34） | `changelogs/2026-09-18-收尾修复轮-I22至I27.md`、`reviews/2026-09-18-待核实发现-核实报告.md` |
| 2026-09-20 | **状态核对与剩余项重排轮**：① 本地 `main` = `origin/main` = index 三者对齐到 **`3da6f4d`**（临时索引干跑 `write-tree` 得 `30186176…` = 远端树哈希 → 工作区逐字节等于 `main`；**此前"本环境写不了 `.git/index` 与 ref"的结论失效**）；② `npm run verify:predeploy` **全绿**（`dist` 11 / `serve-web` 43 / `repo` 35，退出码 0）；③ `I37` **上传包已打出并逐条核对**（`D:\smallbox\Projects\LearnBuddy-deploy-0920`，163 文件 / 2.01 MB / 自检 ✅；旧 `deploy-dist` 工具侧删不掉 → 改用 `PACK_DEPLOY_OUT` 打新目录）；④ 逐条核对实况后改正 `I9`（仍剩 README 行 207 一处对外失真）/`I11`/`I17`/`P-C13`/`J3`/`J5` 与两条已登记断点，新登记 `I40`（lint 残留 3 warning）；⑤ 新增「📌 当前剩余总览」一节，把未完成项按"谁现在能动手"重排 | 本文件、`plans/2026-09-20-I37重部署前置检查清单.md` |
| 2026-09-20 | **计划落地轮 · 第一批 `W0`（4 项）**：按《后续全量推进（按阻断关系）》第 0 层动工 —— ① `W0-1`/`I9`：README 行 207「尚未部署」**删除**，改为「已实现」里的在线链接条目 + 三条真实限制（访问提示中间页 / 线上快照落后于 `main` / `content-disposition` 待人工判定）；② `W0-2`/`I40`：删 3 处未使用绑定，`lint` **0 error / 3 warning → 0 / 0**；③ `W0-5`/`I38`：`ModelAdapter` 契约下沉到叶子模块 `model/adapter.ts` 拆掉环，`no-circular` **先验红再转绿**并**逐条转 `error`**（`lint:boundaries` **7 warn → 6 warn / 0 error**）；④ `W0-4`/`I1`：`SYSTEM_KNOWLEDGE` 补 `graph.edges` ＋ 快照文档逐字同步 ＋ **mock 通道产出边** ＋ 4 条新断言（含把"此前恒真"那处补成正面路径）。另修 `GraphPanel` 的**静默丢边**，新登记 `I42`。**订正了配套文档两处不能照做之处**（应为 `parsed.graph.edges`，不是顶层 `edges`；边方向应为 `单调性 → 导数`）。回归 **425 → 433**（`all` 190→194、`flow` 42→43、`render` 104→107），`verify:predeploy` 退出码 **0** | `changelogs/2026-09-20-计划落地-第一批W0项.md` |
| 2026-09-20 | **`B3` 符号验证落地轮（`W1-1`，唯一的"技术阻断"解除了）**：① 装 `mathjs@15.2.0` 并在 `packages/teaching/package.json` 声明（⚠️ **锁文件未能同步**：本机 npm 写不了已有文件，三条通道全被拦 → 并入 `I39`）；② 新 `symbolic.ts`（四类断言逐条核验：符号求导+数值求值 / **`rationalize` 系数判定**代替 `simplify` / 区间 3 点取样 + 分界点 / 一阶变号）+ `latex.ts`（白名单，**该通的通、该拦的拦**）；③ 提示词改为**结构化断言**并同批同步快照文档；④ 接线：`tasks.ts` → `routes.ts`（`/api/gap` 状态由引擎推导）→ mock（**不再让无密钥路径永远看不到"已验证"**）；⑤ 一并收口 `I5`/`I16`/`I18`/`P-B13`，`I15` **部分收口**（反查在现管线里落空，根因并入 `I42`）；⑥ 同批更新 README / 参赛说明 / 界面文案的如实标注。回归 **433 → 506**（新增 `verify:symbolic` 69 项；`flow` 43→46、`repo` 35→36），`verify:predeploy` 退出码 **0**。新登记 `I43`（体积基线 5→7） | `changelogs/2026-09-20-B3符号验证落地.md` |
| 2026-09-21 | **远端审查 + 状态刷新轮**：① `ls-remote` 核实远端 `main` 已前进到 **`72ae5ba`**（= **PR #12 合并提交**，2026-09-20 23:38 +08；13 个 PR 全部 closed、0 个 open）—— 合并**无损**（PR head 树 ≡ 合并后树，逐字节）；② 本地 `git fetch` + **`reset --hard origin/main` 一次通过**（此前"本机写不了 `.git/index` 与 ref"的结论再次失效）→ `HEAD` = `main` = `origin/main` = `72ae5ba`，`git status` 仅余一份本地 review 文档；③ 用**树哈希**证明本地工作区原本就等于远端 `main` ＋ 1 份未提交文档（冲突为零；此前 `git status` 的 37 条"改动"是索引陈旧的假象，内容零丢失）；④ 修正**对外失真** —— `App.tsx` 页脚把已接入的「符号验证引擎」仍列为"尚未接入"（该失真已随 #12 上线到 `main`），并在《界面验收清单》**追加 V1.1 修订节**（遵守该文件"只追加不改写"的规则）；⑤ 刷新 `todo.md` 头部 / `L6` / `I37` / 剩余总览里"上传包已就绪"的过时说法；⑥ **`W0-7①②` 落地**：把 `toStudentReason` / `buildDroppedBlocks` 提到无副作用叶子模块 `http/student-reason.ts`（**有意识的接口改动**），`verify:guards` 补 **7 项**断言（**81 → 88**），两条"已登记断点"（`I35` 带 id 的 reason / `I14` 的 `droppedBlocks` 正面路径）**关闭**；⑦ **部署包重打** → `…-deploy-0921b`（**175 文件 / 2.25 MB / 自检 ✅**）；回归 **506 → 513** | `changelogs/2026-09-21-远端审查与W0-7断言补齐.md`、本文件、`specs/微积分学伴_界面验收清单.md` §8、`.learnbuddy/audit-0921/` |
| 2026-09-21 | **视觉接入落地 + 线上重部署 + 真实通道识图实测**：PR #14（`eb06ab6`）并入 → 在仓库外重打部署包并发布为**第 005 版**（部署后核对环境变量与实例数未被改动）→ **线上 `POST /api/parse` 实测 `200` / 2.07 s**（转写与公式定位正确，`lowConfidence` 为空）→ `content-disposition` **定案为平台网关行为**；同批修正 6 份文档的对外失真 + 1 处代码页脚 + 分镜 V2 修订节，并新建变更记录 | `changelogs/2026-09-21-视觉接入与在线重部署.md` |
