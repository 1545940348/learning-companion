# 待办（**只列没做的**）

> **已完成条目与全部历史演进**（哪一轮改了什么、当时的数字是多少）归档在
> **`archive/todo-2026-09-full.md`** —— 那是原 `todo.md` 的**全文存档**（604 行）。
> ⚠️ **归档不是删除**：其它文档里「见 `I43`」「见 `P-C5`」这类**按编号的引用仍然有效**，去归档里按编号搜。
> ⚠️ **本文件不再写"历史演进"**：一条做完就划掉，过程写进**当轮 changelog**。

**四条规矩（沿用）**

1. **未实现的能力必须如实标注** —— 不用空数据、模拟行为或"听起来合理"的话冒充已实现；
2. 每条给出**可执行的验收**（做到什么算完），不写"优化一下"这种没法判的话；
3. **改完保留条目**作为追溯 —— 从现在起保留的位置是归档文件；
4. `todo.md` / `知识库.md` 由 C 汇总落笔，他人通过对话或 PR 评论提改动。

**相关入口**：执行顺序 `plans/2026-09-20-后续全量推进（按阻断关系）.md`｜
拆分逐阶段工时与验收 `plans/2026-09-18-单页面Web应用拆分计划书.md` §14｜
界面判据 `specs/微积分学伴_界面验收清单.md`（含 §12／§13 的 V1.x 修订）｜
测试材料 `fixtures/materials/`（10 份 ＋ 清单 ＋ 加载器）。

---

## 一、只有人工能做（机器做不到，且卡着交付）

| # | 事项 | 卡在哪 |
|---|---|---|
| 1 | ~~**线上链接实测**：点开是「打开页面」还是「下载文件」（`content-disposition`）~~ ✅ **2026-09-25 已完成**：真实浏览器实测 ⇒ **是「打开页面」，不是下载**（该响应头是云托管网关行为，不触发下载） | 已闭合 |
| 2 | ~~**话筒输入的真机识别**：点一下是否真能转出文字~~ ✅ **2026-09-26 已由人工在演示机实测**（此前"连弹窗都不弹、直接 `not-allowed`"的部署问题已不复现） | 已闭合；⚠️ **识别质量未量化**，录屏前建议再试一次（见第 4 条） |
| 3 | **真实设备观感**：触屏、滚动惯性、字体回退、**动效手感** | 截图看不出手感（时长、节奏、按下反馈） |
| 4 | **3 分钟录屏**（`P-A10` 第 2 条；脚本已就绪 ＝ `P-A14`） | 录制前先在演示机验一次语音与在线链接 |
| 5 | **`J10`** 提交回执核对、**`J12`** 四件套逐项签字 | 人工 |
| 6 | **演示当天把 `MinNum` 调为 1** | 服务端内存态，缩容会丢会话 |
| 7 | **`J11`** 向组委会确认「赛道二作品需开源」的表述 | `D1` 已按「仓库已 public」关闭，但表述本身未被官方澄清 |
| 8 | 🆕 ⚠️ **提交物清单口径不一致**（2026-09-26 登记）：`specs/大湾区AI_Coding创新大赛_赛事手册.md` **§4.1** 标 ✓ 必需的共 **5 项**（作品链接 / **Demo 视频** / **介绍 PPT** / 源代码仓库 / LearnBuddy 使用记录），而 `specs/微积分学伴_初赛补充说明.md` **§02** 只列 **4 项**（在线链接 / 源码仓库 / 对话记录 / 可交互作品）——**缺「视频」与「PPT」**。手册中**只有 §4.1 这一处**提交清单（`grep 四类\|四件` 无别的出处） | **需人工在提交平台上确认实际有几个上传位**；在澄清前**按并集（5 项）准备**。否则可能漏交视频与 PPT |

## 二、可以马上做（零外部依赖）

| 编号 | 事项 | 关键点 |
|---|---|---|
| **`I43`** | 拆 **9** 个超 300 行文件 | ⚠️ **A 线结构冻结线 09-25 ⇒ 只剩 2 天**：只按「最小可交付」切，不开新结构面。📌 **2026-09-24 代改（待 C 复核）**：由 7 → **9**（`routes.ts` 1234 / `useWorkbench.ts` 1204 / `symbolic.ts` 852 / `tasks.ts` 744 / `store/index.ts` 514 / `model/index.ts` 402 / `workbench-types.ts` 354 / `GraphPanel.tsx` 332 / `api.ts` 326），扫描 77 个源码文件。⚠️ 该脚本**只扫 `apps/` 与 `packages/`**，`fixtures/` 与 `scripts/` **不在范围内** |
| **`P-C5`** | 回归脚本扩展（**持续性**：每次改断言／加检查都要同步计数） | 📌 **2026-09-24 代改（B 执行，待 C 复核）**。**现值：`all` 412 ＋ `flow` 113 ＋ `dist` 11 ＋ `render` 241 ＋ `voice` 68 ＋ `serve-web` 45 ＋ `repo` 37 ⇒ 合计 927**（逐套件本机实测）。⚠️ 数字要**四处同改**：`README` 计数段／`知识库.md` §9／本文件／当轮 changelog。历史演进（从 137 长到 927 的每一步）见归档的 `P-C5` 行。⚠️ **另注**：`verify:flow` 须连**与源码同版的**服务端，否则会以 `TypeError` 崩掉、**看起来像改坏了**（详见 `知识库.md` §9 的 ⚠️ 段） |
| **`P-A15`** | 前端冒烟脚本（**持续性**：随改动补断言） | **现值 `verify:render` 241 项 ＋ `verify:voice` 68 项**（2026-09-24 实测）。⚠️ 它的定位是「渲染不抛异常 ＋ 关键文案在 HTML 里」，**不替代人眼对观感的判断** |
| **`P-B2`** | 失败修正回环 | ✅ **已完成**（2026-09-23，**PR #26**，merge `ab2cb9fe`）；`symbolic` 69 → 81。补记见 `changelogs/2026-09-23-B-教学能力补全（P-B9＋P-B8＋P-B15）.md` §7.2 |
| **`P-B12`** | 图谱归一补测：**别名合并** | ✅ **已完成**（2026-09-23，**PR #25**，merge `7ef97c7a`）；`graph` 67 → 77。补记见同上 §7.1 |
| **`P-B7`** ＋ **`P-B14`** | 教学与边界测试记录（12 问／9 题／E1–E7 等） | 🟡 **PR #28 开着未合并**（`docs/b7-b14-records`，head `f351bd92`，2 文件 +310）。两条同批做更省事；数学结论要**人工核验 ＋ 符号验证双重通过** |
| **`P-B15`** | 教学素材 9 份（3 主题 × 文字／图片／语音） | ✅ **已完成**（2026-09-23~24，B 线）；`fixtures/teaching-materials/`，新增 `verify:teaching-materials` **27 项**（离线，进 `verify:all`）。单调性素材**保留**导数前置缺口（**有硬检查 ＋ 已做负向对照**）。见 `changelogs/…-B-教学能力补全…md` §五 |
| **`P-B8`**／**`P-B9`** | 多智能体降级／错题归因（P1） | ✅ **均已完成**（2026-09-23~24，B 线）。`P-B8`：`orchestrate.ts` ＋ `degraded[]` 如实回传；`P-B9`：`attribution.ts` ＋ `POST /api/quiz/attempt`（**接口数 11 → 12**），归因**必带可核对证据**。⛔ 归因层 ③「诊断 Agent」与「数值倍数推导」**被否决**（理由见 `reviews/2026-09-24-B-教学能力补全复核.md` §二 R2／R3） |
| **`P-B17`** | 变式题（按画像弱点生成 1–2 道） | ✅ **已完成**（2026-09-23，**PR #27**，merge `4d242035`）；`flow` 86 → 95、`render` 215 → 220。补记见同上 §7.3。⚠️ 顺带登记 `P-B18`（mock 题面硬编码前缀与界面说法不一致，**未修**） |
| **`J8`** | 边界用例 E8／E10／E11／E13／E16／E18 执行并留痕 | 多为会话与错误相关，多数可立即做 |
| **`P-C4`**／**`P-C19`** | 模型通道扩展（视觉／嵌入／语音） | `D3` 已拍板 ⇒ **可开工**；下游 `P-B5`／`P-A7` 等它 |
| **`P-C11`** | `/api/teacher` 班级聚合 | ✅ **已完成**（2026-09-23，由 501 改为实现）；只读 ＋ 脱敏（E17）；样本不足**显式提示**；缺 `classId` → 400。⚠️ 该接口**不再是 501**，`README.md` 与 `知识库.md` §5／§8.2 的旧记载已同批订正 |
| **`I39`** | 锁文件缺全平台可选依赖 | **当前不阻断**；根治 ＝ 重生成含全平台可选依赖的锁文件 |
| **`P-A6`** | 教师视图界面 | ✅ **已完成**（2026-09-23，`TeacherPanel` ＋ 左栏入口）；无数据前不渲染。见 `apps/web/src/components/TeacherPanel.tsx` |

## 三、结构性工作（未开工）

> 依据 `plans/2026-09-18-单页面Web应用拆分计划书.md` §14。
> **冻结线**：A 线 **09-25→09-26** 只修缺陷、不动结构；B 线 **10-10** 起停止结构改动。

| 阶段 | 内容 | 负责人 |
|---|---|---|
| 1 | 装配层与目录骨架（`App.tsx` 拆分、面板入 `widgets/`、门面、别名、规则转阻断） | A |
| 2 | `entities` 切片 ＋ 引入 vitest ＋ 19 条单测 | A |
| 3 | `features` 切片 ＋ 13 条单测；`verify:flow` 扩为完整学生动线 | A |
| 4 | 接线与**门面删除**（硬性终态：`grep useWorkbench` 为 0、任一文件 ≤300 行） | A |
| 5 | 样式分片与设计系统 | A |
| 6 | 受限抽包（`packages/ui` ＋ `packages/api-client`） | A |
| 7 | 路由化（`/login`、`/teacher`）—— **须排在登录实现之前** | A |
| 8 | 质量基建：CI、E2E／视觉／可访问性、体积门禁、SBOM | C＋A |
| L | 契约阶段（接口 10 → 约 14、角色、错误码）—— **必须先于登录** | C |
| V | 登录实现（自建演示级、内存态、不做长效 cookie；归属校验 ＋ CSRF 同批上线） | C＋A |
| W | 教师视图形态 A（真数据；**不得出现未标注来源的数字**） | A |

⚠️ **开工前先重估阶段 1**：它原本要消的 `panels-should-not-import-state-internals` **已于 09-23 清零并转 `error`**。
`I38` 的教训是「搬路径会让规则两端同时失效，报 0 条 ≠ 耦合消失」—— 所以**"0 条 warn"不能再单独当验收**。
阶段 4 的量化终态仍有价值（`I43` 的 **9** 个超限文件就等在那儿）。

## 四、要拍板的（Decision Needed）

| 编号 | 事项 | 影响 |
|---|---|---|
| **`D5`** | 客户端请求超时取多少秒、放哪里 | 建议 **120 s**（须大于服务端 `MODEL_TIMEOUT_MS = 90 s`），先放前端常量并在 `知识库.md` §7 登记 |
| **`D6`** | 阶段 0 的边界工具：装依赖还是只用零依赖脚本 | 计划书 §14.1 内部冲突；**实际已按"零依赖脚本先行"执行**，可据此关闭 |
| **`D7`** | 登录与教师视图的形态（含口令注入方式） | **阻塞阶段 V／W**；**冻结线 B（10-10）前必须定** |
| 🆕 | **视觉走查要不要「转正」**（把 `playwright-core` 写进 `devDependencies`、接进 `verify` 链） | 现在它是**本机工具** ⇒ 别人 clone 跑不了；转正后 `I8` 才算"有回归"，代价是「clone 即能跑」的前提要重新论证 |
| 🆕 | **E2E 用例集要不要做**（上传材料 → 知识点 → 补缺口 → 提问 → 练习 → 画像） | 原被 `I8` 挡着，**现已可做**（有 Chromium 就能跑），材料已备好 |
| 🆕 | **P2-2 仍缺的三项**（材料读接口／服务端持久化／画像读接口） | **都要动契约**；建议**先不做**，等评审／演示反馈 |
| 🆕 | **favicon** | 走查会报一条 404（无害）；属**产品决定**，不是缺陷 |

## 五、未闭合的缺陷

| 编号 | 事项 | 状态 |
|---|---|---|
| **`I15`** ＋ **`I42`** | 前置概念**要不要**进 `SessionGraph.nodes` —— 一处决定牵动 `I15`／`I34`／`I42` 三件 | 🟡 `I15` 已退回 `conceptId`（如实、不编造）；**待 B／C 拍板** |
| **`I2`**／`P-C9` | 契约冗余：`ProfileEvent.sessionId`（请求侧每个事件都要带，服务端却会覆盖它） | ⬜ 契约变更 ⇒ **改完 A 必须同步** |
| **`I3`**／`P-C10` | 契约缺材料「替换」语义 ⇒ 用例 E12「就地纠错」无法完整实现 | ⬜ 同上 |
| **`I7`** | 教师视图**曾**未实现（如实 `501`，界面不渲染该面板） | ✅ **已消解**（2026-09-23）：`/api/teacher` 由 501 改为实现（`P-C11`），界面侧 `P-A6` 已接 `TeacherPanel`。📌 **2026-09-24 代改（待 C 复核）**：原文写"未实现"，与代码不符；**事项列也一并改成"曾未实现"** —— 否则一行里"事项说没有、状态说已消解"，**读的人会先信左边那半句**。`NOT_IMPLEMENTED`（501）**仍留在错误码表里**，只是当前无路径在用 |
| **`I8`** | 浏览器视觉验证 —— **能力已解除（部分）** | ⚠️ **仍剩三项**：话筒真机、真实设备观感、线上链接实测（见 §一） |
| **`I39`** | 锁文件缺全平台可选依赖 | ⬜ 见 §二（当前不阻断） |
| **`I42`** | 图谱边的端点可以不是节点（界面**已如实说明**、不静默丢边） | 🟡 见 `I15` |
| **`I43`** | **9** 个文件超 300 行 | ⬜ 见 §二。📌 **2026-09-24 代改（待 C 复核）**：此行原写 **7**，与 §二 的 **9** **自相矛盾** —— 同一份文件里的两处对同一个数字给出两个值，**这正是 §二 那条「数字要四处同改」在防的事**，本轮补上（7→9 的来源见 `changelogs/…-B-教学能力补全…md` §10.6：**不是本轮推上去的**，两个文件在 HEAD 就已超线） |
| **`I44`**（📌 **新登记 2026-09-24，编号待 C 定**） | **`verify:flow` 探不出"服务端是旧的"，会以崩溃的形式假报"改坏了"** | ⬜ **未修**。实况：`verify:flow` **不自己起服务端**，连的是本机已监听的实例；旧版服务端**不报错**，照常回 `ok:true`（健康探针只看 `/api/health`）⇒ 新接口在旧进程上返回 404，脚本崩在 `Cannot read properties of undefined`，**看起来像代码回归，实际是连错了机器**。2026-09-24 实际踩到一次（旧进程残留在 3000/3112）。**建议改法（未实施）**：比对 `build.builtAt` 或直接探一次新接口，命中"旧"就报「**服务端是旧的，请重启**」而不是抛出。⚠️ 未修 ≠ 不阻断 —— **先起与源码同版的服务端**是本轮全部 `flow` 数字的前提条件。📌 **2026-09-24 已清场**：三个残留 mock 进程（`3000`／`3112` 来自 09-23 会话，`3131` 为本轮临时起的）**已全部结束，端口探测均为 down** —— 但**这是手动清场，不是修复**：下次仍会留下旧进程，脚本**仍探不出来** |

**已闭合的编号（去归档里按编号搜）**：
`B1`／`B2`／`B3`｜`I1`／`I4`–`I6`／`I9`–`I14`／`I16`–`I38`／`I40`／`I41`｜
`P-A1`–`P-A5`／`P-A7`／`P-A8`–`P-A14`／`P-A16`（**`P-A15` 见 §二，它是持续的**）｜
`P-B1`／`P-B3`／`P-B4`／`P-B6`／`P-B10`／`P-B11`／`P-B13`／`P-B16`｜
`P-C1`–`P-C3`／`P-C7`／`P-C12`–`P-C17`／**`P-C18`**（2026-09-26，见 §六）／`P-C20`（**`P-C5` 见 §二**）｜
`J1`–`J3`／`J6`／`J7`／`J13`｜`D1`–`D4`

## 六、计划项剩余

| 编号 | 事项 | 状态 |
|---|---|---|
| `P-A10` | 演示材料：录屏（脚本已就绪） | 🟡 见 §一 第 4 条 |
| `P-B5` | 多模态解析（文字／公式／图像／语音 ＋ 低置信度标记） | ⬜ 图像已交付、语音在浏览器侧；**服务端不转写**是既定边界 |
| `P-C6` | 联调与发布（组合教学模块、页面、验证引擎） | ⬜ 等上述大部分完成 |
| `P-C8` | 材料与提交：PPT、源码归档、启动说明、回执 | ⬜ 按时提交并核查回执 |
| `P-C18` | LearnBuddy 对话记录归档 | ✅ **已完成**（2026-09-26）→ `docs/records/`（md ＋ 提交版 PDF ＋ 原始文件校验附录）。⚠️ **与旧口径的差异须留痕**：原定"**人工**从 LearnBuddy 侧导出、不由脚本代劳"，实际改为**脚本机械转换**（`scripts/export-learnbuddy-records.mjs`）。理由与核验方式见 `records/README.md` §二／§五，以及 `changelogs/2026-09-26-C-LearnBuddy对话记录归档.md` |
| `J4` | 逐项核对「已宣传能力必须真实可演示」 | ⬜ 依据 `specs/微积分学伴_初赛补充说明.md` §3.4 |
| `J5` | 核心闭环可在一次演示中走通 | 🟡 `B3` 已落地 ⇒ **不再被卡**；剩真实通道与完整彩排 |
| `J9` | 演示彩排与计时 | ⬜ 3 分钟内走完核心闭环；卡壳处即为要修的阻断项 |
| `J10`／`J11`／`J12` | 回执 ／ 组委会问题 ／ 四件套签字 | ⬜ 见 §一 |

---

**归档**：`archive/todo-2026-09-full.md` —— 原 `todo.md` 全文（604 行）：每条的历史演进、已闭合项、
第 7 轮分配记录、并行泳道表（L1–L37）、已登记断点、以及「最近完成」滚动窗口。**按编号搜。**
