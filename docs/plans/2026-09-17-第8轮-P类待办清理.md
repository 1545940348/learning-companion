# 第 8 轮 · P 类待办全量清理（能做的一次做掉）

> 撰写日 2026-09-17 ｜ 范围依据：负责人指令「**所有 P 类 todo，能做的都做**」
> 待办编号见 [`../todo.md`](../todo.md)，长期约定见 [`../知识库.md`](../知识库.md)
>
> ⚠️ **如实披露一处流程偏差**：按纪律，计划应"每轮开始前"写。
> 本轮的分类与实施在**同一会话内连续完成**，计划未先于动工 —— 因此它记录的是
> **本轮实际采用的范围划分**，不是动工前的承诺。若评审追问，以本条为准，不回填时间。

## 1 先把"能做"的边界划出来

P 类共 **53 条**（P-A 16 / P-B 17 / P-C 20）。逐条判定后：**可做 8 条、被阻塞 20 条**，
其余为已完成（11 条 A 侧 + 3 条 C 侧）。

**判定口径（三条同时满足才算"能做"）**：
1. 不依赖尚未决定的决策（`D3` / `D4`）；
2. 不依赖尚未实现的能力（符号验证引擎、多模态通道、教师视图数据模型）；
3. **不越界改他人负责的实现代码** —— `packages/teaching/**` 的实现属 B，
   本轮只做**不碰其代码**的部分（文档类），实现类一律留回 B。

## 2 本轮范围

| # | 项 | 类型 | 产出 |
|---|---|---|---|
| 1 | `P-C17` 提交前敏感信息检查 | 脚本 | `scripts/check-sensitive.mjs` + `verify:sensitive` |
| 2 | `P-C15` 依赖与许可证核对 | 脚本 + 修错 | `scripts/check-licenses.mjs` + `verify:licenses`，并修掉 `I12` |
| 3 | `P-C20` 部署前检查固定化 | 脚本 | `verify:predeploy` ＝ build → dist → repo |
| 4 | `P-B11` 提示词与工作流配置入库 | 文档 + 防漂移脚本 | `tech/2026-09-17-B-提示词与工作流配置.md` + `verify:prompts` |
| 5 | `P-B10` 参赛说明 | 文档 | `specs/微积分学伴_参赛说明.md` |
| 6 | `P-C12` health 增加构建信息 | 契约 + 代码 + 断言 | `HealthResponse.build` + `config/build-info.ts` + 3 项产物断言 |
| 7 | `P-A4` 图谱边渲染回归 | 断言 | `verify:render` 新增 10 项（42 → 68） |
| 8 | `D3`/`D4` 决策材料 | 文档 | `tech/2026-09-17-托管载体与模型通道选型建议.md` |

## 3 明确不做（连同理由）

| 不做的项 | 卡在哪 | 为什么不在本轮硬做 |
|---|---|---|
| `P-B1`/`B13`/`B16`（符号验证三件套） | 需要实现 `symbolic.ts` + 引入 `mathjs` | **属 B 的核心实现**，且 `B13` 的 LaTeX 白名单需要在引擎上验证才算数；替 B 写会抢掉他的设计决定，还会与他可能在飞的工作冲突 |
| `P-C13`/`C14`/`A16`/`A1`（部署与在线链接） | `D4` 未定 | 实现方式**取决于载体**（长驻服务 vs Serverless）；先做会做一半推翻 |
| `P-C4`/`C19`（通道扩展） | `D3` 未定 | 同上；本轮只把选型建议交出去 |
| `P-C9`/`C10`（契约细化） | 见 `todo.md` L18/L19 | 待办本身就写着"改完 A 必须同步，建议排在 L1/L2 之后"，即**建议在 B 侧交付后再动** |
| `P-B5`/`B15`（多模态、教学素材） | 无视觉/语音生成能力；`B5` 依赖 `C19` | 语音素材环境无法生成；图片生成需消耗额度，且 `B5` 的验收是"6 份素材人工比对"，自动化不了 |
| `P-B7`/`B14`（测试记录） | 属 B 主测 | mock 未必能真实触发 E1–E7 的判定路径，**由我代跑后写"已执行"会失真** |
| `P-A10`（录屏） | 需要可运行的线上实例 | 分镜脚本已完成（`P-A14`），录屏必须等在线链接 |
| `P-A6`/`C11`（教师视图） | 无班级数据模型 | `I7` 明确"无数据前不渲染"，实现在阶段三 |
| `P-B8`/`B9`/`B17`（P1 三项） | 第 7 轮已明确不排入 | 排期顺序问题，不是能力问题 |
| `P-C6`/`C8`/`C18`（联调、提交材料、对话归档） | 尾期 / 需人工操作 | `C18` 的对话记录需从 LearnBuddy 侧导出，**代劳等于补造** |

## 4 验收条件

- 三个新脚本**首次运行就能查出真问题**（不是只输出"全部通过"的空壳）；
- `verify:prompts` 能证明提示词快照与代码逐字一致；
- `verify:dist` 能断言**产物态自报 `dist`**，且 health **不泄漏本机绝对路径**；
- `verify:render` 能覆盖"有边"与"无边"两种图谱状态的渲染，且**两种状态不会同时出现**；
- `typecheck` 4 workspace、`verify:all` 190、`verify:flow` 32、`verify:dist` 11、
  `verify:render` 68、`verify:repo` 33 全部通过；
- 全部改动写进 changelog，被否决/被修改的判断写进 reviews。

## 5 允许修改的目录

| 目录 | 允许 | 说明 |
|---|---|---|
| `scripts/**`（仓库根） | ✅ 新建 | 仓库级检查脚本 |
| `packages/contracts/**` | ✅ **仅加性变更** | 只加 `HealthResponse.build`（可选字段），不改已有字段语义 |
| `apps/server/src/**` | ✅ | 新增 `config/build-info.ts`、health 引用 |
| `apps/server/scripts/**` | ✅ | `verify-dist.mjs` 加断言 |
| `apps/web/scripts/**` | ✅ | `verify-render.tsx` 加断言 |
| `package.json`（根） | ✅ | 脚本与 `devDependencies` |
| `docs/**`、`README.md` | ✅ | 本轮留痕与文档同步 |
| `packages/teaching/src/**` | ⛔ **不改** | 属 B；本轮只引用其提示词生成快照文档 |
| `docs/specs/**` 既有文件 | ⛔ **不改写** | 只**新增** `微积分学伴_参赛说明.md` |
