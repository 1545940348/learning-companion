# 本轮计划：方案V1.4，DS作为默认推理通道

- 授权：负责人本轮要求再次修改方案书，直接使用DS，并反馈平台调用超时；此前禁用DS的要求由本轮选型指令替代。
- 对照：V1.3的“不得接入DeepSeek API”与新要求相悖，本轮正是负责人授权修订该条。赛事手册不改写；历史转述“最好不要包含第三方AI”不视为已经获得许可。
- 展示处理：学生界面不常驻模型品牌；技术文档、启动日志、health仍准确报告，不将DS伪称为平台推理。mock状态仍必须可辨认。
- 范围：仅docs中的方案正本、相关决策状态及四类记录；不改代码／密钥，不实测收费接口，不commit/push，不同步旧Word。
- 步骤：更新模型选型、C任务、排期与验收；保留旧决定并标注被取代；核查文档一致性。
- 验收：明确DS尚未真实验证、型号及图片能力需实测；不承诺换通道必然解决超时；下一轮实现任务清楚且不改变A/B业务接口。
