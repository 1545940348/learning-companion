# 技术决策：DS主通道与展示边界

- 依据：负责人最新要求将DS作为底层模型，并反馈平台调用超时，替代此前禁用DS决定。
- 范围：仅修改方案，代码和真实配置没有切换；DS文字、图片、JSON及性能未实测。
- 参赛约束：保留“最好不要包含第三方AI”的用户转述，不能宣称官方已许可；不改写赛事手册。

## 下一轮实施

1. 保留`ModelCaller`，复用DS适配器；默认真实配置显式选deepseek，不再依赖当前auto选择SDK。
2. 确认官方接口及账号可用型号；文字、图片、JSON各自验证，不能从代码接受image_url就推断模型支持视觉。
3. 缺密钥或调用失败明确报错，不自动切SDK/mock。单业务请求60秒总预算包含重试；记录延迟后再判断超时是否改善。
4. 更新配置示例、启动文案、README和健康检查期望；实际切换后health保持deepseek与mock=false。
5. A学生界面可以不常驻模型品牌，但mock状态及AI补充来源继续显著标注。不能修改health为假通道、抹掉真实技术来源或声称DS答案来自LearnBuddy。
6. 既有SDK历史测试保留；DS另建验收记录。生产部署再验证图文真实请求。

本轮不修改上述代码，不读取密钥，不发起外部调用。A/B五个业务接口、依赖四态与按需补充策略保持原约定。
