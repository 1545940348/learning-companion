# ⚠️ 本文件是范例模板，会随仓库一起提交、评委可见 —— 请勿在此填入真实密钥，只保留空值。
# 实际使用时：cp .env.example .env，再把值填进 .env。
# .env 已被 .gitignore 排除，不会被提交。

# ===== 通道选择 =====
# auto（默认）：有 LearnBuddy 密钥走主通道，无密钥自动降级为 mock
# mock：强制 mock，不调用任何真实模型（A 可无密钥独立开发前端）
# sdk：强制主通道（LearnBuddy）；无密钥时不静默降级，避免误把 mock 当成真实能力
# deepseek：强制备选通道（第三方）—— ⚠️ 见下方说明，最终效果呈现不得启用
MODEL_PROVIDER=auto

# ===== 主通道：LearnBuddy（CodeBuddy Agent SDK）=====
# 仅服务端使用，绝不下发到前端。
# 认证由 SDK 读取下列环境变量完成，因此这里没有 BASE_URL；
# 也**没有固定模型名** —— 模型由上游动态分配
# （同日实测 hy3 / glm-5.3 / minimax-m3 等多个不同结果，只能从响应里回读）。
CODEBUDDY_API_KEY=
CODEBUDDY_INTERNET_ENVIRONMENT=internal

# ===== 备选通道：DeepSeek（第三方）=====
# ⚠️ 赛事方原话：「本次赛事以腾讯 LearnBuddy 平台为载体，最终效果呈现时最好不要包含第三方 AI」
# 因此本段配置的通道：
#   · 只有显式设置 MODEL_PROVIDER=deepseek 才会启用，默认不可达；
#   · 不参与自动降级 —— 主通道失败时不会自动切到这里；
#   · 启用后启动日志会打印醒目警告。
# 定位：本地开发对比、规则允许或需要应急时手动启用。
# 不得用于作品的最终效果呈现（演示、评委体验）。
#
# 模型 id 请照抄控制台或文档原文；写错一个字符会直接 404。
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-flash

# ===== 服务端 =====
PORT=3000
NODE_ENV=development

# 模型请求超时（毫秒），对应说明书 5.3 的 60 秒约束
MODEL_TIMEOUT_MS=60000
