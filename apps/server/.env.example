# ⚠️ 本文件是范例模板，会随仓库一起提交、评委可见 —— 请勿在此填入真实密钥，只保留空值。
# 实际使用时：cp .env.example .env，再把值填进 .env。
# .env 已被 .gitignore 排除，不会被提交。

# ===== 通道选择 =====
# deepseek（默认，说明书 V1.4）：DeepSeek API
# sdk：CodeBuddy Agent SDK（历史接入，须显式启用，不参与自动降级）
# mock：仅供本地开发，不调用任何真实模型
#
# 已取消 auto 档 —— 它会因有无 SDK 密钥而在两个通道间摇摆，正是 V1.4 要消除的不确定行为。
# 三档之间没有任何自动降级：缺密钥或模型失败时明确报错，不静默切换。
MODEL_PROVIDER=deepseek

# ===== 默认通道：DeepSeek API =====
# 仅服务端使用，绝不下发到前端。
# 由 apps/server/src/model/deepseek.ts（C 维护的 HTTP 适配器）调用，OpenAI 兼容格式。
#
# 型号 id 请照抄控制台或文档原文；写错一个字符会直接 404。
# 当前值 deepseek-flash 已于 2026-09-17 用真实密钥实测通过（文字与图片调用均通）；
# 若你的账号下型号名不同，请替换为真实可用的型号。
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-flash

# ===== 历史接入：LearnBuddy（CodeBuddy Agent SDK）=====
# 代码保留，但**不参与自动降级**：只有显式设置 MODEL_PROVIDER=sdk 才会启用。
# 认证由 SDK 读取下列环境变量完成，因此这里没有 BASE_URL；
# 也**没有固定模型名** —— 模型由上游动态分配
# （同日实测 hy3 / glm-5.3 / minimax-m3 等多个不同结果，只能从响应里回读）。
#
# 关于超时：SDK 通道实测文字 12–21 秒、图片 17.7 秒，撑不住"30 秒得到解释"的口径，
# 这是 V1.4 改以 DS 为默认通道的直接原因。保留 SDK 仅为历史接入与对照。
CODEBUDDY_API_KEY=
CODEBUDDY_INTERNET_ENVIRONMENT=internal

# ===== 服务端 =====
PORT=3000
NODE_ENV=development

# 模型请求超时（毫秒），对应说明书 5.3 的 60 秒约束。
# 说明书 V1.4：重试须计入同一 60 秒预算，不额外放宽。
MODEL_TIMEOUT_MS=60000
